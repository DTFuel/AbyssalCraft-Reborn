package com.shinoow.abyssalcraft.system.energy;

/**
 * Amplifier types for the Potential Energy system (owned by PS-5), faithful to the 1.12.2
 * {@code EnergyEnum.AmplifierType}. Charms boost a manipulator's transfer along one of these axes:
 * {@code RANGE} (transfer distance), {@code DURATION} (transfer speed), {@code POWER} (transfer amount).
 */
public enum AmplifierType {
    RANGE,
    DURATION,
    POWER
}
