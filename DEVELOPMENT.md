# AbyssalCraft 开发文档 (DEVELOPMENT)

> 本文件是本仓库的"开发记忆"，是开发状态的**唯一事实来源 (single source of truth)**。
> 每完成一项开发或验证后，请更新第 7/8/9 节（当前状态 / TODO / 变更日志）。
> AI 助手（GitHub Copilot）会在每次会话开始时读取本文件，并在推进后回写更新。
>
> 最后更新：2026-07-27

---

# 操作约束 (Operational Rules)

## 工作区外访问需用户批准 (2026-07-27)
- 当需要通过**任何方式**访问本工作区 (`c:\Users\12044\Documents\EX\IDEA_PROJECT\AbyssalCraft`) 之外的内容时，
  必须先通过**申请/提问**向用户请求，**必须由用户亲自批准后**才能继续。
- 适用范围包括但不限于：读取/写入工作区外的文件或目录、访问外部 Skill 库
  (`C:\Users\12044\Documents\EX\IDEA_PROJECT\MC_Dev_Skills`)、抓取网页、访问远程/共享系统等。
- 未获批准前不得访问；获批后方可继续。

## 1. 项目概览

- 目标：将 AbyssalCraft 以**多加载器**方式现代化到 Minecraft 1.20.1 / 1.21.1。
- modid：`abyssalcraft`　　group/包名：`com.shinoow.abyssalcraft`　　主类：`AbyssalCraft`
- 旧版参考源码（1.12.2，**只读，勿改**）：`docs/AbyssalCraft-1.12.2/`
- 依赖的通用 Skill 库（跨项目资产，动手前先读对应 reference）：
  `C:\Users\12044\Documents\EX\IDEA_PROJECT\MC_Dev_Skills`（入口 `SKILL.md` + `references/01..13-*.md`）

## 2. 版本矩阵 / 工具链

| Stonecutter 节点 | MC | 加载器 | 平台版本 (fml) | Java |
|---|---|---|---|---|
| `1.20.1-forge`    | 1.20.1 | Forge    | 47.4.4   | 17 |
| `1.21.1-neoforge` | 1.21.1 | NeoForge | 21.1.193 | 21 |

- 构建栈：Stonecutter 0.9.6 + Architectury Loom 1.11.458 + Gradle 9.0.0（wrapper 已生成，`gradlew`）。
- JDK：本机装有 JDK 21/22；forge 节点所需的 JDK 17 由 `settings.gradle.kts` 的 foojay 插件自动下载。
- 用 JDK 21 运行 Gradle 即可（即使 forge 节点目标是 Java 17）。

## 3. 工程结构（关键文件）

```
settings.gradle.kts          Stonecutter 节点定义 + foojay 工具链解析插件
stonecutter.gradle.kts       活动节点 (stonecutter active) + 加载器常量 match(loader,...)
build.gradle.kts             两加载器共享的 Loom 构建脚本（含 processResources 展开元数据）
gradle.properties            mod.* 元数据 + JVM 参数
versions/1.20.1-forge/gradle.properties      mcVersion / fml / loom.platform=forge
versions/1.21.1-neoforge/gradle.properties   mcVersion / fml / loom.platform=neoforge
src/main/java/com/shinoow/abyssalcraft/AbyssalCraft.java   跨加载器主类
src/main/resources/
  META-INF/mods.toml           Forge 元数据（${...} 由 processResources 展开）
  META-INF/neoforge.mods.toml  NeoForge 元数据（type="required" 方言）
  abyssalcraft.mixins.json     空 Mixin 配置（待添加）
  pack.mcmeta
  assets/abyssalcraft/lang/en_us.json
```

产物：`versions/<node>/build/libs/abyssalcraft-<loader>-0.1.0+<mc>.jar`

## 4. 常用命令（均已验证可用）

```powershell
./gradlew build                       # 构建全部节点
./gradlew :1.20.1-forge:build         # 只构建 Forge
./gradlew :1.21.1-neoforge:build      # 只构建 NeoForge
./gradlew :1.20.1-forge:runClient     # 启动 Forge 客户端（当前活动节点）
./scripts/run_rr_client_smoke.ps1     # 双节点抵标题屏 marker 后自动正常退出
./scripts/run_rr_server_matrix.ps1    # 双节点固定 seed 新世界 + 持久化重启矩阵
./gradlew releaseAudit                # 对现有双节点 production JAR 执行九项只读审计
./gradlew :1.20.1-forge:compileJava --rerun-tasks   # 新增 .java 文件后强制重编（见第 5 节坑）
```

- 首次 `./gradlew` 会下载 Gradle 9.0.0 发行版（约 128MB）。
- 切换"活动节点"（决定 `src/` 预处理成哪个加载器视图）：改 `stonecutter.gradle.kts` 的
  `stonecutter active "..."`，或用 Stonecutter 生成的切换任务。提交前应 Reset 活动节点。

## 5. 开发约定 (Conventions)

- **注册**统一用 `DeferredRegister`，按类型分组到 `registry/` 下的 `ModItems`/`ModBlocks`/`ModEntities`… 前缀保持一致。挂到 MOD 总线：`REGISTER.register(modBus)`。
- **跨加载器隔离**：仅隔离真正不同的部分（`@Mod`/`IEventBus`、命令事件、1.20.5 移动的包的 import），游戏逻辑共享。语法：`//? if forge { … //?} else { /* … *///?}`。
- **主类模式**：Forge = 无参构造 + `FMLJavaModLoadingContext.get()`（deprecated-for-removal 但可用，加 `@SuppressWarnings("removal")`）；NeoForge = `(IEventBus)` 注入构造；两者共用 `init(IEventBus)`。
- **新增 .java 文件后** `compileJava` 可能显示 UP-TO-DATE（Stonecutter 未重新生成）→ 运行期 ClassNotFound。修复：`./gradlew :<node>:compileJava --rerun-tasks`。
- **连续切 Stonecutter 节点**：同一会话连续跑 Forge→Neo 任务时，目标节点的 `build/generated/stonecutter` 偶尔残留上一节点分支，表现为 Neo 大量解析 `net.minecraftforge.*`。先跑 `:<target>:clean`，再单独执行该节点任务；本仓库 2026-07-24 一手复现并由清理后重编消除。
- 1.20.1 的 datapack 目录用**复数**：`loot_tables/` `advancements/` `recipes/` `structures/`（复数=1.20.1，单数=1.21+）。放错目录会被静默忽略。
- **Mixin**：不要 mixin 自己的 mod 类（dev 下静默失效），注入原版类；Forge dev 必须在 `build.gradle.kts` 里 `forge{ mixinConfig("abyssalcraft.mixins.json") }` 注册（已配）。
- **子系统文档分层**：子系统的详细设计 / 内契约 / 实现记忆 / 踩坑 / 逐项验证写 `docs/spec/<子系统>.md`（见 [`docs/spec/README.md`](docs/spec/README.md) 的分工表、模板与索引）。本文件（§7/§9）与 `docs/porting/` 主文档只留必要状态 + 一行进度 + 指向 spec 的链接，避免子系统细节堆积漂移。
- **延后纪律（2026-07-22）**：除非有非常必要的硬理由（跨版本/加载器深分叉风险、未移植前置依赖、跨 owner 归属），否则**禁止延后任务**。必须延后时须：①文档写明「延后原因 + 依赖 + 已就位物料」；②分离为显式可跟踪的后续任务（如 `PH-xb`）进 `docs/porting/` 两张任务表并从父任务反向指针链接；③不得只留一句"延后"。详见[设计案 §2](docs/porting/00-porting-design.md)。
- **用户验证隔离（2026-07-27）**：真人交互/视觉/听觉统一归 [`03-user-validation-plan.md`](docs/porting/03-user-validation-plan.md) 的 `U-*`。R4-R7 Agent 任务与 AUTO/CODE Gate 不依赖用户；失败新建 `FIX-U-*` 回交源码 owner。只有最终 `U-GATE` 阻塞 M11。
- **并行运行纪律**：Agent 可并行编辑不重叠的 Owns，但 Stonecutter 切节点、`runData`、`build`、`runClient` 与联网矩阵由 Gate Integrator 串行执行；注册/客户端/datagen relay、`platform/**`、lang 与三张状态文档只由 Gate Integrator 回写。

## 6. 已知坑 (verified gotchas)

- **Loom 1.11**：`loom{ mixin{} }` 里设 `defaultRefmapName` 必须同时 `useLegacyMixinAp = true`，否则配置期报 `You need to set useLegacyMixinAp = true`。（已配）
- **Gradle 9 的 wrapper 任务**默认联网校验 distributionUrl 会失败 → 生成时加 `--no-validate-url`；且该任务要求目录内有 settings 文件。
- 本机无系统 Gradle：wrapper 由下载解压的 `gradle-9.0.0-bin.zip` 生成。
- **PowerShell 持久终端**：多行命令首行会被吞字符 → 一律用单行命令。
- **Gradle 9 daemon registry 损坏**：若启动前即报 `Could not read cache value ... daemon/9.0.0/registry.bin` / `StreamCorruptedException`，这是用户级 Gradle daemon 注册表损坏，不是源码编译失败；确认无 Gradle 进程后备份/删除该生成文件，再重跑双节点门禁。

## 7. 当前状态 (Current Status) —— 随开发随时更新

> **状态审计（2026-07-28，当前完成度唯一判据）**：以 [`docs/porting/01-porting-task-plan.md`](docs/porting/01-porting-task-plan.md) 的 `☑/☐` 拆分任务、[`docs/porting/02-porting-parallel-tasks.md` §3A](docs/porting/02-porting-parallel-tasks.md#3a-审计后当前执行队列唯一状态源) 的 Agent 队列，以及独立的 [`docs/porting/03-user-validation-plan.md`](docs/porting/03-user-validation-plan.md) 为准。本节下方的长篇交付记录是历史证据；其中“阶段通过”“框架交付”“标题屏加载”“注册/自测通过”都**不**代表完整原版玩法、内容或人工视觉验收已完成。
>
> Agent 自动实现与 Gate 已完成至 `M10`：R4/R5 代码 Gate、世界保真矩阵、27/27 disruption、NET/GUI/FX/JEI/SYSTEM/ADV-API、全资产/datagen、compat/rename、双节点 build/server/restart 与隔离客户端 TitleScreen smoke 均☑。标题屏 smoke 只证明客户端初始化和资源烘焙，不替代真人视觉、音频、交互或玩法验收。
>
> 当前仅剩独立真人车道 `U-*`/`U-GATE`，以及明确依赖 `U-GATE` 的 T11.3/T11.4/R8-Gate/M11。自动报告、构建产物和发布预检不能代替这些真人证据，也不得提前关闭最终发布 Gate。

- [x] 2026-07-31 **刷怪蛋语义与 Anvil Forging 收口（GitHub Copilot）**：旧 `spawn_egg.json` 不是任意实体蛋机制，而是 `ODB → EntityTag shadowboss` 的 Sacthoth NBT 蛋；现代已有专用 `shadowboss_spawn_egg`，故该旧配方正式 RETIRED，crafting 四态更新为 `339/61/0/1`。恢复 1.12.2 全部 74 条 Anvil Forging（24 charm + 40 PE 网络块 + 10 Sacrificial Altar 升级）、双目录数据与 `AnvilUpdateEvent` 执行链；补回五级独立献祭祭坛的生命力采集、PE持久化/物品输出、模型/BER/语言。Forge/Neo live RecipeManager 均输出 `RR_ANVIL_FORGING_RUNTIME_OK recipes=74 ... altarUpgrade=ok`，Forge `runData` 输出 `migrated=74 retired=0 blocked=0` 与全资源闭包，双端完整 build 通过。

- [x] 2026-07-28 **R4-R7 自动证据最终对账（GitHub Copilot）**：Forge/Neo 固定 seed 新世界+重启矩阵均通过；官方 loader `PotentialSpawns` 查询在两端输出 `callbacks=11 applied=2 dimensions=5 weights=exact groups=exact`。真实 ServerLevel 食物 fixture 覆盖 8 种效果食物、2 种解毒剂、10 剂量及玻璃瓶返回。Forge datagen 输出 JEI 12 分类、27 disruption/0 blocked、401 actionable 书页、13 Screen、94/94 clientvars、145/145 配置消费者、5 附魔效果、复活矩阵、`RR_ASSET_ITEM_AUDIT_OK items=560`、`RR_ASSET_LANG_AUDIT_OK languages=8 keys=1361 blocks=232 items=560`、`RR_BER_HOST_CLOSURE_OK registered=4 noBer=21 total=25 deferredLegacy=0` 与 `RR_DATAGEN_AUDIT_OK missing=0 json=2617 logical=1628`。demo生产注册/资产/loot/lang已清零；资产审计闭合644旧纹理与42复杂模型，并新增雕像逐面OBJ UV、晶簇动画及势能分层模型Gate；compat/rename分别为`businessLoaderReferences=0`、`stale=0`；双节点build与隔离客户端smoke输出`RR_CLIENT_SMOKE_RUNNER_OK nodes=2`。据此所有Agent自动任务和M10均已关闭；全部`U-*`、T11.3/T11.4与M11仍保持未完成。

- [x] 2026-07-27 **RR-SERVER/T11.2 + 发布自动预检（GitHub Copilot）**：`run_rr_server_matrix.ps1` 以固定 seed `1251393890` 在 Forge/Neo 各执行新世界与 SavedData 重启，共四阶段输出 `RR_SERVER_MATRIX_RUNNER_OK nodes=2 phases=4`；同一路径强制验证 4 AC 维度、37 结构覆盖、11 个 vanilla 自然刷怪场景、97 张 live entity loot、219 machine recipe、AW/DL 19 坐标 FULL chunk 性能与复活持久化。新增隔离 gameDir 的 `run_rr_client_smoke.ps1`，两端须连续两 tick 到真实 `TitleScreen`、无 AC 资源/生命周期错误后正常退出，输出 `RR_CLIENT_SMOKE_RUNNER_OK nodes=2`；该 smoke 不替代视觉/交互验收。最终双节点 build 与九项 release audit 通过，production JAR SHA-256 为 Forge `1a3f577dd5abb304cdfc9d3cfed972241ef8517213641b69c8eb31a0462b2fc2`、Neo `0c59ebc25856c7e3877213a14c40db549be515f07c8829a81cc9b03eceb3d400`，资源索引 `matched=3245 allowed=15`，临时验证残留为 0。该日 crafting/smelting/tag 快照为 `339/61/1/0`、`52/1/0/0`、`181/351`；刷怪蛋结论与 crafting 四态已由 2026-07-31 条目更新。所有 `U-*`、T11.1、T11.3/T11.4 与 M11 仍保持未完成。

- [x] 2026-07-26 **RR-ADV-API · 命令/进度/插件接口收口（GitHub Copilot）**：修复 Forge 9条错误的1.20对象式item predicate与Neo 1.21 icon/predicate schema，root恢复真实Necronomicon与现有darkstone背景；两端`AdvancementEarnEvent`将9项幂等持久化为独立Progression书知识，type7+full snapshot即时刷新已打开书并登录回填旧玩家。`IACPlugin`由ServiceLoader/显式入口发现，在ServerAboutToStart原子发布五类实体扩展；Shoggoth与两瘟疫真实消费，Forge另桥接5个旧IMC key，13个配方/Crystal/Ghoul贴图key迁移到datapack/resource pack。双端compile/runData永久Gate、9进度专服加载、外部fixture消费者均通过；Forge真实联网无权限拒绝、OP两次toggle、9进度/书页、重复幂等与同名重连客户端同步通过。临时fixture不进入生产源码/JAR。

- [x] 2026-07-26 **RR-CLIENT-FX-AUTO · 实现与自动 Gate（GitHub Copilot，CR-73）**：新 `platform/DimensionSkyCompat` 吸收 renderSky 双端签名 fork（Forge PoseStack ↔ Neo Matrix4f）与 1.20/1.21 即时顶点 fork，四维 tinted 六面天空盒经 `ACDimensionEffects`/`ACDimensionSkies` 绘制、色实时取自 `ClientVars`（AW 0/105/45、DL 100/14/14、Omothol 40/30/40、DarkRealm 30/20/30 复用 omothol_sky）。迁 blueflame.png + 注册 `blue_flame`/`BlueFlameParticle`，`ClientRitualEffects` 8 基座发 BlueFlame+smoke+ItemRitual（供品由 `RitualManifestCatalog` 重建，不扩 `RitualStartMessage`）；`AbstractShoggoth.playStepSound` 补最后一个 `shoggoth.step`并修正 `jzahar.shout` 字幕键。永久 `ClientFxSelfTest` 双端 runData `RR_CLIENT_FX_SELF_TEST_OK skies=3 particles=2 sounds=45 ogg=106 subtitles=41 rituals=62`，双端 compile/build/JAR 通过。PEStream 由 RR-NET（`PEUtils`/`ClientNetworkEffects.peStream`）交付，未重复。T6.3b/T6.4b/T6.5b☑；四维天空与声音/字幕真人矩阵独立归 `U-FX` ☐，不阻塞 R5-CODE-GATE。

- [x] 2026-07-26 **RR-RITUAL-SPELL-PORTAL · R4 实现与自动 Gate（GitHub Copilot）**：以旧源码冻结 62 个仪式注册项（40 infusion/3 creation/1 transformation/18 specialized）与 14 法术；祭坛恢复持久 ceremony、20t PE、原子供品、活祭/research/disruption/重启安全，18/18 专用行为含 Portal、Boss、复活、5 biome、enchant/weather/house；法术恢复六卷轴、instant/50t charging、多容器PE回滚、绝对伤害/PvP门、MobSpell目标提示+服务端重验、7槽Spellbook与旧统一铭文overlay。Portal Anchor/BE、Gateway/Silver Keys、目标同步、renderer与持久关联已接。必要旧资产、29 item model、3 block set、语言与 Coralium Pearl 掉落完成。Forge/Neo `compileJava`、`runData`、production `build` 全绿：`rituals=62 handlers=18`、`spells=14 handlers=14 spellbook=14`、`itemModels=29 blockSets=3 damageTags=4`、Portal=`dimensions=7 edges=6 keyTiers=4`；双 JAR 关键7项审计零缺失。Forge/Neo 真人 Portal、Spellbook/卷轴与仪式视觉听觉矩阵现独立归 `U-R4`，不阻塞 R5 Agent。

- [x] 2026-07-26 **RR-ENTITY-BEHAVIOR · M3-CONTENT（GitHub Copilot，CR-72）**：全族专属行为与 9 legacy 边角完成；69 旧 loot 建立永久四态审计并由单一 datagen owner 生成 97 逻辑/194 物理现代表，Boss 奖励不再硬编码。双端通过行为矩阵、97表/69死亡路径、11场真实 `NaturalSpawner`、5实体+5 owner create→stop→verify；永久 runData Gate、production build/JAR 与无属性专服 `/reload`/stop 全绿。`SpawnCandidateCompat` 修复只读候选列表和稳定 `SpawnerData` 身份；临时 fixture class、属性与快照残留均为0。Forge/Neo JAR SHA-256 分别为 `13DAFBEBD5F666B32B2E4A63A8D4F43124105790801B38937463E41F1B9D3A5C` / `BBA3A29203EDEC30720EC17DEC12DFF81E72F35706276207D0B7B7880AD7EF7F`。

- [x] 2026-07-25 **RR-RENDER-VISUAL · 双端总场景目视收口**：Forge/NeoForge 各以一次 `/acrrrender all` 覆盖 Legacy、effect/projectile/ODB、动态层与持物、7套护甲及特殊宿主、Boss death 代表帧、Research/Pedestal BER，用户最终确认无问题。期间修复 Chagaroth 系列误挂 carrier、Goliath 甲层遮骨、Dreadguard carrier 朝向、Gecko 持物矩阵、三套护甲 alpha、Anti humanoid 缺基础甲层、Skeleton 细肢外层与 Anti Ghoul 错误人形模型。临时命令、setter、名字旁路及生命周期冻结已删除；双端完整 production build 与 JAR 审计通过，关键渲染条目齐、三张修复贴图哈希一致、验证残留为0。`T4.6d` 缺宿主 BER 继续独立未完成。

- [x] 2026-07-25 **RR-KNOWLEDGE · R3知识实现切片（GitHub Copilot，CR-70）**：完成 Forge capability/Neo attachment 的 necrodata mutation 与协议v2同步、42 research/42 conditions（33+9）/11 offerings、type5/6与分类研究目录、三DamageType、五效果/七药水、plague传播/carrier/immunity/两解毒/唯一转化、Purged六类交互、食物与六条brewing、命名非Boss死亡快照生产，以及复杂配置不可变解析与load/reload。双端runData输出`RR_KNOWLEDGE_SELF_TEST_OK research=42 conditions=42 offerings=11`，双端build/server/production JAR通过；Forge黑盒证三damage、antidote、Antimatter、多效果唯一生成和压缩NBT重启，Neo证三damage与Antimatter→AntiPig。**仍未完成**：活玩家死亡/重连实网矩阵、artifact/page/whisper与完整旧书actions、Dread Plague动态群系扩散、复活仪式消费、全配置消费者/GUI，已拆成独立任务。

- [x] 2026-07-25 **R3-Gate（GitHub Copilot，CR-71）**：RR-ENERGY与RR-KNOWLEDGE两个直接前置均完成；同一最终源码的双端runData同时输出两类永久Gate，双端production build/JAR、专服与重启门禁通过，临时验证世界与探针清理。R3系统API/存档/配置双节点Gate完成并解锁R4；`M7-PLAYABLE`和所有拆出的`T7.*c/T8.2c`仍保持未完成。

- [x] 2026-07-25 **R2-AUTO-GATE / R2-Gate（GitHub Copilot，CR-69）**：新增永久datagen不变量`R2GateSelfTest`，验证91个总registrar中12个R2 registrar唯一挂载、Research/Brewing/Transfer注册ID与3菜单宿主、MenuHost/Transfer capability bootstrap；客户端真实loader回调验证7 Screen、64实体renderer、25 model layer与2 BER唯一接入。双端`compileJava`、`runData`全部既有RR标记+`R2_GATE_SELF_TEST_OK`、1594 generated JSON零错、分别production build；无属性专服25593/25594到`Done`、`/reload`成功且`acrrrender` Unknown；双端无属性客户端到Sound Engine/atlas并输出三类`R2_GATE_CLIENT_*_OK`。production JAR元数据互斥、448×2 recipe、181×2 block loot、37结构模板正确分目录，R2关键类齐全，旧RR-DATA与Render验证探针均为0。Forge仍有既知9个advancement格式错误、旧世界缺失datapack引用/注册ID重排警告；Neo有既知subtitle缺键，均归后续任务。Gate交付时两条R2-FIDELITY尚未验；同日后续 RR-RENDER-VISUAL 已收口，RR-WORLD-FIDELITY仍☐。

- [x] 2026-07-25 **RR-WORLD · 现代混合保真自动切片（GitHub Copilot，CR-68）**：四维地形、真实 Liquid Coralium/Antimatter/dead log、carver、第六 Coralium Infested Swamp、Dark Realm 固定 cavity hash、36 旧 NBT 确定性转换+第37 House、全模板布局、Chains 与 Mineshaft/Stronghold 拓扑全部落地；新增仅作用于两个 AC structure ID 的 palette/loot Mixin，双端实测 Dreadwood/Abyssal brick 与 AC mineshaft loot。494 JSON 解析、双端 compile/runData/生产 JAR、Forge/Neo 专服矩阵与 Neo 同世界重启通过；JAR 模板目录分别为 Forge 37复数/Neo 37单数。**仍未完成**：旧噪声逐位 oracle、动态 marker 对应真实 crate/spawner/pedestal/lock 等玩法、地形/结构人工视觉与自然生态/玩家 portal 最终矩阵，已拆入 `RR-WORLD-FIDELITY`/T5.2c/3c/4c/6c/9b。详见 [`worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md) §14。

- [x] 2026-07-25 **RR-MENU-HOST · Research/Brewing/Transfer 三切片（GitHub Copilot，CR-67）**：Research Table 恢复 176×238 专用资源、方块朝向/形状/光粒子和真实 36 槽 Screen；Sequential Brewing 恢复 8 槽/槽面自动化、brewing hook、remainder、输出堵塞、20t 邻机传递与 400t 动态 GUI；Spirit Tablet 恢复 5 槽过滤、两按钮、主副手宿主锁，Forge 全 BE capability/Neo attachment 与 Spirit Altar 16 格扫描/20t 调度。双端 `runData` `RR_MENU_HOST_SELF_TEST_OK`、原版箱子 seed/restart `3/1→2/2`、连续两级酿造 `ticks=820` 通过；Forge/Neo 真实联网客户端三 Screen 的库存点击、shift-click、按钮、主副手重开与六张实际截图矩阵均输出 `RR_MENU_HOST_CLIENT_MATRIX_OK`。目视发现并修复 Research/Tablet 深色标签对比度。临时夹具、命令、属性入口已删除；发布态双端编译与 JAR 门禁通过。详见 [`machine-subsystem.md`](docs/spec/machine-subsystem.md) §0A 与 [`item-transfer-subsystem.md`](docs/spec/item-transfer-subsystem.md)。

- [x] 2026-07-24 **RR-RENDER-AUTO · 实现与自动门禁（CR-65）**：九 legacy renderer/model 与 12 effect stand-in 清零；恢复羊毛/eyes/持物/头、Star Spawn/Dread carrier、Shadow brightness alpha、Boss 服务端权威长死亡与 Jzahar/Dragon rays。Samurai 3D、Depths inner/outer、Ghoul/Dreadguard 怪物甲及 Research Table/Pedestal BER 已接；Goliath 怪物甲后因覆盖头骨/胸骨而按用户要求完全移除，装备仍保留属性但不渲染。双端 `compileJava`/全量 `build`，Forge/Neo `runClient` 抵 Sound Engine+atlas，0 renderer/layer/AC texture/GeckoLib 异常；JAR required entries 齐、render validation=0。该自动切片交付时尚缺双端视觉与T4.6d；视觉矩阵已于2026-07-25收口，T4.6d仍独立待宿主。

- [x] 2026-07-24 **RR-DATA · M1 recipe/tag/loot（Agent A，CR-66）**：旧资源402个JSON中401个有效 crafting recipe（另1个为13项 `_constants.json`）完成四态闭包 `305 MIGRATED / 61 REPLACED / 35 BLOCKED / 0 RETIRED`；`AbyssalCrafting` 的53项展开 smelting/回收闭包为 `51/1/1/0`。新增 `LegacyCraftingRecipeData`、`CookingRecipeData`、`ACTagData`、`OreLootData`；ID以旧注册源码链为权威，显式解 Ethaxium brick block/item冲突，去除 `dltplank_alt` 语义重复。M1 tags为177逻辑/343物理文件（公共双写、loader专属单写）；13矿均有Silk Touch，6材料矿保留数量+Fortune+explosion，7矿普通掉自身。**采集根修复**：Forge 1.20四级AC工具注册进官方 `TierSortingRegistry`，Neo 1.21用`incorrect_for_*`，旧level 2/3/4/5矩阵统一；双端真实服务器 `9 tools × 13 ores = 117`项全部通过。代表性真实RecipeManager/loot矩阵在Forge/Neo及`/reload`后通过（3 crafting、2 smelting、13普通+Silk、6 Fortune）。双端runData四标记通过，production build/JAR审计中单双recipe各448、block loot各181、JSON/重复/探针残留=0。35个crafting BLOCKED及1个smelting BLOCKED保留真实下游内容依赖，不伪报可用。旧`smelting_recipes`配置键保留但弃用，recipe由datapack控制。详见[`docs/spec/item-content.md`](docs/spec/item-content.md) §5f及两份`rr-data-*-audit.csv`。

- [x] 2026-07-24 **R1 Gate · 双端机器 UX/重启矩阵收口（Agent A）**：Forge 1.20.1 与 NeoForge 1.21.1 均以真实联网玩家打开实际 Crystal Bag/Crystallizer/Materializer/Transmutator Menu 与 Screen，通过正常容器网络包完成逐槽手放、shift-click、Materializer 结果制作、双输出取出、容器余留物、玩家 XP 与工作态视觉。Bag 两端均验证 54 槽、宿主热栏槽锁定及 `2+3=5` 关闭重开持久化；Crystallizer 双输出各 10、`xp=2`，Transmutator 输出 20、空桶返还、`xp=4`。**精确重启证据**：Neo 重启前后 `Progress=129/BurnTime=672/input=56/fuel=63/output=8+8` 与完整 ledger 一致，Screen=`0.645/0.28`；Forge 重启前后 `Progress=80/BurnTime=2121/input=63/fuel=63/output=1+1` 与两条 ledger 一致，Screen=`0.4/0.88375`。临时属性门控验证命令、Screen hook 与冻结路径已全部删除，源码 grep 为 0。清理后双端 `compileJava`、`runData` 三类 R1 标记、同一 invocation 全量 `build`，以及无验证属性 dedicated server `Done`/干净 stop 全部通过。`RR-MACHINE-UX` 与 `R1-Gate` 现为 ☑，解锁 R2。

- [x] 2026-07-24 **RR-ENTITY-CATALOG · 九漏实体/生态/Darklands（Agent C）**：补齐 `abyssalzombie/coraliumsquid/dreadling/dreadspawn/greaterdreadspawn/lesserdreadbeast/shadowcreature/shadowmonster/shadowbeast`，目录 invariant 精确通过 `content=63 all_ac=64 eggs=48 placements=44`。九实体具属性/分类、基础 AI、蛋、loot 双目录与核心服务端行为；双端各 9 个镜像 spawn modifier，`SpawnCandidateCompat` 补 AW/DL 低层 Shadow 与 Dreadlands Darklands 候选。required TerraBlender 注入五 Darklands、surface rules 与树特征。**验证**：双节点 `runData`、完整 `build`、全新专服 `Done`、`/reload`、九 `/summon`、五 Darklands 定位、AW 负测/Dreadlands 正测、Hard 村民→单一 Abyssal Zombie 均通过；Forge 另证 Dread `5→1` 与 `1→2`。仍未完成：自然生成统计、九实体边角行为矩阵、第六 `coralium_infested_swamp`、旧 69 loot 全量及九实体忠实客户端渲染。详见 [`docs/spec/entity-subsystem.md`](docs/spec/entity-subsystem.md) §11A 与 [`docs/spec/worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md) §13。

- [x] 2026-07-23 **RR-MACHINE-ENGINE · 三试点机器忠实引擎（Agent A）**：三类 `ProcessingRecipe` alias 已拆为中央 `ModRecipes` 唯一持有的 `CrystallizationRecipe`（双输出+XP+time）/`MaterializationRecipe`（1–5 counted crystal）/`TransmutationRecipe`（XP+time）及双版本 serializer。Crystallizer 恢复 4 槽/专用燃料/remainder/sided automation/FACING+LIT/双输出 XP ledger；Materializer 恢复 bag+book 两真实槽、18 虚拟结果分页、recipe-ID 原子扣晶体和旧三槽安全迁移；Transmutator 恢复专用燃料/remainder/sided automation/FACING+LIT/XP ledger。新增四级 Crystal Bag 数据层与 Forge/Neo item capability compat。**验证**：两端 `compileJava`、`runData` `RR_MACHINE_SELF_TEST_OK`（Bag/custom-data、完整 shape、NBT/XP ledger、侧面槽位、Materializer 迁移）、全量 `build`；两端 server codec 加载；Neo 实际 stone→darkstone、Crystallizer 双输出，远区块重启无漂移恢复 `Progress=80/BurnTime=2321`。两端 client 抵 `Sound engine started` 且本任务 ID 无资源警告。其后四级 Bag 入口、Liquid Coralium/Transmutation Gem/cluster 内容与双端活玩家矩阵已由 2026-07-24 R1 Gate 收口；JEI 两燃料分类、忠实美术和旧 223 配方对账仍归后续任务。详见 [`docs/spec/machine-subsystem.md`](docs/spec/machine-subsystem.md) §0。

- [x] 2026-07-20 多加载器脚手架搭建完成；`gradle build` 两节点均产出 remap 后的生产 jar。
  - 已验证：配置、Stonecutter 预处理、编译（零告警）、打包、`mods.toml`/`neoforge.mods.toml` 占位符展开与按加载器互斥排除（解包 jar 核实）。
- [x] 2026-07-21 **运行期加载已验证（服务端·两节点）**：`:1.20.1-forge:runServer`（`Done (11.082s)`）与 `:1.21.1-neoforge:runServer`（`Done (4.093s)`）均加载 mod（日志见 `AbyssalCraft (abyssalcraft) starting up`）、全注册表启动、`stop` 干净关服（BUILD SUCCESSFUL）。判据 = run 日志 + 退出码。neoforge Mod List 含 `AbyssalCraft 0.1.0 (abyssalcraft)`。唯一 ERROR 为首启缺 `server.properties`（vanilla 自动创建，非致命）。
- [x] 2026-07-21 **`loaderVersion="[4,)"` 已运行期核实（R4）**：NeoForge 21.1.193（FML loader 4.0.41）接受该约束，mod 正常加载未被拒。
- [x] 2026-07-21 **PA-2 注册聚合骨架 + 创造栏 + 主类挂载（Agent 2）**：`registry/ModRegistries`（`ALL` 聚合列表）+ `registry/ModCreativeTabs`（空 AC 创造页）+ 主类 `init` 挂载 `ALL.forEach(r -> r.attach(bus))`。两节点 `build` 绿、6 `.class` 齐、javap 核 `attach(IEventBus)` 字节码、forge `runServer`→`Done (2.168s)` CREATIVE_MODE_TAB 冻结无错。`registry/` 零分叉 API（`IEventBus` 仅主类，见平行表 CR-2）。空栏“可见”属客户端可视、需 opt-in `runClient`。
- [x] 2026-07-21 **PA-4 datagen 基建（Agent 4）**：新增 `platform/DataGenCompat`（GatherDataEvent 分叉仅 import；两加载器方法同签名经 jar 核）+ `data/ACDataGenerators`（※ 聚合，零分叉）+ `data/ACExampleProvider`（vanilla `DataProvider` 空壳）+ `build.gradle.kts` data run config（`data()`+programArgs，经 javap 核 `RunConfigSettings` API）+ 主类 `init` 挂 `DataGenCompat.register` + en_us 种子。**两节点 `runData` 均 Starting provider→written**，产出 `src/main/generated/data/abyssalcraft/datagen_smoke.json`（disk 核实）；两节点 `build` 绿。见平行表 CR-4。坑：run-config lambda 内 `property()` 遮蔽 → `kotlin.Unit`；`file()` 相对子项目 → 改 `rootProject.file`。遗留：pack.mcmeta `pack_format=15` 未按节点区分（neoforge 1.21.1 warning 级，非致命）。
- [x] 2026-07-21 **Gate A2 + Gate A3 已过（两节点·集成验证）**：主类 `init` 永久挂载 `ACConfig.register()`（CR-3 计划的 Gate A2 集成）。**Gate A2**：两节点 `build` 绿 + `runServer` 抵 `Done`（forge 2.344s / neoforge 0.955s）、`run/config/abyssalcraft-common.toml` 两节点均以 11 键默认值重生（disk 核）、创造栏 tab 注册无错。**Gate A3**：两节点 `runData` 产出 `datagen_smoke.json` 无报错。判据 = run 日志/退出码 + disk 文件。
- [x] 2026-07-21 **PA-5 最小竖切 + Gate A4 = M0 冻结（Agent 5，两节点）**：新增 `content/demo/DemoBlock`（vanilla `Block` 子类）+ `registry/DemoRegistrar`（demo 方块/方块物品/物品 + 专属创造页 `abyssalcraft:demo`）+ 手写资产（blockstate/模型引用原版 obsidian/nether_star 贴图，免二进制 PNG）+ en_us 三键；Gate A4 集成把 `DemoRegistrar.{BLOCKS,ITEMS,TABS}` 接入 `ModRegistries.ALL`（见平行表 CR-5）。demo 全 vanilla API 零 `//?`（Block/Item/BlockItem/CreativeModeTab 构造经两节点 jar javap 核同签名）。两节点 `compileJava --rerun-tasks` 绿；两节点 `runClient` 加载 mod→资源/模型烘焙**零错**（无 `Unable to load model`、无 demo_* 报错）→抵标题屏→`Stopping!` 干净退出（BUILD SUCCESSFUL）；neoforge Mod List 含 `AbyssalCraft 0.1.0`。判据 = run 日志/退出码 + 资源烘焙无 model 报错。
- [x] 2026-07-21 **客户端运行期已跑（`runClient`·两节点）**：两节点 dev client 均加载 mod、完成资源/模型烘焙零错、抵标题屏、`Stopping!` 干净退出（BUILD SUCCESSFUL）。**如实标注**：仅可视交互（创造页可见/可放置、进世界放置方块）未机核，需人工目视；用户已开窗（forge ~1m38s / neoforge ~2m24s）后手动关闭。
- [x] 2026-07-21 **PP-1 试点机器框架（BE/菜单/配方）+ 试点物品 + Gate P1（Agent 2，两节点）**：`content/blockentity/base/MachineBlockEntity`(+registrar，`Container`+`ContainerData`+save/load 基类)、`content/menu/base/MachineMenu`(+registrar，`AbstractContainerMenu` 基类、MenuType 经 `MenuCompat`)、`content/recipe/base/ProcessingRecipe`(+registrar，单输入处理配方)、`content/item/crystal/CrystalItems`(2 试点物品)。新增兼容层 `ContainerCompat`/`RecipeCompat`/`RecipeSerializerCompat` + 增强 `BlockEntityCompat`(save/load 加 `HolderLookup.Provider`)——吸收 1.21 Recipe/RecipeSerializer/ItemStack-NBT 大改，业务 `content/` 与 registrar 零 `//?`（见平行表 CR-6）。5 registrar 接入 `ModRegistries.ALL`。recipe 两版签名先 `javap` 核 MC jar 后写。两节点 `build` 绿 + `runServer`→`Done`（forge 2.340s / neo 0.970s）：ITEM/BLOCK_ENTITY_TYPE/MENU/RECIPE_TYPE/RECIPE_SERIALIZER 注册表冻结无错（1.21 `MapCodec`/`StreamCodec` 构造运行期无错）。**限制**：试点无机器方块（P2 提供）→ BE/菜单/tick 运行期未被调用（仅编译+注册证明）；无资产（图标/名称/配方 JSON 留 P2/PK）。
- [x] 2026-07-21 **PP-2 Crystallizer 竖切（Agent 6，两节点）**：结晶器 方块/BE/菜单/屏幕/`crystallization` 配方，镜像 Materializer 复用 PP-1 框架+兼容层（业务零 `//?`）。两节点 `build`+`runServer`+`runClient` 绿：注册表冻结、配方解析（forge 9 / neo 1292 recipes）、模型/屏幕烘焙均零错、抵标题屏。旁修 PP-4 `TransmutatorRegistry` 菜单自引用编译 bug（1 行，解锁构建）。**细节见 [`docs/spec/machine-subsystem.md`](docs/spec/machine-subsystem.md)**；见平行表 CR-7。
- [x] 2026-07-21 **PP-3 接手复核 + PP-4 客户端资产补完（Agent 6，两节点）**：接手排查「另一 agent 机器出问题·疑似任务冲突」，定位 **PP-4 Transmutator 缺 3 个客户端资产**（`blockstates/transmutator.json` + `models/block/transmutator.json` + `models/item/transmutator.json`）——Agent 3 标 ☑ 但仅跑 `runServer`（服务端不加载客户端模型）、`runClient` 可视留 P3 → 缺资产未察觉、运行期紫黑缺失模型方块 + 无图标物品。补齐三资产（镜像 materializer，`cube_all` + `minecraft:block/smithing_table_top`，两版皆存在）；两节点 `build` 绿（jar 含三资产，ZipFile 核）+ 两节点 `runClient` 核三机器模型烘焙**零告警**、干净退出（补上 Agent 3 推迟的可视验证）。另复核 **PP-3 Materializer**（代码/资产/配方齐全、与 Crystallizer 逐行一致）→ 竖切成立。**细节见 [`docs/spec/machine-subsystem.md`](docs/spec/machine-subsystem.md)**；见平行表 CR-9。
- [x] 2026-07-21 **PP-5 JEI 试点 + Gate MP 构建/加载层（Agent 6，两节点）**：`integration/jei/{ACJEIPlugin,MachineRecipeCategory}`——三机器各一 JEI 分类（输入→箭头→输出 + 燃料火焰，JEI 内置 drawable 免贴图）+ 三机器方块催化剂 + pilot_fuel 信息页。**业务零 `//?`**：javap 双 jar 核 JEI 15(forge)/19(neo) 所用 API 逐方法同签名，唯一版本分叉（`getAllRecipesFor`：1.20 `List<T>` / 1.21 `List<RecipeHolder<T>>`）落 `platform/RecipeCompat.allOfType`。JEI 走 `modCompileOnly`(API) + `modLocalRuntime`(全 jar，dev 专属、不入发布)，`@JeiPlugin` 自动发现 → **缺 JEI 不崩**（无外部引用 / 无 mods.toml 硬依赖，grep 核）。两节点 `build`+`runClient` 绿：JEI 加载（forge 15.20.0.134 / neo 19.39.0.369）、插件实例化零错、抵标题屏干净退出。**如实标注**：三分类/配方/催化剂由 JEI 于进世界时注册，实际展示需人工目视。**细节见 [`docs/spec/machine-subsystem.md`](docs/spec/machine-subsystem.md)**；见平行表 CR-10。
- [x] 2026-07-21 **PB-2 食物/杂项物品（Agent 7，两节点）**：`content/item/misc/MiscItems`——15 食物（nutrition/saturation 取自 1.12.2 ItemHandler）+ 9 杂项 shell（clean snake_case id）。**新增 `platform/FoodCompat`** 承 `FoodProperties.Builder` 的 `saturationMod`↔`saturationModifier` 跨版本分叉（javap 双 jar 核；业务零 `//?`）。接 `ModRegistries.ALL` + en_us 24 键。两节点 `compileJava --rerun-tasks`+`runServer` 绿（`ITEM` 注册冻结零错，forge 2.954s/neo 1.050s，干净 stop）。**进食效果延 T7.10、贴图/模型延 PK**（PB-2 验收=注册+可食，客户端图标/实际进食需人工目视）。细节见 [`docs/spec/item-content.md`](docs/spec/item-content.md)；见平行表 CR-11。
- [x] 2026-07-21 **PB-5 装饰/素功能方块（Agent 8，两节点）**：`content/block/deco/{DecoBlocks,DecoFacingBlock}`——29 块 + 各 BlockItem（17 FACING 装饰雕像/mural/墓碑 + 10 素块 + 2 植物，clean snake_case id）。**`DecoFacingBlock extends Block` 复用 `HorizontalDirectionalBlock.FACING` 规避 1.21 抽象 `codec()`**（`HorizontalDirectionalBlock`/`BushBlock` 在 1.21 把 codec() 重声明 abstract → 直接子类编译失败，此前一度卡 neoforge，见平行表 CR-12/CR-13）。接 `ModRegistries.ALL` + en_us 29 键。两节点 `compileJava --rerun-tasks`+`runServer` 绿（`BLOCK`+`ITEM` 冻结零错，forge 4.460s/neo 1.660s，干净 stop）。**模型/blockstate/贴图延 PK；monolith_pillar(PE)/召唤 statue(仪式)/crystal cluster(晶体) 延各系统**。细节见 [`docs/spec/item-content.md`](docs/spec/item-content.md)；见平行表 CR-13。
- [x] 2026-07-21 **PB-1 材料/晶体物品（Agent 6，两节点）**：`content/item/material/MaterialItems`——117 物品 = 39 基础材料（ingot/nugget/gem/cluster×8/chunk/brick/dust/plate/misc + coin，1.12.2 真实贴图按原模型 `layer0` 映射复制）+ 78 晶体（26 元素 × crystal/shard/fragment，灰度贴图按元素 `ItemColor` 染色）。新增 `platform/ClientColorCompat`（`RegisterColorHandlersEvent.Item`，javap 双端核 `register(ItemColor,ItemLike...)` 同签名、仅事件包分叉）+ `ACClientSetup.registerItemColors` + 主类 client attach。自带 `materials` 创造页（vanilla `displayItems` 零分叉）。手写 117 模型 + 48 贴图 + 118 lang。接 `ModRegistries.ALL`（ITEMS+TABS）。两节点 `build`+`runClient` 绿：117 模型/贴图烘焙**零告警**、颜色注册零错、抵标题屏（曾被并发 PB-5 缺 1.21 `codec()` 短暂卡 neoforge，PB-5 修后两端全绿）。**datagen/配方/标签延 PK/T1.9；晶体染色外观 + 创造页可取需人工目视**。细节见 [`docs/spec/materials-subsystem.md`](docs/spec/materials-subsystem.md)；见平行表 CR-14。
- [x] 2026-07-21 **PB-3 建材方块族（Agent 3，两节点）**：`registry/BaseBlocks` + `data/gen/BaseBlockData` + 新增 forked `platform/BlockModelGen`（`BlockStateProvider` 兼容层）——**86 块 / 11 家族**（darkstone/abyssal/dreadstone/elysian/coralium/ethaxium/dark_ethaxium/omothol/monolith 石系 + darklands_oak/dreadwood 木系；含 stone/cobble/brick/chiseled/cracked/glowing/slab/stairs/wall/fence/pillar/leaves 变体）经 datagen 铺 blockstate/方块模型/物品模型。49 贴图从 1.12.2 老资产按原 model JSON 映射复制（含 6 个 layered 砖 base+overlay 用 System.Drawing 合成）。**基建修复**：`build.gradle.kts` 接 `src/main/generated`→resources（PA-4 datagen 产物此前从未被 processResources 消费、是死文件）+ 增强 `platform/DataGenCompat.Gen`（加 `packOutput`/`existingFiles`）。方块只用具体 vanilla 类（避 1.21 抽象 `codec()`）。两节点 `compileJava`+`runData`（86 blockstate + 177 block model + 86 item model，49 贴图过 `ExistingFileHelper`）+`runClient`（`ModelBakery`/`BlockStateModelLoader` 对本任务块**零告警**、atlas 烘焙成、干净退出）全绿。**命名冲突解**：建材砖块 `ethaxium_brick`→复数 `ethaxium_bricks`（避与 PB-1 同名合成物品撞 `ITEM` 注册名，仿 vanilla `bricks`/`brick`）。**剩余部分已挪至应属阶段**：分叉构造变体（button×5/pressure_plate×5/sapling×2 + door/fence_gate，需 `platform/BlockFactory`）→ **PB-8**（Stage B2）；掉落表/配方/标签 provider → **PK-5**（Stage K，T1.9/T1.10）。见平行表 CR-12。
- [x] 2026-07-21 **PB-6 工具（Agent 9，两节点）**：`content/item/tool/ToolItems`——20 工具 = 4 tier（abyssalnite/refined_coralium/dreadium/ethaxium）× {pickaxe,axe,shovel,hoe,sword}。tier 数值（uses/speed/attack/enchant/harvest level）取自 1.12.2 `AbyssalCraftAPI`；修理料 = PB-1 锭（`registry.get(id)` lazy 只读，不碰 `MaterialItems`）。**新增 `platform/ToolCompat`** 承 1.21 工具大改（`Tier.getLevel()`↔`getIncorrectBlocksForDrops()` tag；工具 ctor `(Tier,dmg,speed,Props)`↔`(Tier,Props.attributes(XxxItem.createAttributes(...)))`，javap 双 jar 逐一核），业务零 `//?`。接 `ModRegistries.ALL` + en_us 20 键。两节点 `compileJava --rerun-tasks`+`runServer` 绿（`ITEM` 冻结**零工具错**，forge 2.522s/neo 0.922s，干净 stop）。特殊工具（longbow/katana/soulreaper/staff of rending）延 M7；贴图/模型延 PK。**如实标注**：neoforge `runServer` 另有 pre-existing 报错（committed 矿石 loot/tag 引用 **PB-4 未注册矿石**，非致命、**非 PB-6**，工具 id 在错误行=0，待 PB-4 协调）。细节见 [`docs/spec/item-content.md`](docs/spec/item-content.md)；见平行表 CR-15。
- [x] 2026-07-21 **PB-7 护甲（Agent 3，两节点）**：`content/item/armor/ArmorItems`——**7 护甲材料 × {helmet/chestplate/leggings/boots} = 28 件**（abyssalnite / refined_coralium / plated_coralium / depths / dreadium / dreadium_samurai / ethaxium）。护甲值/耐久/附魔/韧性取自 1.12.2 `AbyssalCraftAPI`（defense 按 `{boots,legs,chest,helmet}` 槽序）；修理料 = PB-1 锭/板（`registry.get(id)` lazy 只读，不碰 `MaterialItems`）。**新增 `platform/ArmorCompat`** 承 1.21 护甲大改（`ArmorMaterial` 接口→record+`Holder<ArmorMaterial>`、耐久由材料 `getDurabilityForType` 搬到物品 `Properties.durability(type.getDurability(factor))`；1.20.1 匿名实现接口 / 1.21 构造 record + `Holder.direct` + 空 layer 列表），业务零 `//?`。接 `ModRegistries.ALL`。两节点 `compileJava --rerun-tasks`+`runData`（mod 构造 = RegisterEvent 逐一 build 28 件护甲，零错、干净退出，证实 1.21 record+Holder 运行期路径）。**穿戴/护甲数值目视 + 物品模型/贴图延 PK、穿戴层贴图延 E 渲染阶段、lang 延 PK-4**。见平行表 CR-16。
- [x] 2026-07-21 **PB-8 建材方块分叉构造变体（Agent 10，两节点）**：串行接 PB-3，**16 块 = button×5 + pressure_plate×5 + sapling×2 + door×2 + fence_gate×2**（石家族 darkstone/abyssal_stone/coralium_stone 用 `BlockSetType.STONE`；木家族 darklands_oak/dreadwood 用 `OAK`）。**新增 `platform/BlockFactory`** 承 5 类 vanilla ctor 的 1.20.1↔1.21 分叉（`ButtonBlock`/`PressurePlateBlock`(`Sensitivity` 1.21 删)/`DoorBlock`/`FenceGateBlock` 参序变化、`SaplingBlock` 的 `OakTreeGrower()`↔`TreeGrower.OAK`；javap 双 jar 逐一核，业务零 `//?`）。顺序扩 PB-3 冻结的 `BaseBlocks`(+16 块+3 props helper)/`BaseBlockData`(+16 模型)/`BlockModelGen`(+`door()`/`fenceGate()` helper)；**`BaseBlocks.BLOCKS/ITEMS` 已由 PB-3 接入 `ModRegistries.ALL` → 本任务零注册改动**。8 张专属贴图（6 门 + 2 sapling）从 1.12 资产改名迁入 + en_us 16 键。两节点 `compileJava --rerun-tasks`+`runData`（`ExistingFileHelper` 严格校验 16 模型贴图存在）+`runServer`（注册冻结零错、我方 16 id 在 ERROR 行=0，forge 2.304s/neo 1.153s，干净 stop）全绿。**sapling grower 占位 vanilla oak 待 G 阶段真树生成；dlts/drts sapling 贴图 best-guess 待 PK 复核；放置/门开合/plate 触发目视 + 模型/贴图精修延 PK**。细节见 [`docs/spec/item-content.md`](docs/spec/item-content.md)；见平行表 CR-17。
- [x] 2026-07-21 **PB-4 矿石方块（Agent 6，两节点；RR-DATA于2026-07-24收口数据）**：`content/block/ore/OreBlocks`注册13矿与BlockItem，硬度/抗性取自旧`BlockHandler`。PB-4当时的基础loot/tag占位已由RR-DATA替换为单一provider owner：忠实Silk/Fortune/explosion、完整level 2/3/4/5采集层级与smelting均已双端真实验证；仅矿石layered overlay模型仍归T9.2b。细节见[`docs/spec/item-content.md`](docs/spec/item-content.md) §5e/§5f。
- [x] 2026-07-21 **Gate B（Stage B2 集成）创造栏填充（Agent 6，两节点）**：把全部 M1 内容按 1.12.2 `ACTabs` 忠实还原为 **7 个创造栏分类页**（blocks=建材+矿+机器 / items=材料基础+杂项 / tools=镐斧锹锄 / combat=剑+护甲 / food / decorations / crystals），全定义在 relay `registry/ModCreativeTabs`，用 vanilla fork-free `CreativeModeTab.Builder.displayItems` 惰性拉各内容 list。**增强 `platform/ModRegistrar`** 加 `entries()`（记录注册项供 relay 遍历填页，免各内容文件各暴露 list）；剑靠 `_sword` 后缀从工具页分入战斗页；`acceptAll` 加 `asItem()!=AIR` 守卫防无 BlockItem 方块崩创造页。删 PB-1/PB-4 自带的 `materials`/`ores` 页并入集中 7 页；`MaterialItems` 加 `BASICS`(39) list。en_us 加 7 页标题键。两节点 `compileJava --rerun-tasks`+`runData` 绿 + 两节点 `runClient` **进世界**：JEI 成分表（forge 1896/neo 2026）含全部 AC 内容、**无 AC 页被标空**（仅 vanilla Operator Utilities 空）、无 `displayItems` 错、`Stopping!` 干净退出。**如实标注**：创造栏 7 页视觉分组/图标需人工目视（大量内容因贴图/模型延 PK，创造栏显缺失模型占位，属预期）。细节见 [`docs/spec/item-content.md`](docs/spec/item-content.md)；见平行表 CR-19。
- [x] 2026-07-21 **PC-3 菜单框架（Agent 3，两节点）**：Stage C1 菜单框架。新 `registry/ModMenus`（canonical MenuType 注册器）+ `content/menu/base/{ContainerMenuBase(可复用抽象基类：3×9+hotbar 玩家背包布局 + 通用 content↔背包 shift-click),ItemContainerMenu(变长物品容器示例，9 宽网格，client factory 从 buffer 读 size —— crystal bag/spirit tablet/spellbook 共享基类)}`；PP-1 的 `MachineMenu`/`MachineMenus` 保持原样（机器专用）。**泛化 `platform/MenuCompat.open`** 加 `Consumer<FriendlyByteBuf>` 任意 buffer-writer 重载（物品容器传 size 而非 BlockPos；neoforge 1.21 `RegistryFriendlyByteBuf` 经 `::accept` 适配；原 BlockPos 重载委托新重载、**3 机器调用点不受影响**）。接 `ModRegistries.ALL`；业务零 `//?`（分叉全走 `MenuCompat`）。两节点 `compileJava --rerun-tasks`+`runData`（mod 构造 = RegisterEvent 注册 `item_container` MenuType，两端零错、干净退出）。**示例开屏经 `MenuCompat.open(writer)`；实际 in-game 开屏 via C2 物品消费者、屏幕延 M6/PH-1（同 PP-1 框架「仅编译+注册证明」先例）**。见平行表 CR-20。
- [x] 2026-07-21 **PC-1 BlockEntity 框架（Agent 6，两节点）**：泛化 PP-1 机器 BE 为**通用 BlockEntity 框架**（PP-1 `MachineBlockEntity` 原样保留）。新增 `content/blockentity/base/{ACBlockEntity(通用基类 extends `platform/BlockEntityCompat` + `markUpdated`),DirectionalBlockEntity(承 1.12.2 `TEDirectional`，存 facing),InventoryBlockEntity(可复用 `Container`，size=1 = 1.12.2 `ISingletonInventory` 单例库存 / size>1 = 箱笼),TickingBlockEntity(接口 = `ITickable`，`serverTicker()` 泛化 `MachineBlockEntity::serverTick`)}` + `registry/ModBlockEntities`（BE 类型注册器 + directional/inventory 两 block-less smoke 类型）。业务零 `//?`（版本分叉全在 `BlockEntityCompat`/`ContainerCompat`）。接 `ModRegistries.ALL`。两节点 `compileJava --rerun-tasks`+`runServer` 绿（`BLOCK_ENTITY_TYPE` 冻结零错、抵 Done，forge 2.342s/neo 0.841s；neo 首跑撞并发 agent 的 25565 端口 `BindException` → 换 25580 重跑抵 Done，与代码无关）。tick 机制 compile 核 + 由 P2 机器运行期实证；非机器 BE 消费延 C2/S。见平行表 CR-21。**详见 [`docs/spec/machine-subsystem.md`](docs/spec/machine-subsystem.md)**。
- [x] 2026-07-21 **PC-4 物品转移能力（Agent 3，两节点）**：Stage C1 物品转移框架。新增 `system/transfer/{ItemTransfer(转移引擎 move/transfer/run + selfTest；hopper 式 sim→real 抽取插入 + 安全回插余量),ContainerItemView(vanilla `Container`→中性 `ItemView` 适配、fork-free),ItemTransferConfiguration(1.12.2 同名数据面：origin/dest + 5 槽过滤 + exit/entry 面 + save/load 经 `ContainerCompat`),ItemTransferHost(1.12.2 `IItemTransferCapability` 现代化为 BE 实现接口，`instanceof` 查询免 Forge `AttachCapabilitiesEvent`/neo `BlockCapability` 自定义能力注册)}`。增强 `platform/CapabilityAccess` 加中性 `ItemView`+`itemView()`（唯一碰 forked `IItemHandler` 处经 `wrap` 隐藏，业务不再 import forked 类型）、`platform/ContainerCompat` 加 `canStack()`（`isSameItemSameTags`↔`isSameItemSameComponents` 分叉）。业务零 `//?`。**两节点 `compileJava --rerun-tasks` + `runData`（主类临时 `selfTest()` 日志、验后已还原，同 PA-3 先例）：`ItemTransfer self-test: moved=40 src=24 dst=40 -> PASS` 双端一致**——转移逻辑运行期实证。无 `ModRegistries`/lang 改动（纯逻辑）。**state transformer / rending pedestal / spirit altar 宿主方块（数据面消费者）延 C2/S**（implements `ItemTransferHost` + 每 tick 调 `ItemTransfer.run`）。见平行表 CR-22。
- [x] 2026-07-21 **PC-2 自定义配方框架（Agent 11，两节点）**：Stage C1 / T2.2 的 5 类自定义配方。crystallization/materialization/transmutation 由 MP 试点 3 机器已注册（绑简化 `ProcessingRecipe`、frozen）；本任务补 **2 类真实形状**——`anvil_forging`（`input1`+`input2`→`result`+`price`+`forging_type`，仿 1.12.2 `api.recipe.AnvilForging`）+ `rending`（`name`+`max_energy`+`entity`→`result` essence，仿 `api.rending.Rending`，`Predicate<EntityLiving>`→entity id 串）。新 `registry/ModRecipes`（中央目录：owns anvil/rending + alias 引用 3 试点类型做全 5 catalog）+ `content/recipe/{anvil/AnvilForgingRecipe,rending/RendingRecipe}`（fork-free）+ **`platform/{DataRecipeCompat（抽象 `Recipe<Container↔RecipeInput>` fork base，吸收 1.21 Recipe 接口大改；`matches`→stub，data 配方由 `getAllRecipesFor` 枚举）,AnvilForgingRecipeSerializer,RendingRecipeSerializer（forked：`fromJson`/`fromNetwork`/`toNetwork`↔`MapCodec`/`StreamCodec`，javap 双 jar 核 `ByteBufCodecs.STRING_UTF8`+`StreamCodec.composite` 6 元）}`。4 示例 JSON 双目录双格式（forge `recipes/` result 用 `"item"` / neo `recipe/` 用 `"id"`）。接 `ModRegistries.ALL`；业务零 `//?`。两节点 `compileJava --rerun-tasks` 绿；forge `runServer` `Loaded 12 recipes`（可复现）+ `/reload` 零 parse 错 + 干净 stop；neo `runServer` `Loaded 1295 recipes` + Done + 干净 stop——anvil/rending 两端零 parse 错加载。**诚实标注**：forge 12 vs neo 1295 = vanilla 配方在 forge-dev/neo-dev 数据包可用性的可复现差异（干净重跑仍 12、与 PC-2 无关、与 CR-18 记「forge 1293」不符）；3 试点类型仍机器自持、集中化+升级到 1.12.2 完整形状（多输出/多输入）延 C2a 回归。见平行表 CR-23。
- [x] 2026-07-21 **PC-8 Research Table + Sequential Brewing Stand（Stage C2a，Agent 6，两节点）**：2 机器复用 PC-1 BE 框架 + PC-3 菜单 + MP 模板（业务零 `//?`）。**研究桌** `content/machine/researchtable/*`（`ResearchTableBlockEntity` extends `ACBlockEntity` 空壳 marker——1.12.2 原 TE 即空、知识 hook 延 S-B；`InteractiveBlockCompat` 块 onUse 开菜单；`ContainerMenuBase` contentSlots=0 仅背包；vanilla `crafting_table.png` 占位屏）。**顺序酿造台** `content/machine/brewing/*`（`BrewingStandBlockEntity` extends PC-1 `InventoryBlockEntity`(8 槽)+`TickingBlockEntity`：3 药水/材料/燃料/3 传出槽，vanilla 酿造 + 每 20t 传出槽→facing 邻居输入槽=**酿造顺序链**；`FACING` blockstate 块 + getTicker；`brewing_stand.png` 屏）。**新 `platform/PotionBrewingCompat`**（1.20.1 静态 `PotionBrewing` ↔ 1.21 `level.potionBrewing()` 实例，编译双端核）。接 `ModRegistries.ALL`(8 registrar) + `ACClientSetup`(2 屏幕) + en_us 4 键 + 6 vanilla 占位资产。两节点 `compileJava`+`runServer`（BLOCK/BE/MENU 冻结零错、抵 Done forge 2.330s/neo 1.037s）+ forge `runClient`（2 机器模型烘焙零告警、屏幕注册、进世界 JEI 1896、干净退出）。**PC-5..7 三合成机器随框架泛化一并回归全绿、零改动**。**右键开界面 + 酿造链需人工目视；faithful 贴图/药水进度/槽校验延 PK**。细节见 [`docs/spec/machine-subsystem.md`](docs/spec/machine-subsystem.md)；见平行表 CR-24。
- [x] 2026-07-21 **PC-9 机器配方数据 datagen（Stage C2b，Agent 12，两节点）**：新 `data/gen/MachineRecipeData`（`DataProvider`，接 `ACDataGenerators`）生成 3 合成机器示例配方 **27 条**（crystallization 7 / transmutation 11 / materialization 9，`ProcessingRecipe` 形状 input→result+time，忠实 1.12.2 `AbyssalCrafting`、按现存物品裁剪、全用一手核对 id）。业务零 `//?`（`DataProvider.run`/`saveStable(CachedOutput,JsonElement,Path)` javap 双 jar 同签名）。**关键坑**：自定义配方 datagen 有 1.20↔1.21 目录（`recipes/`↔`recipe/`）+ result key（`"item"`↔`"id"`）双分叉，且两 loader `runData` 共享 `src/main/generated` → 单 loader 专属目录被另一 loader HashCache 清除（实测 neo runData 清掉 forge 的 `recipes/`）→ 改为**每 runData 同时生成两目录两格式**、各 loader 只读自己目录。两节点 `compileJava`+`runData`（各 27+27 无 clobber）绿；**neo `runServer` `Loaded 1322 recipes`（1295+27）零 parse 错、干净 stop → 27 条全加载可用**。forge `runServer` 仍 `Loaded 12`（27 在 forge `build/resources/main/.../recipes/`→jar/生产正常；forge-dev runServer 只挂 `src/main/resources` 不挂 `src/main/generated` = PC-2 已记可复现限制，非缺陷、属 PA-4 datagen 基建 forge-dev gap）。细节见 [`docs/spec/machine-subsystem.md`](docs/spec/machine-subsystem.md)；见平行表 CR-25。
- [x] 2026-07-22 **PD-2 AI goals + pathfinding（Stage D1，Agent 13，两节点）**：可复用 AI/寻路框架（解耦具体实体、用 vanilla `Mob`/`PathfinderMob`，与 PD-1 并行独立）。一手比对 1.12.2 的 6 AI + 2 pathfinding → 现代 MC 多被 vanilla 吸收；**交付 2 个无 vanilla 等价的自定义可复用 Goal**：`content/entity/ai/WorshipGoal`（膜拜：`PathfinderMob` + `TagKey<Block>` 目标 + 可选 `SoundEvent`）、`content/entity/ai/{SwellGoal<T extends Mob & SwellingMob>,SwellingMob}`（膨胀：解耦 vanilla `SwellGoal`(需 `Creeper`)，供 anti-creeper 非 Creeper 基类）；**2 canonical 导航** `content/entity/pathfinding/{ACGroundPathNavigation,ACWallClimberNavigation}`（薄扩展点，1.12.2 `PatchedPathNavigate*` 修的 spinning bug(Forge PR#6091) 现已 vanilla-native、无需 override）。**vanilla 覆盖文档化不重造**：远程→vanilla `RangedBowAttackGoal<T>`；近战 reach→vanilla `MeleeAttackGoal`(现代 `Mob.getMeleeAttackRangeSqr` 已 width-based)。**延 D2a Shoggoth**：acid-melee/建碑（Shoggoth 特有 sprayAcid/isBig + 多实体协作 + `WorldGenShoggothMonolith`）——可 extends/复用本框架。业务零 `//?`（**避开** `MeleeAttackGoal.getAttackReachSqr`(1.20.1)↔`canPerformAttack`(1.21) fork；javap 核 `WallClimberNavigation(Mob,Level)` 双端同）。两节点 `compileJava --rerun-tasks` 绿（framework 无实体/无注册，同 PP-1/PC-1/PC-3「仅编译证明」；泛型签名证可复用）。无 CR（仅新增自有 dir，不碰 ModRegistries/platform/lang）。见平行表 PD-2。
- [x] 2026-07-22 **PD-1 实体注册框架 + 基类（Stage D1，两节点）**：`registry/ModEntities`（`ModRegistrar<EntityType>` + `pilot_mob` 示例 = `ACMob` 直注册，同 PC-1 block-less smoke test）+ 新 `platform/EntityAttributeCompat`（mod-bus `EntityAttributeCreationEvent` 的 forge↔neo import fork，`register`/`attach` 分离供 D2a 各族登记属性）+ `content/entity/base/ACMob`（忠实承 1.12.2 `common.entity.base.EntityMobBase`=`extends EntityMob`→`extends Monster`、`createAttributes()` 默认 vanilla monster 模板供子类覆写；自定义导航→PD-2、硬核穿甲/放大伤害依赖未移植 `ACConfig` 延后）。接 `ModRegistries.ALL`(+1) + 主类 `EntityAttributeCompat.attach`(+1) + en_us(+1 `entity.abyssalcraft.pilot_mob`)。业务零 `//?`（分叉全在 `EntityAttributeCompat`；编译核 `EntityType.Builder.build(String)` 1.20.1↔1.21.1 同签名、实体注册本体 fork-free）。两节点 `compileJava` BUILD SUCCESSFUL + forge `runServer` `summon abyssalcraft:pilot_mob {PersistenceRequired:1b,NoGravity:1b}` → `Summoned` + `data get ... Health` `20.0f`（无属性 summon 当场崩→成功即证属性链通）+ 持久存活零异常 + 干净 stop。踩坑：敌对 `Monster` 无 128 格内玩家即刻 despawn（验成活须 `PersistenceRequired`）；无渲染器 `runClient` 启动崩（Stage E 前只 `runServer` 验）。刷怪放置注册（`SpawnPlacements` forge/neo 事件双分叉）延 D2a + T3.9、具体实体族 D2a/D2b、渲染 E。细节见 [`docs/spec/entity-subsystem.md`](docs/spec/entity-subsystem.md)，见平行表 CR-26。
- [x] 2026-07-22 **PD-4 demon/evil 农场动物族（Stage D2a，Agent 14，forge `runServer` 实证 / neo 编译绿）**：4 demon + 4 evil 敌对农场动物（chicken/cow/pig/sheep 各一对）。**10 类塌缩 2 类 + 1 enum**：`content/entity/demon/{AnimalKind(enum：声音/宽高/血量，chicken 10HP·cow/pig 15HP·sheep 12HP),DemonAnimal,EvilAnimal}`（extends PD-1 `ACMob`，kind 烘进 EntityType 工厂 lambda `(t,l)->new DemonAnimal(t,l,kind)`）+ registrar `DemonEntities`（`EnumMap<AnimalKind,Supplier<EntityType>>` 供替身查型、`.fireImmune()`、`static{}` 登记 8 属性）。`EvilAnimal.die()` 死亡时同 kind demon 就地替身——`create(Level)`+`moveTo`+`addFreshEntity`（**fork-free 避 `finalizeSpawn` 分叉**）。AI 复用 PD-1/vanilla（`MeleeAttackGoal`+`NearestAttackableTarget<Player>` 等）。接 `ModRegistries.ALL`(+1 `DemonEntities.ENTITIES`) + en_us(+8 `entity.abyssalcraft.{demon,evil}_{chicken,cow,pig,sheep}`) + 16 战利品表（`{loot_table,loot_tables}/entities/*.json` 双目录同容：demon→rotten_flesh 0-2、evil→vanilla 农产；默认表 id 自动派生免 override）。业务零 `//?`（javap 双 jar 核 `EntityType.create(Level)`/`Mob.convertTo` 同签名）。**forge `runServer`**：8 实体各 `/summon` → `Summoned new Demon/Evil …`（属性链通、lang 解析）+ evil→demon 替身实证（kill demon_cow 清场 → kill evil_cow → `execute if entity @e[type=…demon_cow] run say` 命中）+ 干净 stop + BUILD SUCCESSFUL。neo：PD-4 4 `.class` 已编入 neo build output（一手核）+ 早前双端 `compileJava` 全绿；本次 neo `runServer` 被并发 PD-3 `platform/SpawnPlacementCompat` 未修 neo 分支（`SpawnPlacements.Type`→1.21 `SpawnPlacementType`）暂阻、PD-4 码不在报错集（瞬态并发中断，同 PC-2/PC-4 先例）。**延后**：刷怪蛋（→T3.9）、群系自然刷怪（forge→T3.9 / neo→PL-4）、剪毛/火焰蔓延/硬核穿甲/新月限制/evil-sheep 网络（依赖未移植 config/API）、渲染 E。见平行表 CR-27。
- [x] 2026-07-22 **PD-3 anti 反物质族（Stage D2a，forge `runServer` 实证 / neo 待 PD-5 解阻）**：11 anti 实体。**8 直 extends vanilla**（`Zombie`/`Creeper`/`Skeleton`/`Spider`/`Cow`/`Pig`/`Chicken`/`Bat` 白拿 child/膨胀/爬墙/弓箭/繁殖/悬挂）+ `AntiGhoul`/`AntiPlayer`(无 vanilla 等价) extends PD-1 `ACMob`+显式 vanilla goals（1.12.2 把 vanilla AI 抄进 `EntityMobBase`；现代直承 vanilla 更忠实+省重造）。`content/entity/anti/{AntiEntity(marker),11 实体,AntiEntities(registrar)}` + 新 `platform/SpawnEggCompat`（forge `ForgeSpawnEggItem`↔neo `DeferredSpawnEggItem` 类名分叉、FQN 非 import，供 D 家族刷怪蛋复用）。属性经 PD-1 `EntityAttributeCompat`（复用已挂 attach、**无主类改动**）。接 `ModRegistries.ALL`(+2 `AntiEntities.{ENTITIES,ITEMS}`) + en_us(+22) + 9 战利品表双目录（`set_count` uniform、默认表 id 自动派生免 override）。业务零 `//?`（分叉全在 `SpawnEggCompat`）。**forge `compileJava`+`runServer` 全绿**：11 anti 各 `/summon` 成活（全显 lang 名 = 注册+属性+构造+lang 四通、无属性即崩→成功即证）+ `data get` Anti-Ghoul `45.0f`/Anti-Creeper `30.0f` + 干净 stop 零异常。**neo（诚实）**：本会话未取绿编译——被并发 PD-5 `platform/SpawnPlacementCompat`（`SpawnPlacements.Type`→需 1.21 `SpawnPlacementType`）阻断，但报错 100% 在该文件、我 13+1 文件零错，且 `DeferredSpawnEggItem` 一手核在 neoforge-21.1.193.jar → 我码 neo-正确、全绿待 PD-5 修。延后：湮灭爆炸(config+`ExplosionUtil`)/刷怪放置+biome(PD-5+G2/T3.9)/蛋模型(PK)/`getBreedOffspring`/渲染 E。细节见 [`docs/spec/entity-subsystem.md`](docs/spec/entity-subsystem.md)，见平行表 CR-28。

- [x] 2026-07-22 **PD-5 ghoul + shoggoth 家族（Stage D2a，两节点 `runServer` 全绿）**：5 ghoul（ghoul/depths/dreaded/omothol/shadow，经 `AbstractGhoul` extends PD-1 `ACMob`）+ 3 shoggoth（lesser/shoggoth/greater，`AbstractShoggoth`：PD-2 `ACWallClimberNavigation` 爬墙 + 击退抗性 + 水中滑行）+ 各族 registrar（8 `EntityType` + 8 刷怪蛋经 PD-3 `SpawnEggCompat` + 属性经 PD-1 `EntityAttributeCompat` + `SpawnPlacementCompat` 地面刷怪放置）。**新增 `platform/SpawnPlacementCompat`**（forge `SpawnPlacementRegisterEvent` ↔ neo `RegisterSpawnPlacementsEvent` 分叉，`SpawnPlacements.Type`→1.21 `SpawnPlacementTypes.ON_GROUND` 常量分叉封兼容层内、业务经 `registerGroundMonster` 零分叉；javap 双 jar 核 neo 5-arg `register(EntityType,SpawnPlacementType,Heightmap.Types,SpawnPlacements.SpawnPredicate,Operation)`）——**主类挂一行 `attach`，并修好此前阻断 PD-3/PD-4 neo 编译的 `SpawnPlacements.Type` bug（neo 现两端全绿）**。跨主 gap-fill：PB-2 `MiscItems` +5 shoggoth flesh plain item（1.12.2 非食用 `ItemACBasic`、PB-2 漏移、shoggoth loot 需之）。接 `ModRegistries.ALL`(+4) + en_us(+21：8 entity+8 egg+5 flesh) + 16 战利品双目录（`{loot_table,loot_tables}/entities/*.json`，flesh `set_count` uniform 0-2、避 1.21 改名 `looting_enchant`、默认表 id 自动派生）+ 2 forge biome_modifier（`spawn_ghoul` ghoul→plains/desert/taiga/savanna 忠实、`spawn_shoggoth` lesser→`#minecraft:is_overworld` 占位待 AC 群系 G）。omothol on-hit slow/blind/nightvis（vanilla effect fork-free）+ 火免/毒免；刷怪蛋色取自 1.12.2 `registerEntityWithEgg`。业务零 `//?`（`MobEffectInstance`/`isPushedByFluid`/`onClimbable`/`doHurtTarget`/`createNavigation`/`canBeAffected` javap 双 jar 核 fork-free）。**两节点 `compileJava --rerun-tasks`+`runServer` 全绿**：8 实体各 `/summon` 成活（无属性即崩→成活即证属性链）+ ghoul `attribute max_health base get`=30.0（值精确）+ `/loot spawn abyssalcraft:entities/{ghoul,shoggoth}` 双端掉 Ghoul Flesh/Shoggoth Flesh（运行期解析 + 双目录路径两端通）+ 干净 stop（forge Done 3.280s / neo 1.028s）。**延后**：水下呼吸（`canBreatheUnderwater` 1.21 为 final 标签驱动）、coralium/dread plague on-hit（药水 MobEffect）、shoggoth 招牌 AI（酸击/喷酸 `EntityAcidProjectile`/建巨石 `WorldGenShoggothMonolith`→G/ooze/进食/多部件/膜拜——依赖未移植、PD-2 框架预留）、5 TYPE 变体、音效/粒子、硬核/精英（config）、depths `dghead` 稀有掉落、维度族自然刷怪（AC 群系→G）、neo 自然刷怪（PL-4）、渲染 E。细节见 `docs/spec/entity-subsystem.md`，见平行表 CR-29。

- [x] 2026-07-22 **PD-6 misc + projectile 家族（Stage D2a，Agent 15，两节点 `runServer` 全绿）**：15 非生物 EntityType（5 projectile + 10 misc），非 LivingEntity → 无属性/loot/蛋。**2 platform fork-base 吸收 `Entity.defineSynchedData` 分叉**（forge `()` ↔ neo `(SynchedEntityData.Builder)` abstract）：`platform/ACSimpleEntity extends Entity` + `platform/ACThrowableProjectile extends ThrowableProjectile`——业务零 `//?`。**5 projectile**：`AcidProjectile`/`DreadSlug`/`InkProjectile`(extends ACThrowableProjectile，onHitEntity→thrown 6/6/2)、`CoraliumArrow`(extends 具体 `Arrow`——避 `AbstractArrow` abstract pickup 分叉)、`DreadedCharge`(extends `AbstractHurtingProjectile`，getTrailParticle FLAME/shouldBurn false/hit 4)。**10 misc**(8 类，ODB×2 / Portal×2 塌缩)：`BlackHole`/`Implosion`(引力拉拽)、`PrimedODB`(boolean core：fuse 200/40→`level.explode` 4/6 MOB→discard、fuse 存 NBT)、`CompassTentacle`(忠实)、`PSDLTracker`/`DimensionPortal`/`SpiritItem`/`GatekeeperEssence`(占位)。2 registrar `ProjectileEntities`+`MiscEntities`(`MobCategory.MISC`、忠实 1.12.2 id)。接 `ModRegistries.ALL`(+2) + en_us(+15)、无主类改动。业务零 `//?`（javap 双 jar 核 4 base ctor + onHit/onHitEntity + `DamageSources.thrown` + `Level.explode(...,ExplosionInteraction)` 双端同）。**两节点 `compileJava`+`runServer` 全绿**：15 各 `/summon`→`Summoned new X`（lang 解析、零异常）+ primedodbcore fuse→explode→discard 双端实证 + blackhole tick 实证 + 干净 stop（forge Done 3.441s / neo 0.900s）。**PD-5 修好 `SpawnPlacements.Type` bug → neo 不再受阻、双端运行期齐证**。**延后**：自定义伤害源 acid/dread（vanilla thrown 占位）、potion（药水系统）、酸蚀方块/群系转化/hardcore（ACConfig/ACBlocks/ACBiomes）、Jzahar 关联（PD-7）、维度传送/spirit 路径/essence 物品（各未移植系统）、渲染 E。细节见 [`docs/spec/entity-subsystem.md`](docs/spec/entity-subsystem.md)，见平行表 CR-30。
- [x] 2026-07-22 **Gate D2a ✅ 已过（两节点集成实证）**：PD-3..6 四族全 ☑（anti 11 / demon-evil 8 / ghoul 5 + shoggoth 3 / misc 10 + projectile 5 = **42 EntityType**）。**两节点 `compileJava` BUILD SUCCESSFUL**（PD-5 修好 `platform/SpawnPlacementCompat` 的 `SpawnPlacements.Type`→1.21 `SpawnPlacementTypes` 分叉、解阻此前 PD-3/PD-4 的 neo 编译）。**两节点 `runServer` 集成**（unique 端口避并发）：forge `Done 2.376s` / neo `Done 1.188s`（`Loaded 1322 recipes`），跨族 `/summon` 各 `Summoned new X`（Ghoul/Shoggoth/Demon Cow/Black Hole/Anti-Ghoul 两端）+ 属性链通（Ghoul `30.0f` / Anti-Ghoul `45.0f` 双端精确）+ 干净 stop + BUILD SUCCESSFUL、**全程零 exception**。延后：自然刷怪 biome_modifier 群系赋值（Darklands G2 / T3.9）、neo 刷怪双份（PL-4）、特殊 AI/爆炸/变身/potion（未移植 config/API/方块/维度）、渲染 E、蛋模型 PK。**解锁 Stage D2b（PD-7 BOSS）**。见平行表 Gate D2a。

- [x] 2026-07-22 **PD-7 BOSS 家族（Stage D2b，Agent 16，两节点 `runServer` 全绿）**：12 BOSS EntityType（**4 血条 boss** chagaroth / jzahar / shadowboss=Sacthoth / dragonboss + **8 elite/minion** dreadguard / gskeleton / remnant / shuboffspring / jzaharminion / chagarothfist / chagarothspawn / dragonminion）。**新 boss-bar 框架** `content/entity/boss/ACBossMob extends ACMob`（vanilla `ServerBossEvent`：`customServerAiStep`→setProgress + 三段变色 blue&gt;66%/green&gt;33%/red、`start/stopSeenByPlayer`→add/removePlayer、setDarkenScreen；javap 双 jar 核全 fork-free）。enum 塌缩（同 PD-4）：`BossKind`(4)/`BossMob` + `EliteKind`(8)/`EliteMob`——忠实 1.12.2 属性（Chagaroth 1000/15、Jzahar 500/30、Sacthoth 300/15、DragonBoss 400、Dreadguard 60/10、GatekeeperMinion 100/18…）+ 标准敌对 goal。12 忠实刷怪蛋（`SpawnEggCompat`）。接 `ModRegistries.ALL`(+2) + en_us(+24)、无主类改动、boss 非自然刷怪无 SpawnPlacement。两节点 `compileJava`+`runServer`：12 boss 各 `/summon` 成活（全显 lang 名）+ 属性精确（Chagaroth `1000.0f` 双端 / Jzahar 500 / Dreadguard 60）+ 4 血条 boss `customServerAiStep` 逐 tick setProgress/变色 **零 exception**（~3min）+ 干净 stop。**Gate D2 收口**（D2a 42 + D2b 12 = 54 EntityType）。延后：boss 招牌多阶段技能 / dragon 多部件飞行 / Remnant 商人交易+剪羊毛 / boss 战利品（依赖未移植 config/API/维度/药水/物品）、血条视觉 + 全实体渲染（Stage E）。细节见 [`docs/spec/entity-subsystem.md`](docs/spec/entity-subsystem.md)，见平行表 CR-31。
- [x] 2026-07-22 **Gate E1 ✅ 渲染注册框架（Stage E1 / PE-1，两节点 `runClient` 实证）**：新 `platform/EntityRendererCompat`（`EntityRenderersEvent`（含 `RegisterRenderers`/`RegisterLayerDefinitions`）forge↔neo **仅 import 分叉**——neo 保留 forge 方法名故 body 中性；暴露 fork-free 中性 `Renderers`/`Layers` sink）+ `registry/ModModelLayers`（client-only，`PLACEHOLDER` 层）+ `client/render/entity/ACPlaceholderRenderer`（bake PLACEHOLDER + 小 cube + vanilla 贴图占位）+ `client/render/ACEntityRenderers`（遍历冻结 `BuiltInRegistries.ENTITY_TYPE` 按 namespace 给**全 54 实体**注册占位渲染器、自动覆盖全族+boss+未来）。主类 `runWhenClient` 块 +1 `EntityRendererCompat.attach` + 2 import（无 relay/lang 改）。**核心**：此前 54 实体无渲染器 → `runClient` 启动 `EntityRenderers.validateRegistrations` 硬崩；E1 全注册 → 过验证。业务零 `//?`（分叉仅 compat import；`ACPlaceholderRenderer` 只用版本稳定 4 参 `ModelPart.render`/`RenderType.entityCutoutNoCull`/`EntityRenderer.render`+`getTextureLocation`；坑：`PartPose` 在 `client.model.geom` 非 `.builders`）。两节点 `compileJava` BUILD SUCCESSFUL + 两节点 `runClient`：**0 missing-renderer / 0 validateRegistrations / 0 crash**，各抵标题屏（ResourceManager reload 过验证 → OpenAL/Sound engine → atlases → Realms/title）。**解了 D2a/D2b 全程记录的「缺渲染器 runClient 崩」**。占位=小 cube（faithful 每族实体/护甲/BE 模型/渲染器/图层 → E2 PE-2..6；视觉/尺寸/贴图/图层忠实需人工目视）。细节见 [`docs/spec/entity-subsystem.md`](docs/spec/entity-subsystem.md)，见平行表 CR-32。
- [x] 2026-07-22 **PE-2 anti + demon 渲染（Stage E2，两节点 `runClient` 实证）**：19 实体（anti 11 + demon 8）faithful 渲染。新 `client/render/entity/{ACTexturedRenderer(通用 `MobRenderer<T,EntityModel<T>>` + 固定贴图),AntiDemonRenderers(注册 19)}` + 迁 18 贴图（13 anti + 5 demon 从 1.12.2 `textures/model/`）。**忠实 1.12.2 `RenderAnti*`/`RenderDemon*`**：anti/demon = vanilla 模型 + AC 贴图（`textures/model/anti/*.png`/`demon_*.png`）；**evil = vanilla 模型 + vanilla 贴图**（长得像正常动物，忠实 `RenderEvilCow`）。bipeds→`HumanoidModel`、动物→`Cow/Pig/Chicken/Sheep/Bat/Creeper/Spider Model`，`bakeLayer(ModelLayers.X)` 复用 vanilla 层（无新 `ModModelLayers`）。接 PE-1 `ACEntityRenderers`（加 `handled`-Set 分发、真渲染器覆盖 E1 占位、占位 loop 跳过 handled 避重复注册）、无 main/lang 改。业务零 `//?`（rendering API 两端稳定；坑：`BatModel` 非泛型 → 不带 `<>`）。两节点 `compileJava` BUILD SUCCESSFUL + `runClient`：各抵标题屏、**0 missing-renderer / 0 missing-texture(anti/demon) / 0 duplicate / 0 crash**。延后：自定义 `ModelGhoul`（近似 biped）/sheep wool/spider eyes/armor(→PE-5)/creeper charge 层、**精确视觉（人工目视 + 进世界 summon）**。细节见 [`docs/spec/entity-subsystem.md`](docs/spec/entity-subsystem.md) §12，见平行表 CR-33。

- [x] 2026-07-22 **PE-3 ghoul + shoggoth 渲染（Stage E2，两节点 `runClient` 实证）**：8 实体（ghoul 5 + shoggoth 3）faithful 渲染。**异于 PE-2**：ghoul/shoggoth 贴图 UV 绑其 1.12.2 专属网格（vanilla biped 会错位）→ 新 `client/model/entity/{GhoulModel,ShoggothModel}`（`HierarchicalModel` 简化-忠实：主体块用真 texOffs/坐标，细节延后）+ `client/render/entity/{GhoulRenderer,ShoggothRenderer,GhoulShoggothRenderers,layers/SimpleEyesLayer}` + 迁 22 贴图。发光眼用 vanilla `EyesLayer` 子类（避 `renderToBuffer` 1.20↔1.21 fork）。接 PE-1 `ACEntityRenderers`（handled-set 分发 + 2 自定义 `ModModelLayers.{GHOUL,SHOGGOTH}` 层），无 main/lang 改。业务零 `//?`。两节点 `compileJava` + `runClient` 全绿：各抵标题屏、0 missing-renderer/0 layer-bake/0 validateRegistrations/0 crash。**诚实**：headless `runClient` 只验注册/烘焙/加载不崩、不能目视模型 → 精确视觉 = 人工目视；细节 cube / armor 层(→PE-5) / TYPE 彩蛋 / 半透明 / 动画延后。细节见 [`docs/spec/entity-subsystem.md`](docs/spec/entity-subsystem.md) §13，见平行表 CR-34。

- [x] 2026-07-22 **PE-5 标准护甲穿戴层（Stage E2，两节点 `compileJava` + neo `runClient` 实证）**：7 材料 × 4 件 = 28 护甲（PB-7 注册）穿戴时经 **vanilla `HumanoidArmorLayer`** 渲染——原版 humanoid 渲染器自带该层、**标准护甲无需自定义 `client/render/armor/` 代码**。接线全在 `platform/ArmorCompat`（PB-7 冻结文件的 E5 集成、其 note 已预告「穿戴层延 E 渲染」）：**1.20.1** 材质 `getName()` 返 namespaced `abyssalcraft:<name>`（Forge armor-location patch 拆 namespace）、**1.21** 材质带单元素 `Layer` 列表 `new ArmorMaterial.Layer(ACRef.id(name))` —— 两端均解析到 `abyssalcraft:textures/models/armor/<name>_layer_{1,2}.png`。迁 14 贴图（7 材料 × 2 层）从 1.12.2 `textures/armor/*` 到 `textures/models/armor/*`。两节点 `compileJava` BUILD SUCCESSFUL + neo `runClient` 抵标题屏 **0 crash / 0 armor-tex-missing**（材质带层构造正常、客户端加载不崩）。**延后（诚实）**：自定义护甲模型（武士甲专属外形 via `IClientItemExtensions` / depths 内外双层发光）、怪物护甲层（ghoul/goliath `LayerGhoulArmor` → 协调 PE-3[已成]/PE-4[未成]、贴图未迁）、**穿戴视觉 = 人工目视**（护甲贴图按需在穿戴时加载、headless 标题屏无穿戴者不触发、机器不可验）。细节见 [`docs/spec/entity-subsystem.md`](docs/spec/entity-subsystem.md) §12.3，见平行表 CR-35。

- [x] 2026-07-22 **PS-1 网络层双实现（Stage S-A / T7.1，两节点编译 + 4 启动实证）**：**`platform/NetworkChannel` 首个运行期消费者**。新 `net/ACNetwork`（`CHANNEL=NetworkChannel.create("main")` + `static{}` 注册全 23 消息 id 0-22 + `bootstrap`/`sendToServer`/`sendToPlayer`/`sendToAll`）+ **23 消息**（11 server C→S + 12 client S→C，impl `NetworkChannel.ACPacket`）忠实 primitive 序列化（BlockPos/UUID/NBT/UTF/varint/long/boolean，javap 双 jar 核双端同签名）。主类 `init` +1 行 `ACNetwork.bootstrap(modBus)` + 1 import（mod-bus 站点，与 `EntityAttributeCompat.attach` 并列，服务端+客户端安全）。**ItemStack fork**：`FriendlyByteBuf.writeItem/readItem` 仅 forge 有、neo 1.21 删（需 `RegistryFriendlyByteBuf`+STREAM_CODEC）→ `TransferStackMessage` 的 ItemStack 用 item-id(`BuiltInRegistries.ITEM.getKey`→UTF)+count(varint) fork-free（丢组件/NBT、够用于其延后的材料袋转移；neo 字节码核 `getKey`→`writeUtf`+`writeVarInt` 无 `writeItem`）。**handler 全为延后 stub**（各注目标系统：ritual→PS-6 / spell→PS-7 / PE→PS-5 / necromancy·knowledge→PS-8 / disruption→PS-9 + 未移植 items/blocks/menus）；**登记 `Context.player()`=发送方**（C→S handler 正确；client-bound handler 需 `SideExecutor` 取客户端接收方玩家）。业务零 `//?`（loader 网络 API 全封 `NetworkChannel`；ItemStack fork 用 `ACRef.parse`+`BuiltInRegistries`）。**两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL** + neo 字节码核 ItemStack fork-free。**临时 selfTest（验证后已还原，同 CR-22 PC-4 先例）**：全 23 消息 write→decode→write 字节稳定 round-trip，**forge/neo 均 `network self-test: 24/24 messages round-tripped byte-stable`**——序列化双端运行期实证（含 ItemStack fork）。forge `runServer` `Done (6.816s)`+干净 stop、forge `runClient` 抵标题屏（Realms）、neo `runClient` 抵标题屏（LWJGL+OpenAL）——bootstrap（forge SimpleChannel / neo Payload playBidirectional）双端双向注册无崩。**neo `runServer` 未抵 Done**：被**无关既存** `dimension_type/mini.json` IntProvider 格式崩阻断（非 `net/**`、属维度/世界数据任务、已登记协调项；PS-1 网络 bootstrap+自测在其前已跑通）。**诚实**：实网 C↔S 逐消息同步需活客户端-服务器会话（人工）；本任务证 = 通道 bootstrap 双端 + 全 23 消息字节稳定 round-trip 双端 + 编译/字节码。细节见 [`docs/spec/network-subsystem.md`](docs/spec/network-subsystem.md)，见平行表 CR-38。

- [x] 2026-07-22 **PG-0 worldgen 框架 + 迷你维度竖切 + R1/R2/R3 调研（Stage G0 / T5.1，两节点 `runServer` 实证）**：**框架**（3 Java）`world/ACDimensions`※（4 真维度 abyssal_wasteland/dreadlands/omothol/dark_realm + `MINI` 的 `ResourceKey<Level>` 冻结契约，fork-free）+ `world/feature/MiniPillarFeature`（示例代码 `Feature`，证 PG-4 路径）+ `registry/ModWorldgen`（`Registries.FEATURE` registrar，接 `ModRegistries.ALL`；文档化 G1 自定义 ChunkGenerator/BiomeSource[Codec↔MapCodec 分叉→platform/]/StructureType codec 入口）。**竖切**（6 数据包 JSON，两端共享单目录——`dimension`/`dimension_type`/`worldgen/*` 路径跨版本不分叉）`abyssalcraft:mini` 全链路：dimension_type + noise_settings（自定义 `final_density` via vanilla `y_clamped_gradient` + depth-gated `stone_depth` surface_rule，**无需**自定义 DensityFunction codec）+ biome + dimension（`minecraft:noise`+`minecraft:fixed`）+ configured/placed_feature。**跨版本坑**：`dimension_type.monster_spawn_light_level` 的 IntProvider 写法 1.20.1（min/max 套 `"value"`）↔1.21（顶层）分叉 → 共享 worldgen JSON 用**纯 int** 规避（两端 `either(INT,..)` 均接受；**顺解 PS-1/CR-38 记录的 neo `runServer` `dimension_type/mini.json` 崩**）。**两节点 `compileJava --rerun-tasks` + `runServer` 实证**（隔离 level-name/port 避并发）：forge/neo 均 `Done` 零 worldgen 加载错、`/execute in abyssalcraft:mini run forceload add 0 0` 生成区块 + `locate biome abyssalcraft:mini` 0 blocks + `if block 0 10 0 minecraft:stone` OK + `fill … air replace glowstone` = **8**（2 柱×4，特征自然装饰阶段执行——双端一致）。**§5 定稿冻结** R1（数据驱动 schema 全链路 + 跨版本坑）/R2（36 nbt 可 jigsaw 但需格式重存+调色板重映射、程序化退 StructureType）/R3（简单维度纯数据包、多群系维度可能退代码 BiomeSource）+ 维度(4)/群系(18)/特征(~11)/结构 ID 命名冻结。**解锁 Stage G1（PG-1..5 据 §5 只读 ID 互不依赖并行）**。细节见 [`docs/spec/worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md)，见平行表 §5 + CR-39。

- [x] 2026-07-22 **PG-3 Omothol + Dark Realm 维度（Stage G1 / T5.4，两节点 `runServer`）**：2 简单纯数据包维度（R3「简单维度纯数据包」）——**8 数据包 JSON、无 Java**（复用 G0 竖切 noise_settings 模板 + 忠实 1.12.2 换块/暗天）：`abyssalcraft:{omothol,dark_realm}` 各 dimension/dimension_type/noise_settings/biome。忠实：Omothol default_block=`omothol_stone`、Dark Realm=`darkstone`（一手核 `ChunkGeneratorOmothol/DarkRealm`+`BiomeOmothol/DarkRealm` top+filler）；暗天（旧 WorldProvider celestial 0.5 定夜 + 暗雾）→ `effects=minecraft:the_end` + `has_skylight:false` + `natural:false` + `bed_works:false` + ambient 0.15/0.05（对旧 lightBrightness f 0.25/0.10）；单群系 `minecraft:fixed`；`default_fluid=air`（无海）。**跨版本安全（承 G0 R1）** `monster_spawn_light_level` 纯 int 7。**接入 ACDimensions** = G0 冻结的 `OMOTHOL`/`DARK_REALM` `ResourceKey<Level>` + dim id 对齐（零码改）。**验证**：两节点 `runServer` 均 `Done` 零 worldgen 加载错（8 JSON schema 双端有效、无 1.21 IntProvider 崩）；**forge 全 runtime** `/execute in abyssalcraft:{omothol,dark_realm}` 进入 + forceload 生成 + `if block 0 10 0` = omothol_stone/darkstone + `locate biome` 各 0；**neo** `Done` + `/execute in abyssalcraft:omothol` 进入 + forceload crashless 生成。**诚实**：neo 显式块/biome/dark_realm 探针未获（neo runServer 抵 Done 后 ~20s 空闲即退 + 1 次并发环境早崩，3 runServer 后止试）；据 neo Done + omothol chunk-gen crashless + G0 已在 neo 全证同一 vanilla noise/fixed-biome 机制 + darkstone 已注册 + forge 全探针 判 neo 等价。**延后**：结构(PG-5)/自然刷怪(biome_modifier D2a/PL-4)/自定义天空雾(`ACSkyRenderer`→T6.3)/浮空岛真形(现平实体 omothol_stone)。细节见 [`docs/spec/worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md)，见平行表 CR-40。

- [x] 2026-07-22 **PS-2 necrodata 玩家能力（Stage S-A / T7.2，两节点编译 + runServer/runClient 实证）**：item transfer 能力已由 **PC-4 `ItemTransferHost`**（fork-free BE 接口）覆盖；本任务交付 **necrodata 玩家能力**。新 `platform/PlayerDataCompat`（每玩家持久数据的最大 loader 分叉：**Forge 实体能力**（`Capability<Holder>`+`RegisterCapabilitiesEvent`+游戏总线 `AttachCapabilitiesEvent`+`PlayerEvent.Clone` 死亡拷贝）↔ **NeoForge 数据附着**（`AttachmentType<CompoundTag>`+`serialize(CompoundTag.CODEC)`+`copyOnDeath`），存中性 `CompoundTag` 业务只读写 vanilla NBT；javap 双 jar 核全 API）+ 3 业务文件 `system/cap/necrodata/{KnowledgeType(5 枝),NecroData(CompoundTag 数据层：7 触发列表+研究+全知识标志+等级+5 枝知识点，fork-free NBT，dimension int→String),NecroDataCapability(accessor+save/apply)}`。主类 `init` +1 `PlayerDataCompat.bootstrap(modBus)`。业务零 `//?`。**坑**：neo `AttachmentType.builder(CompoundTag::new)` 在 `Supplier`↔`Function<IAttachmentHolder,T>` 重载间方法引用歧义 → 显式 `() -> new CompoundTag()`。两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL；**临时 selfTest（已还原）NecroData NBT round-trip forge/neo 均 PASS**；两节点 `runServer` Done+干净 stop（能力/附着注册无崩，**neo 现抵 Done** 得益 G0/CR-39 修 `mini.json`）+ `runClient` 抵标题屏（客户端侧注册无崩）。`isUnlocked`/条件处理 + 客户端同步消息 handler 接线归 **PS-8**（PS-2 供 `NecroDataCapability.{get,save,apply}` API）；活玩家逐次 attach/持久化/同步 = 人工会话。细节见 [`docs/spec/capability-subsystem.md`](docs/spec/capability-subsystem.md)，见平行表 CR-41。

- [x] 2026-07-22 **PG-2 Dreadlands 维度（Stage G1 / T5.3，两节点 `runServer` 全 runtime）**：Dreadlands 4 群系维度（R3「多群系维度用 `multi_noise` 数据包」）——**7 数据包 JSON、无 Java**：`abyssalcraft:dreadlands` dimension/dimension_type/noise_settings + 4 biome（dreadlands/forest/mountains/ocean）。**异于 PG-3 简单 fixed 维度**：4 群系分布用 `minecraft:multi_noise` biome source——climate 点按 continentalness 三段(ocean -0.85 / base+forest 0 / mountains 0.85) + humidity 分 forest(0.75)；noise_router climate 引 vanilla `minecraft:overworld/{continents,erosion,ridges}` + inline `shifted_noise`(temperature/vegetation)（ref06 多噪声群系法、避全 0 塌缩）。**忠实 1.12.2**（一手核 `ChunkGeneratorDreadlands` + 4 biome + `BiomeProviderDreadlands`）：default_block=`dreadstone`、无海用 lava(sea_level 31)、noise-hilly(`y_clamped_gradient` + `overworld/base_3d_noise`)、surface_rule 按 biome 换块(forest=dreadlands_grass/dirt、ocean=dreadlands_muck、base/mountains=dreadstone)、dread 红观感(foliage/grass 0x910000 / fog 0x330808)、nether-like(`the_nether` effects + fixed_time 18000 + 无天气 + natural/bed false)。跨版本安全（承 G0 R1）`monster_spawn_light_level` 纯 int。**接入 ACDimensions** = G0 冻结 `DREADLANDS` `ResourceKey<Level>`（零码）。**验证（两节点全 runtime，隔离 level-name/port 避并发 awtest agent）**：forge/neo 均 `runServer` `Done` 零 worldgen 加载错（7 JSON + multi_noise + overworld density fn 引用双端有效、无 1.21 IntProvider 崩）+ `/execute in abyssalcraft:dreadlands` 进入 + **4 群系全 `locate biome` 命中双端**（forge 0/0/128/1538、neo 181/0/731/430 blocks = 群系分布双端成立）+ forceload 生成 + `if block 0 40 0`=dreadstone 双端。**延后**：dreadwood 树/dread 矿/石笋特征(PG-4)、mineshaft/lair 结构(PG-5)、mob 刷怪(D2a/PL-4)、逐 biome 真高度差(mountains 1.3/ocean -1.0；现共享 hilly)、darklands 注入第 5 群系、忠实天空雾(T6.3)。细节见 [`docs/spec/worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md) §8.2，见平行表 CR-43。

- [x] 2026-07-22 **PG-1 Abyssal Wasteland 维度（Stage G1 / T5.2，两节点 `runServer` 全 runtime）**：AW 5 群系维度（R3「多群系用 `multi_noise` 数据包」，同 PG-2）——**8 数据包 JSON、无 Java / 无 relay / 无 main / 无 lang**：`abyssalcraft:abyssal_wasteland` dimension/dimension_type/noise_settings + 5 biome（wastelands/desert/swamp/plateau/coralium_lake）。**忠实 1.12.2**（一手核 `WorldProviderAbyssalWasteland` + `ChunkGeneratorAbyssalWasteland` + 5 biome + `BiomeProviderAbyssalWasteland`）：dimension_type has_skylight + 无 ceiling + `ambient_light:0.25` + `fixed_time:18000` + `bed_works:false`（干维度无雨雷）；default_block=`abyssal_stone`、sea `minecraft:water`@55（忠实=liquid_coralium、待流体）、noise-hilly(`y_clamped_gradient` + continentalness)；**biome-conditioned surface_rule**(plateau→coralium_stone / wastelands·swamp→fused_abyssal_sand 顶 + abyssal_sand 填 / 余→abyssal_sand，忠实各 biome top/filler)；忠实 grass/foliage 色。5 群系分布用 `multi_noise` 编造 temp/humidity climate 带逼近 1.12.2 `GenLayerBiomesAW` 均匀分布。跨版本安全（承 G0 R1）`monster_spawn_light_level` 纯 int。接入 ACDimensions = G0 冻结 `ABYSSAL_WASTELAND` `ResourceKey<Level>`（零码）。**验证（两节点全 runtime，隔离 level=awtest/port 25590·25591 避并发 PG-2）**：forge/neo 均 `runServer` `Done` 零 worldgen 加载错（8 JSON + multi_noise + shifted_noise + biome surface_rule 双端 schema 有效）+ `/execute in abyssalcraft:abyssal_wasteland` 进入 + **5 群系 `locate biome`**（forge 全 5 命中 0/115/704/747/320、neo plateau 32 away = multi_noise 分布双端成立）+ forceload 生成 abyssal_stone@y10 双端 + surface 双端（forge abyssal_sand 1111 + wastelands 块 fused_abyssal_sand 256=1/柱；neo plateau 块 coralium_stone 542）。**延后**：sea=liquid_coralium(流体)、AW 特征(石笋/coralium 湖/dead tree→PG-4、Gate G1 挂 biome features)、自然刷怪(D2a spawn_*/PL-4)、per-biome 高度差(plateau mesa/lake basin；现共享 hilly)、忠实天空雾(`ACSkyRenderer`→PH-2/T6.3)、精确观感人工目视；3 darklands-in-AW legacy 怪癖不移植（清洁化）。细节见 [`docs/spec/worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md) §8.3，见平行表 CR-44。

- [x] 2026-07-22 **PS-3 附魔（5）双实现（Stage S-A / T7.9，两节点编译 + runServer `/enchant` 探针 + runClient）**：5 附魔（blinding_light/iron_wall/light_pierce/multi_rend/sapping）双端可获得。**硬 fork（1.20 类 / 1.21 数据驱动）**——网络之后第二深的 1.20↔1.21 分歧：新 `platform/EnchantmentCompat`（forge `DeferredRegister<Enchantment>` + 通用 `AbyssalEnchantment` 参数化 metadata / neo `bootstrap` no-op，数据驱动）+ `system/enchant/ACEnchantments`（**fork-free** 5 `ResourceKey<Enchantment>`）+ 5 datapack JSON `data/abyssalcraft/enchantment/*.json` + 空 tag `#abyssalcraft:enchantable/staff_of_rending` + en_us 5 键 + 主类 `EnchantmentCompat.bootstrap`。忠实 1.12.2 metadata（rarity→weight/cost/maxLevel/slot/supported items）；multi_rend/sapping 用未移植 Staff 分类（1.20.1 `EnchantmentCategory.create(→false)` / 1.21 空 tag）。javap 双 jar 核 1.20.1 `Enchantment` ctor/cost/`EnchantmentCategory.create`；subagent 提取 vanilla 1.21 `fortune`(definition-only)/`sharpness` 核 JSON schema。业务零 `//?`（fork 全在 `EnchantmentCompat`）。**验证**：两节点 `compileJava` BUILD SUCCESSFUL；neo `runServer` `Done` = 5 datapack 附魔 JSON 解析入注册表零 schema 错（游戏 codec 强校验，同 `mini.json` 先例）；forge `runServer` `Done` = 5 代码附魔注册无崩；**两节点 `/enchant` 探针**（summon 僵尸持钻剑/钻胸甲）`Applied enchantment {Light Pierce III/I, Iron Wall} to Zombie's item`（注册+applicable+tag+lang 四通双端）+ 干净 stop；两节点 `runClient` 抵标题屏。**附魔效果**（iron_wall 受击零动量/blinding_light/light_pierce vs shadow）延后 **PS-11** event hook；multi_rend/sapping 待未移植 Staff of Rending。细节见 [`docs/spec/enchantment-subsystem.md`](docs/spec/enchantment-subsystem.md)，见平行表 CR-45。

- [x] 2026-07-22 **PS-4 药水效果（Stage S-A / T7.10，两节点编译 + runServer `/effect` 探针 + runClient）**：5 MobEffect（coralium_plague/dread_plague/antimatter=HARMFUL 带伤害 tick + coralium/dread antidote=BENEFICIAL）+ 7 brewable Potion（cplague/_long、dplague/_long/_strong、antimatter/_long）双端交付。**MobEffect 两端仍代码化**（异于 1.21 附魔数据驱动），ctor `(MobEffectCategory,color)` 稳定 → 注册 fork-free 用 `ModRegistrar`；2 fork 落 `platform/MobEffectCompat`：`ACMobEffect`（`applyEffectTick` void/boolean + `isDurationEffectTick`/`shouldApplyEffectTickThisTick` 重命名，委托 fork-free `Tick` 回调）+ `effectInstance`（`MobEffectInstance` raw / `Holder` via `wrapAsHolder`）。**PotionBuilder 等价** = `ACMobEffect`+`Tick`。新 `system/effect/ACEffects`（fork-free）+ `ModRegistries.ALL` +2 + 12 lang（5 effect + 3 potion）。忠实颜色（ACClientVars 0x00FFFF/0xAD1313/0xFFFFFF/0x00FF06）+ duration。javap 双 jar 核 `MobEffect`/`MobEffectInstance`/`Potion`/`wrapAsHolder`。业务零 `//?`（fork 全封 `MobEffectCompat`）。**验证**：两节点 `compileJava` BUILD SUCCESSFUL；两节点 `runServer` `Done`（MOB_EFFECT+POTION 冻结无崩，neo `wrapAsHolder` 建 potion 无崩）+ **`/effect` 探针实证效果生效**（summon 牛[不日晒燃烧] + `/effect give @e antimatter` → `Applied effect Antimatter to Cow` + 牛 10HP→死 = 伤害 tick 双端实证）+ 干净 stop；两节点 `runClient` 抵标题屏。**效果延后 PS-11**（plague 传播/免疫/antidote 治愈/antimatter→anti 转化，现 `magic()` 占位伤害源）+ brewing → PC-8；potion `/give` 需活玩家（注册由 Done 证）。细节见 [`docs/spec/potion-subsystem.md`](docs/spec/potion-subsystem.md)，见平行表 CR-46。

- [x] 2026-07-22 **Gate S-A ✅ 已过（Stage S-A 收口，两节点实证）**：PS-1..4 全 ☑ —— 网络层（PS-1，全 23 消息序列化双端 self-test 24/24）+ necrodata 玩家能力（PS-2，Forge 能力/Neo 数据附着 + NBT round-trip）+ 5 附魔（PS-3，1.20 类/1.21 datapack，`/enchant` 双端）+ 5 药水效果/7 药水（PS-4，`/effect`「antimatter 杀牛」双端）。**四 compat 新增/消费**：`NetworkChannel`(PA-1 首个消费者)/`PlayerDataCompat`/`EnchantmentCompat`/`MobEffectCompat`。两节点 `compileJava` + `runServer` `Done` + `runClient` 抵标题屏全绿。**延后至下游**：消息 handler 效果（各 PS-X）、necrodata isUnlocked/同步 handler（PS-8）、附魔效果 + 药水 plague/cure/转化 event hook（PS-11）、brewing recipes（PC-8）；活玩家 attach/持久/同步 + 实网 C↔S = 人工会话。**解锁 Stage S-B（PS-5..8：能量/仪式/法术/知识）**。

- [x] 2026-07-22 **PS-5 PE 能量网络框架（Stage S-B / T7.3，两节点编译 + selfTest）**：能量框架（内容-无关，同 PD-2/PC-4 框架先例）——新 `system/energy/{DeityType(7 神),AmplifierType(3),IEnergyContainer,IEnergyCollector,IEnergyManipulator,IEnergyTransporter,PEUtils}`。忠实 1.12.2 `api.energy` 核心：能量 BE 接口（fork-free BE 实现，同 PC-4 `ItemTransferHost`）+ `PEUtils` 能量算术（addEnergy 溢出 / consume / container↔container transfer）+ manipulator→collector BE 网络传输。仅 vanilla `Level`/`BlockEntity`/`BlockPos`，业务零 `//?`，**无 relay/platform/main/lang 改 → 无 CR**。两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL + 临时 `PEUtils` selfTest（已还原）双端 PASS（能量算术 + container transfer，runData 触发 init；一次 neo 瞬态并发 mod-load 崩→重跑即 PASS）。**延后内容**：能量方块（collector/container/pedestal/relay/depositioner × 4 tier）+ BE + idol/charm + 采集(deity statue)→player 传输(需 Necronomicon)→PE stream(PS-1 `PEStreamMessage`)——依赖未移植块/物品/书，落地时复用本框架。细节见 [`docs/spec/energy-subsystem.md`](docs/spec/energy-subsystem.md)。

- [x] 2026-07-22 **PS-6 仪式系统框架（Stage S-B / T7.6，两节点编译 + selfTest）**：仪式框架（内容-无关）——新 `system/ritual/{Ritual,RitualRegistry,InfusionRitual}`。忠实 1.12.2 `NecronomiconRitual`：`Ritual` 抽象基（offerings[≤8]/bookType/dimension/requiredEnergy[对 PS-5]/sacrifice + 抽象 `complete(Level,BlockPos,Player)` + order-free `matches`）+ `RitualRegistry`（register/find by offerings+book+dim）+ `InfusionRitual` pilot（产 ItemStack，fork-free spawn 同 PD-4）。**解耦 PS-8**：1.12.2 `IResearchable` 知识门控 → 可选 `researchId`（PS-8 读），不依赖并行 PS-8。业务零 `//?`，无 relay/platform/main/lang 改（无 CR）。两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL + 临时 selfTest（已还原）双端 PASS（offering match 精确/数量/缺项 + registry order-free find，runData 触发）。**延后内容**：altar/pedestal 块 + BE 触发逻辑 + 13 具体仪式（infusion/portal/summon/creation/transformation）+ 产物（gem/catalyst/gateway key/召唤 Asorah 龙/维度门）——依赖未移植块/物品/实体/Necronomicon；反馈用 PS-1 `RitualMessage`/`RitualStartMessage`。细节见 [`docs/spec/ritual-subsystem.md`](docs/spec/ritual-subsystem.md)。

- [x] 2026-07-22 **PS-7 法术系统框架（Stage S-B / T7.7，两节点编译 + selfTest）**：法术框架（内容-无关）——新 `system/spell/{ScrollType,IScroll,Spell,SpellRegistry,EntityTargetSpell,LifeDrainSpell}`。忠实 1.12.2 `api.spell.*`：`ScrollType`（6 级带 quality）+ `IScroll`（scroll 物品 marker）+ `Spell` 抽象基（id/bookType[0-4]/requiredEnergy[对 PS-5]/reagents[order-free `matches`]/scrollType/parent/color/nbtSensitive/requiresCharging/canBeCastByOthers/glyph + 抽象 `canCastSpell`/`castSpellServer`/`castSpellClient` + `castSpell` 按 side 派发）+ `SpellRegistry`（单例 register[bookType 0-4 校验 + id 去重]/getSpell/`find` by reagents+bookTier+scrollType）+ `EntityTargetSpell`（targeting pilot 基）+ `LifeDrainSpell`（具体 pilot：`hurt`+`heal`，fork-free `damageSources().magic()`）。**解耦 PS-8**：1.12.2 `Spell implements IResearchable` → 可选 `researchId`（PS-8 读），不依赖并行 PS-8。业务零 `//?`（`hurt`/`heal`/`magic()`/`isSameItem`/`LogUtils` 两端同签名，编译实证）。两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL + 临时 selfTest（已还原）双端 PASS（register/校验/去重 + find reagent/bookTier/scrollType 门控）。**延后内容**：`SpellUtils`（PE 消耗自 `IEnergyContainerItem`[PS-5 物品变体，未移植] + 持物 NBT[1.20 tag/1.21 component fork] + client raytrace + cast 派发）+ 14 具体法术 + scroll/staff/spellbook GUI + glyph 贴图 + rayTrace 内部 handler + MobSpell 网络接线（PS-1 handler 现 stub）。细节见 [`docs/spec/spell-subsystem.md`](docs/spec/spell-subsystem.md)。

- [x] 2026-07-22 **PG-4 特征迁移（Stage G1 / T5.5，两节点 `runServer` 全 runtime）**：3 代码 `Feature`（`world/feature/{StalagmiteFeature(锥形尖塔 h5-12 半径递减),MonolithFeature(3×1×7-12 monolith_stone 板),DeadTreeFeature(裸干 4-7+3 短枝)}`，fork-free `place`/`FeaturePlaceContext`/`BlockStateConfiguration`/`setBlock(pos,state,2)`）+ 29 数据包 JSON：8 configured_feature（abyssal/dreadlands_stalagmite 自定义类型 + shoggoth_monolith + dead_tree + darklands/dreadlands_tree=`minecraft:tree` 数据驱动挂 AC log/leaves 含 distance/persistent/waterlogged + lake_liquid_coralium/antimatter=`minecraft:lake` **water 占位**）+ 8 placed_feature（rarity/count + in_square + heightmap WORLD_SURFACE_WG + biome）+ 3 configured_carver（cave×2=`minecraft:cave` / abyssal_ravine=`minecraft:canyon`，**全 plain-float provider 避 value-wrapper 分叉**，replaceable=`#minecraft:overworld_carver_replaceables`）+ 2 additive tag `tags/{block,blocks}/overworld_carver_replaceables`（加 abyssal_stone/dreadstone/coralium_stone/darkstone required:false）。触碰 1 处 G0 框架 `registry/ModWorldgen`（`FEATURES` registrar +3 register：stalagmite/monolith/dead_tree；**无 relay/main/lang 改**）。worldgen 路径两端共享单目录；carver FloatProvider 用纯 float（承 G0/CR-39 IntProvider 教训）。**验证**：两节点 `compileJava` BUILD SUCCESSFUL + 两节点 `runServer` `Done` 零 worldgen 加载错（29 JSON schema 双端有效）+ forge `/place feature abyssalcraft:abyssal_stalagmite`→abyssal_stone + neo `shoggoth_monolith`→monolith_stone + `darklands_tree`→darklands_oak_log。**延后**：lake=liquid_coralium/antimatter（流体阶段）、dead_tree_log 占位 darklands_oak_log、特征挂 biome `features[]`/carver 挂 `carvers[]`=Gate G1（biome 归 PG-1/2/3，用 §5 冻结 ID）。细节见 [`docs/spec/worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md) §9，见平行表 CR-47。

- [x] 2026-07-22 **PG-5 结构迁移（Stage G1 / T5.6，程序化子集 ☑，两节点 `runServer` 全 runtime）**：程序化 `StructureType` 交付——4 Java `world/structure/{StructureKind(GRAVEYARD/ABYRUIN/DARK_SHRINE),ACStructure(extends Structure，MapCodec=settingsCodec+kind，findGenerationPoint 用 getFirstOccupiedHeight WORLD_SURFACE_WG),ACStructurePiece(5×5 floor+4 角柱[graveyard h2/余 h3]+中心 loot 箱，load-ctor SAM=StructurePieceSerializationContext，全 box.isInside 门控)}` + `platform/StructureCompat`（**2 fork**：`structureType(MapCodec)` 1.20.1 `codec.codec()`↔1.21 `codec` + `setChestLoot` 1.20.1 `ResourceLocation`↔1.21 `ResourceKey<LootTable>`）+ `registry/ModWorldgen` STRUCTURE_TYPE/PIECE registrar + AC_STRUCTURE/AC_PIECE。15 数据包 JSON：3 structure（type=abyssalcraft:structure + kind + biomes=#has_structure/<name> + surface_structures + beard_thin）+ 3 structure_set（random_spread spacing20/22/24 异 salt）+ 3 has_structure biome tag（graveyard→AW+swamp / abyruin→AW+desert+plateau / dark_shrine→dark_realm）+ 6 loot table（**双目录 loot_table[1.21]+loot_tables[1.20.1]**，vanilla 物品）。触碰 relay `registry/ModRegistries.ALL` +2（STRUCTURE_TYPES/PIECES）+ G0 框架 `ModWorldgen`（无 main/lang 改）。**验证**：两节点 `compileJava` BUILD SUCCESSFUL + 两节点 `runServer` `Done` 零加载错（15 JSON schema 双端）+ `/place structure abyssalcraft:<kind>` 建体（floor+柱+箱）双端 + `data get block <chest> LootTable`=`abyssalcraft:chests/<kind>`（forge dark_shrine / neo graveyard，**证 setChestLoot 分叉双端**）+ `execute in abyssalcraft:abyssal_wasteland run locate structure`（forge graveyard [128,~,96] 160 blocks / neo abyruin [-304,~,-320] 441 blocks = structure_set+biome tag 自然生成双端）。**移出本任务（已挪到应属阶段，不阻塞收口）**：①36 个二进制 1.12.2 `.nbt` 模板（chagaroth_lair/jzahar_temple/omothol 城建/graveyard nbt/shoggoth_pit/dark_shrine nbt，需游戏内格式重存+调色板重映射，**非 headless**）+ 其余程序化结构（ethaxium_house/chains/dreadlands_mineshaft/abyssal_stronghold）→ **T5.6b**；②箱 AC 专属 loot 物品 → **PK-5**（现 vanilla 物品占位）；③has_structure biome 交叉引用校验 → **Gate G1 / PG-7**。细节见 [`docs/spec/worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md) §10，见平行表 CR-48。

- [x] 2026-07-22 **PS-8 知识/死灵之书数据层框架（Stage S-B / T7.8，两节点编译 + selfTest）**：知识框架（内容-无关）——新 `system/knowledge/{IResearchItem,ResearchItem,ResearchRegistry,KnowledgeGate}` + `system/knowledge/condition/{IUnlockCondition,UnlockCondition,EntityCondition,DimensionCondition,MiscCondition,MandatoryMultiEntityCondition,IConditionProcessor,ConditionProcessorRegistry}`。忠实 1.12.2 `api.knowledge.*`：研究注册表（register/去重/getById + 5 KnowledgeType 供品表）+ 研究条目（requiredLevel -1 恒解/-2 恒锁 + unlockConditions）+ 解锁条件（type→processor）+ 条件处理器注册表（构造期注册 10 个 fork-free 处理器 type 0-4/7-11；延后 type 5/6 谓词——需 loader 注册表 resolve biome/entity）+ `KnowledgeGate.isUnlocked`（条件版/研究版，忠实 `NecroDataCapability.isUnlocked`，研究全条件满足则 auto-complete）。**读 PS-2**：直接消费 `system/cap/necrodata/{NecroData,KnowledgeType,NecroDataCapability}`——PS-2 只存、PS-8 判（PS-2 javadoc 明言消费归 PS-8）。业务零 `//?`（`ResourceLocation` 构造走既有 `platform/ACRef`[仅 selfTest 只读用]，永久代码不构造 RL）。两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL + 临时 selfTest（已还原）双端 PASS（entity 条件触发前后 + 研究解锁+auto-complete + type-11 全实体门控 + hasAllKnowledge 越过 type-1 不越 type-11 + 注册表去重）。**延后内容**：Necronomicon 书 GUI（章节/页/entry）+ 20+ 具体 UnlockConditions（未移植实体/群系/维度/书）+ type 5/6 谓词处理器 + 触发事件钩子（入群系/杀怪/入维度→`NecroData.triggerX`）+ 同步 handler 接线（PS-1 necrodata 5 消息现 stub；PS-2 `save/apply` 就绪）。细节见 [`docs/spec/knowledge-subsystem.md`](docs/spec/knowledge-subsystem.md)。

- [x] 2026-07-22 **Gate S-B ✅ 已过（Stage S-B 收口，两节点编译 + selfTest）**：PS-5..8 全 ☑ —— PE 能量网络（PS-5）+ 仪式（PS-6）+ 法术（PS-7）+ 知识（PS-8，读 PS-2 necrodata）。四子系统**框架先于内容**、仅新增各自 `system/` 目录、业务零 `//?`、无 relay/platform/main/lang 改 → **四者全无 CR**。每任务两节点 `compileJava` BUILD SUCCESSFUL + 临时 selfTest（已还原）forge/neo 均 PASS。**跨任务契约成立**：PS-6/7 `requiredEnergy`→PS-5 `IEnergyContainer`；PS-6/7 `researchId` 解耦 PS-8；PS-8 消费 PS-2 `NecroData`（只存/判分离）。延后内容（能量方块/采集链、altar+13 仪式+产物、SpellUtils+14 法术+GUI、书 GUI+20+ 条件+触发钩子+同步 handler）依赖未移植块/物品/实体/书；运行期效果需活会话人工验。**解锁 Stage S-C（PS-9 扰动/PS-10 PoP/存档钩子）**。

- [x] 2026-07-22 **PS-9 扰动系统框架（Stage S-C / T7.5，两节点编译 + selfTest）**：扰动框架（内容-无关）——新 `system/energy/disruption/{Disruption,DisruptionHandler,PotionDisruption,LightningDisruption}`。忠实 1.12.2 `api.energy.disruption.*`：`Disruption` 抽象基（name + 可选 deity[PS-5 `DeityType`] + 抽象 `disrupt`）+ `DisruptionHandler`（单例 register/去重/find/`getRandom`[deity 过滤：deity-less 恒合格]/`generate`）+ `PotionDisruption`（pilot：`Supplier<MobEffectInstance>` 施 16 格内）+ `LightningDisruption`（pilot：`LIGHTNING_BOLT`）。**Reads PS-5**（deity 过滤）。业务零 `//?`（`new MobEffectInstance(MobEffects.POISON,dur)` co-var fork-free；`LIGHTNING_BOLT.create`/`AABB`/`addEffect`/`RandomSource` 两端同签名）。两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL + 临时 selfTest（已还原）双端 PASS（register/去重 + deity 过滤随机）。**延后**：~27 具体扰动（未移植实体）+ deity-image 检测（PS-5 能量块）+ 网络反馈（PS-1 `DisruptionMessage` stub）。细节见 [`docs/spec/disruption-subsystem.md`](docs/spec/disruption-subsystem.md)。

- [x] 2026-07-22 **PS-10 Places of Power 框架（Stage S-C / T7.4，两节点编译 + selfTest）**：PoP 框架（内容-无关）——新 `system/energy/structure/{IPlaceOfPower,IStructureComponent,IStructureBase,StructureHandler,SimplePlaceOfPower}`。忠实 1.12.2 `api.energy.structure.*`：`IPlaceOfPower`（identifier/bookType/`getAmplifier(AmplifierType)`[PS-5]/canConstruct/construct/validate/`getRenderData():BlockState[][][]`/ambientEffect）+ `IStructureComponent`/`IStructureBase`（多块 BE marker）+ `StructureHandler`（单例 register/去重/`canFormStructure`/`formStructure` bookType 门控）+ `SimplePlaceOfPower`（pilot：formable 布尔避 world）。**Reads PS-5**（`AmplifierType`）。**解耦 PS-8**：`IResearchItem` → 可选 `researchId`。业务零 `//?`。两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL + 临时 selfTest（已还原）双端 PASS（register/去重 + bookType 门控 + canConstruct）。**延后**：具体 PoP 多块 + construct/validate/ambient + renderData + BE 实现（依赖未移植能量块/statue）。细节见 [`docs/spec/places-of-power-subsystem.md`](docs/spec/places-of-power-subsystem.md)。

- [x] 2026-07-22 **PS-11 存档 + 知识钩子框架（Stage S-C / T7.11，两节点编译 + selfTest + runServer，有 CR）**：**最重 fork**——新 `platform/SavedDataCompat`（`SavedData.save` [1.20.1 `(tag)`]↔[1.21 `(tag,HolderLookup.Provider)`] + `getDataStorage().computeIfAbsent` [1.20.1 `(load,create,name)`]↔[1.21 `(Factory,name)`] fork，委托 fork-free `saveData`）+ `system/data/NecromancyData`（extends `SavedDataCompat`，忠实 1.12.2 `NecromancyWorldSavedData`：≤20 复活快照[超 20 丢最旧] + save/load round-trip）+ `platform/GameHooksCompat`（game-bus 订阅 fork：`LivingDeathEvent`→杀怪 / `PlayerChangedDimensionEvent`→换维度）+ `common/handlers/KnowledgeHooks`（fork-free：→`NecroDataCapability.get(player).triggerX`[PS-2]，PS-8 读）。主类 init +`GameHooksCompat.attach()`（永久激活，CR）。业务零 `//?`（fork 全封 2 compat）。**坑**：`PlayerChangedDimensionEvent.getEntity()` 已返 `Player` → 去多余 `instanceof`（否则「模式类型是表达式类型子类型」编译错）。两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL + 临时 selfTest（已还原）双端 PASS（store/cap-20/round-trip）+ **两节点 `runServer` `Done`**（forge 17.560s/neo 5.456s，挂钩不崩 + 干净 stop）。**延后**：Plague/Purge 钩子（未移植 plague 传播/dread purge）+ 复活仪式消费（PS-6）+ 客户端同步接线（PS-1 stub）+ biome 触发（需活玩家）。细节见 [`docs/spec/saveddata-hooks-subsystem.md`](docs/spec/saveddata-hooks-subsystem.md)。

- [x] 2026-07-22 **Gate S-C ✅ 已过（Stage S-C 收口，M7 系统层收尾，两节点编译 + selfTest + runServer）**：PS-9..11 全 ☑ —— 扰动（PS-9）+ Places of Power（PS-10）+ 存档/知识钩子（PS-11）。PS-9/10 框架先于内容、纯新增自有 `system/energy/{disruption,structure}/**`、无 relay/platform/main/lang 改 → 无 CR；PS-11 有 CR（2 platform compat + 主类挂钩）。每任务两节点 `compileJava` BUILD SUCCESSFUL + 临时 selfTest（已还原）forge/neo PASS；PS-11 另两节点 `runServer` `Done`（挂钩不崩）。**跨任务契约**：PS-9/10 读 PS-5（`DeityType`/`AmplifierType`）；PS-11 写 PS-2 `NecroData` 触发 → PS-8 `KnowledgeGate` 读（知识环路闭合）。**至此 M7 全系统层（PS-1..11）框架交付完毕**。延后内容（具体扰动/PoP 多块/Plague·Purge 钩子/复活消费/网络同步）依赖未移植块/物品/实体/书 + 活会话人工验。

- [x] 2026-07-22 **PG-7 群系特征放置（Stage G2 / T5.8·T5.9，两节点 `runServer` 全 runtime）**：14 feature biome_modifier（7 feature × forge/neoforge 双份，`data/abyssalcraft/{forge,neoforge}/biome_modifier/feature_*.json`，`{forge,neoforge}:add_features`）把 PG-4 placed_feature 挂各维度 biome——abyssal_stalagmite→AW{wastelands,desert,plateau}、lake_liquid_coralium+dead_tree→abyssal_swamp、dreadlands_stalagmite→dreadlands{4}、dreadlands_tree→dreadlands_forest、shoggoth_monolith→omothol、lake_liquid_antimatter→dark_realm（忠实 1.12.2 populate + §5 冻结 ID）。纯数据包无 Java/relay/main/lang。**验证**：forge/neo 均 `runServer` `Done` 零 biome_modifier 加载错（schema+biome/feature 引用双端 resolve）+ **neo omothol 100 chunk 采样 `fill air replace monolith_stone`=12（shoggoth_monolith gen 期实际生成——特征挂接端到端实证）**。**移出本任务**：carver 挂 biome（Forge 无 add_carvers + biome JSON `carvers` 1.20.1 `{"air":[…]}`↔1.21 flat-list 分叉；configured_carver 已备[PG-4]）→ 待办；darklands_tree + 主世界 Darklands 覆盖 → 待 Darklands 维度/群系任务。细节见 [`docs/spec/worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md) §11，见平行表 CR-50。

- [x] 2026-07-22 **PG-6 传送门 teleport 框架（Stage G2 / T5.7，◐ 框架，两节点 `runServer` 全 runtime）**：新 `platform/TeleportCompat`（**portal 唯一深 fork**：1.20.1 `Entity.changeDimension(ServerLevel,ITeleporter)`+`PortalInfo` ↔ 1.21 `changeDimension(DimensionTransition)`，Mojang 1.21 重写传送）+ `world/portal/DimensionTeleport`（1.12.2 `TeleporterAC` 落点现代化：`coordinateScale` 比缩放 + world border clamp + `getHeight(MOTION_BLOCKING_NO_LEAVES)` 地表 + `getChunk` 强制生成）+ 接线 PD-6 `DimensionPortal`（placeholder→功能：`destination` server 字段+NBT `Destination`[`ACRef.parse` 避 synched fork]、`tick()` 传送 box 内非 portal 实体、singleUse 用后 discard）。业务零 `//?`（fork 全封 `TeleportCompat`）。**验证**：两节点 `compileJava` BUILD SUCCESSFUL（1.21 `DimensionTransition` + 1.20.1 `ITeleporter`/`PortalInfo` fork 双端 resolve）+ **两节点 `runServer`**：`summon abyssalcraft:portal {Destination:"abyssalcraft:abyssal_wasteland"}`+cow → **cow 跨维 overworld→abyssal_wasteland 双端**（forge FORGE_TELEPORT_OK / neo NEO_TELEPORT_OK）。**延后（未移植依赖）**：portal_anchor 方块+BE、点燃/激活（`NecronomiconPortalRitual`→PS-6 / gateway key 物品）、目标 mob 刷怪（`DimensionData`）、portal-frame 建造、home spell/dark realm teleporter 变体、渲染（E）。细节见 [`docs/spec/worldgen-subsystem.md`](docs/spec/worldgen-subsystem.md) §12，见平行表 CR-51。

- [x] 2026-07-22 **PH-5 死灵之书 GUI 框架（Stage H2 / T6.2，两节点编译 + runClient）**：死灵之书 GUI 框架（内容-无关）——新 `client/necronomicon/{NecronomiconEntry,NecronomiconScreen,ACNecronomicon}`。忠实 1.12.2 `GuiNecronomicon` + 递归 `api.necronomicon.NecroData` 树：`NecronomiconEntry`（递归节点 id/titleKey/textKey/icon/可选 `researchId` 门控 + children）+ `NecronomiconScreen extends Screen`（plain Screen；category 按钮下钻 + back/done + `font.split` 正文 + **PS-8 只读门控**隐藏未解锁 entry）+ `ACNecronomicon`（pilot 树 + `open()` setScreen）。**Reads PS-8**：`researchId==null || hasAllKnowledge || completedResearches.contains(rid)`（读 PS-2 同步 necrodata，避 `KnowledgeGate.isUnlocked` auto-complete 副作用）。业务零 `//?`（`renderBackground` 分叉走既有 `ClientScreenCompat.background`；render 顺序坑：1.20.1 不自画 bg / 1.21 自画 → 自定义文字放 `super.render` 后最上层）。两节点 `compileJava` BUILD SUCCESSFUL + 两节点 `runClient` 抵标题屏资源初始化（NecronomiconScreen 类加载零错）。**延后**：20+ 具体章节/页 + 忠实书贴图/布局（PK）+ Necronomicon 书物品（未移植）+ lang；翻页/渲染观感 = 人工目视。细节见 [`docs/spec/necronomicon-gui-subsystem.md`](docs/spec/necronomicon-gui-subsystem.md)。

- [x] 2026-07-22 **PH-6 HUD/字体/clientvars 框架（Stage H2 / T6.6，两节点编译 + runClient，有 CR）**：HUD/字体/clientvars 框架——①字体 `client/font/AkloFont`（`location()=ACRef.id("aklo")`）+ `assets/.../font/aklo.json`（reference 占位，Aklo 位图延 PK）；②clientvars `client/hud/{ClientVars(Gson POJO 色子集),ClientVarsManager(ResourceManagerReloadListener 热重载 + accessor)}` + `assets/.../clientvars.json`；③HUD 新 `platform/ClientHooksCompat`（**loader fork**：overlay forge `RegisterGuiOverlaysEvent`+`IGuiOverlay`↔neo `RegisterGuiLayersEvent`+`LayeredDraw.Layer` 中性 `HudRenderer` + reload listener 注册；overlay/reload 签名 javap 双 jar 核）+ `client/hud/ACHud`（pilot overlay + relay）。主类 client block（`runWhenClient`）+2 行 `ACHud.register()` + `ClientHooksCompat.attach(modBus)`（CR，同 PE-1/PS-11）。**Reads PS-5**（HUD PE meter 读能量物品，延后）。业务零 `//?`（fork 全封 `ClientHooksCompat`）。两节点 `compileJava` BUILD SUCCESSFUL + 两节点 `runClient` 抵标题屏（aklo.json 字体 + clientvars.json reload 解析零错 + HUD overlay 注册零错）。**延后**：HUD 内容（PE meter/spirit path，未移植能量/staff 物品）+ Aklo glyph 位图 + 全 ~100 clientvars 色 + 5 keybind；HUD/字体观感 + 热重载 = 人工目视。细节见 [`docs/spec/hud-font-clientvars-subsystem.md`](docs/spec/hud-font-clientvars-subsystem.md)。

- [x] 2026-07-22 **Gate H2 ✅ 已过（Stage H2 收口，两节点编译 + runClient）**：PH-5..6 全 ☑ —— 死灵之书 GUI（PH-5）+ HUD/字体/clientvars（PH-6）。PH-5 框架先于内容、纯新增 `client/necronomicon/**`、无 CR；PH-6 有 CR（`platform/ClientHooksCompat` HUD overlay/reload fork[javap 双 jar 核] + 主类 client block +2 行）。两节点 `compileJava` BUILD SUCCESSFUL + 两节点 `runClient` 抵标题屏资源初始化（Sound engine started + 全 texture atlas；aklo.json/clientvars.json 解析零错 + HUD 注册零错 + NecronomiconScreen 类加载零错）。**跨任务契约**：PH-5 读 PS-8/PS-2（necrodata 门控书 entry，知识→书环路）；PH-6 HUD 读 PS-5（延后）。延后内容（20+ 书章节/页 + 书贴图、HUD 内容 + Aklo 位图 + 全 clientvars 色 + keybind）依赖未移植 research/物品/书/贴图；**翻页/分类/HUD/字体渲染 + 热重载 = 人工目视**（headless 不能开 Screen/看 HUD）。

- [x] 2026-07-22 **Gate H1 ◐ 部分过（Stage H1 / T6.1·T6.3·T6.4·T6.5，两节点编译 + forge `runClient` 实证）**：PH-4 音效 ☑（`registry/ModSounds` 45 SoundEvent + sounds.json + 106 .ogg + 42 subtitle，接线 `ModRegistries.ALL`）；PH-2 天空/雾 ◐（`client/sky/ACDimensionEffects`+`ACDimensionSkies` 忠实 1.12.2 常量雾色 + `platform/DimensionEffectsCompat` RegisterDimensionSpecialEffectsEvent fork；4 维 dimension_type effects 改指 abyssalcraft key；自定义天空盒渲染延后，3 贴图已就位）；PH-3 粒子 ◐（`registry/ModParticles` abyssal_fx + `client/particle/ACFadeParticle` TextureSheetParticle + `platform/ParticleCompat` RegisterParticleProvidersEvent fork；PEStream/ItemRitual 近似 vanilla dust/item 延后）；PH-1 机器屏幕已由 P2/PC-8 交付（5 屏），物品容器屏幕延后。业务零 `//?`（fork 全封 platform）+ 主类 client block +2 attach（插并发 PH-6 HUD 之前）。两节点 `compileJava` EXIT=0 + **forge `runClient`** 进世界干净退出（sounds.json 零解析错、abyssal_fx 进 particles atlas、4 维 effects 全绑定无 fallback）。**渲染观感=人工目视**（player 在 overworld，未视觉核 AC 维雾/天空/粒子）。见平行表 Gate H1 / CR-52。

- [x] 2026-07-22 **PH-4b 实体音效接线（Stage H1 收尾 / T6.5，两节点 compileJava，有 CR-53）**：把 PD-5/PD-7/PD-3 实体接到 `ModSounds`（新增 `ModSounds.event(String id)` 查询 helper）——ghoul→ghoul.normal.idle/hit/death、shoggoth→shoggoth.idle/hit/death、`EliteMob`（新增 `private final EliteKind kind` 字段，ctor 此前忽略 kind 参）DREADGUARD→dreadguard.idle/hit/death · REMNANT→remnant.scream(ambient)、`BossMob`（新增 `private final BossKind kind` 字段）SACTHOTH→shadow.hit(hurt)/sacthoth.death、`AntiPlayer`→antiplayer.hurt。override 均 protected `Mob` 方法（`getAmbientSound`/`getHurtSound`/`getDeathSound`，两版 fork-free 同签名）；无 bespoke 音的 kind fall-through 到 `super`（vanilla GENERIC_*）=非回归。业务零 `//?`。两节点 `compileJava` BUILD SUCCESSFUL + 13 处 `ModSounds.event(...)` grep 核实全落地。**仍延后（依赖未移植行为，已具体化非模糊）**：shoggoth AI 音（shoot/consume/birth）、boss 技能音（chant.*/jzahar.shout）、remnant 交易台词、depths-ghoul idle 彩蛋。**音效实际播放=人工客户端听测**（headless 不可验，编译+override 落地是机器可验上限）。见平行表 PH-4b / CR-53。

- [x] 2026-07-22 **PS-9 扰动内容扩充（Stage S-C / T7.5，两节点 compileJava + runData selfTest，有 CR-54）**：从「框架先于内容」推进到 **10 具体 vanilla-only 扰动**——新 `system/energy/disruption/{Disruptions,PlayerDisruption}` + 主类 `init` +1 行 `Disruptions.bootstrap()`（注册入 `DisruptionHandler` 单例）。**10 忠实扰动**（对照 1.12.2 `AbyssalCrafting.addDisruptions()`）：lightning（spawn 族 pilot）；5 药水云（`DisruptionPotion`，16 格内 `LivingEntity` 600t：poison/slowness[`MOVEMENT_SLOWDOWN`]/weakness/wither 用 vanilla `MobEffects` + coralium 用 PS-4 `ACEffects.CORALIUM_PLAGUE` via `MobEffectCompat.effectInstance`）；4 玩家定向（`PlayerDisruption` `BiConsumer<Player,Level>`：freeze=SLOWNESS amp14/100t、randomTeleport=`LivingEntity.randomTeleport` vanilla ender 数学、famineAzathoth/famineShubNiggurath[deity-tied]=`getFoodData().setFoodLevel(0)`）。业务零 `//?`（`new MobEffectInstance(MobEffects.X,dur)` 常量/ctor co-vary + `getFoodData`/`randomTeleport`/`addEffect` 两端同签名）。两节点 `compileJava` BUILD SUCCESSFUL + **两节点 `runData` 临时 selfTest（已还原）`count=10 ok=true`**（全注册无 name 碰撞 + deity 过滤）。**仍延后（分离为显式 PS-9b）**：实体 spawn/swarm、ooze（未移植 `shoggoth_ooze` 块）、PE drain、corruption、fire/fireRain（ignite 方法 loader fork）、deity-image 检测、网络反馈（PS-1 stub）。**实际扰动触发=活会话人工验**（trigger=未移植 PE manipulator）。见平行表 PS-9 / PS-9b / CR-54。

- [x] 2026-07-22 **上游内容解锁：Necronomicon 书物品（→ PH-5，两节点 compileJava + forge runServer，有 CR-55）**：应「补上游前置内容真正解锁 Stage 6/7」建 Necronomicon 书**物品**，使 PH-5 死灵之书 GUI 可在游戏内右键打开。新 `content/item/book/{NecronomiconItem,BookItems}`——`NecronomiconItem`（extends `Item`，`stacksTo(1)` + `bookType`[0-4] + `use()` 非潜行右键 → `SideExecutor.runWhenClient(()->()->ACNecronomicon.open())` 打开 PH-5 屏幕，client-only 延迟 classload；潜行=pass 留 `NecronomiconActionRegistry` 延后）+ `BookItems`（`ModRegistrar<Item>` 注册 5 忠实书 necronomicon/abyssal_wasteland_/dreadlands_/omothol_necronomicon/abyssalnomicon[类型 0-4] + `ALL`）。接线：`ModRegistries.ALL` +1 `BookItems.ITEMS` + `ModCreativeTabs` items 页 + `acceptAll(BookItems.ALL)`（忠实 1.12.2 tabItems）+ en_us.json +5 书名。业务零 `//?`（`Item.use`/`InteractionResultHolder`/`isShiftKeyDown` 两端同签名）。两节点 `compileJava` BUILD SUCCESSFUL + **forge `runServer` `Done`（3.471s）**——5 书注册无 id 碰撞 + **server 端 Done 证 client-classload 正确延迟**（`ACNecronomicon`/`NecronomiconScreen` 未在专用服 classload，否则 `NoClassDefFoundError`）。**解锁 PH-5b 的书物品依赖**（书可打开）；仍延后 = 20+ 章节内容 + 书贴图（PK）+ bookType 门控（PS-8b）。**实际右键开书 GUI = 人工客户端目视**。见平行表 CR-55 / PH-5b。

- [x] 2026-07-22 **Gate J ◐ 部分过（Stage J / T8.1·T8.2·T8.3·T8.4，两节点编译 + forge `runServer` 实证）**：PJ-2 配置 ☑（`config/ACConfig` 130 标量选项 12 类，runServer 生成 355 行 `abyssalcraft-common.toml`）；PJ-3 命令 ☑（`system/command/ACCommands` fork-free Brigadier `/acunlockallknowledge` + `platform/CommandCompat` RegisterCommandsEvent fork + 主类 attach，runServer 控制台 → 「A player is required」证注册+执行）；PJ-4 进度 ☑（9 dual-dir advancement，1.12.2 → 现代 items 格式，23 item id 核验，necronomicon → dreadium_ingot，runServer 零加载错）；PJ-1 JEI ◐（承 PP-5 3 机器分类 + 新 anvil_forging 分类 `AnvilForgingCategory`，平台加 `DataRecipeCompat.allOfType`，两节点编译）。业务零 `//?`（fork 全封 CommandCompat/ConfigCompat/DataRecipeCompat）。**3 处硬阻塞延后分离为 PJ-1b/2b/4b**（JEI rending·ritual·spell / 配置非标量 / IMC·IACPlugin）。**JEI 面板 + 命令玩家侧执行 + 配置 GUI = 人工目视**。见平行表 Gate J / CR-56。

- [x] 2026-07-23 **Stage K 资产 ◐ 部分推进（T9.1·T9.3·T9.4，两节点编译 + forge `runData`）**：PK-4 语言 ☑（7 非英 lang，每 419 键，映射链+英文回退，Python 回读 0 失配）；PK-3 物品模型 ◐（新 `platform/ItemModelGen` + `data/gen/ModelItemData`，runData 生成 174 模型：spawn egg template + 有贴图者 basicItem，existingFileHelper 跳过已有不覆盖）；PK-1 贴图 ◐（+51 迁移，确定性 name-chain，MD5 byte-verified，现 item 91/block 65）；PK-2 方块模型 ◐（建材 228 gen，PB-3）。业务零 `//?`（fork 全封 ItemModelGen）。**耦合/人工延后分离为 PK-1b/2b/3b**（复杂贴图/块模型/特殊物品模型：矿石 layered/机器多面/装饰自定义几何/crystal 染色/多层物品，像素正确性人工目视）；PK-5 战利品/标签/配方汇总未开始。**Stage 9 为大幅推进而非完成**（余量大）。见平行表 Gate K / CR-60。

## 8. 路线图 / TODO

> **完整移植计划见 `docs/porting/`**：[设计案](docs/porting/00-porting-design.md) → [总任务表](docs/porting/01-porting-task-plan.md)（里程碑 M0–M11 的详细任务/验收）→ [平行任务表](docs/porting/02-porting-parallel-tasks.md)（多 agent 并行 + 文件归属矩阵）。下方为速览，细节以该三件套为准。

- [ ] **按审计后二态队列推进**：只认领平行表 §3A 的 R1–R8；完成一个收窄的可验收切片才从 ☐ 改为 ☑，不得以历史 Gate、框架、注册或标题屏加载跳过任何 `b/c/d/e` 任务。
- [x] **R1 Gate 已完成**：`RR-MACHINE-ENGINE`、`RR-MACHINE-CONTENT`、`RR-MACHINE-UX`、`RR-CONTENT`、`RR-ENTITY-CATALOG` 与 `R1-AUTO-GATE` 全部完成；Forge+Neo 真人逐槽/shift-click/XP/分页/重连/工作态视觉与停服重启矩阵通过，现按平行表进入 R2。
- [x] **R2 RR-MENU-HOST 已完成**：`T2.6b/T2.7b/T2.8c/T2.9b` 与 CORE/BREWING/TRANSFER 三队列均完成；Spellbook 已由 R4/T2.8d 完成，State Transformer 与 Rending Pedestal 仍为独立未完成任务。
- [x] **R2 RR-WORLD 自动切片已完成**：`T5.2b/T5.3b/T5.4b/T5.5b/T5.5c/T5.6b/T5.8e/T5.9` 双端自动矩阵通过；oracle/动态 marker/性能归 `RR-WORLD-FIDELITY-AUTO`，人工视觉归 `U-WORLD`，二者均不反向阻塞后续 Agent。
- [x] **R2 自动集成 Gate 已完成**：`RR-DATA/RR-WORLD/RR-RENDER-AUTO/RR-MENU-HOST`与双端relay/datagen/build/server/client/JAR门禁全过，R3已解锁；世界与渲染保真轨道不阻塞R3但继续为未完成。
- [x] **R3 RR-KNOWLEDGE 实现切片已完成**：`T7.2b/T7.8b/T7.10b/T7.11b/T8.2b`及双端datagen/build/server/JAR门禁通过；复活仪式消费已由 R4 完成，剩余边界仍不含真人实网、完整旧书消费、动态Dreadlands扩散和配置GUI。
- [x] **R3-Gate 已完成并解锁 R4**：RR-ENERGY/RR-KNOWLEDGE双车道及联合runData/build/server/JAR门禁全过；只解锁下一阶段，不代表M7-PLAYABLE或拆分待办完成。
- [x] **R4 RR-ENTITY-BEHAVIOR 已完成**：`T3.2b–T3.8b/T3.9d/T3.10c` 与 `M3-CONTENT` 收口；双端全族行为、69 loot死亡路径、11场真实自然生成和实体/owner持久化矩阵通过。
- [x] **R5 RR-ADV-API 已完成**：`T8.3b/T8.4b/T8.4c` 收口；双端进度schema/Gate、外部插件消费者与生产sink通过，Forge命令/9进度/书页/重连真实联网矩阵通过。
- [ ] **数据与保真缺口**：RR-DATA 已完成401 crafting/53 smelting、M1完整tags和13矿loot，RR-ENTITY-BEHAVIOR 已完成69实体loot全量；继续迁移223机器 recipe，补齐slab/进度与全仓数据引用审计、复杂资产模型和缺现代宿主的实际 BER。
- [ ] **下一波 Agent 自动收口**：并行推进 `RR-BER-R4-HOSTS`、`RR-NET-AUTO`、`RR-CLIENT-GUI-AUTO`、`RR-JEI-AUTO`、`RR-WORLD-FIDELITY-AUTO`；GUI 完成知识/page 消费面后，单向解锁 `RR-SYSTEM-AUTO`。Gate Integrator 串行接 relay/compat/lang 并运行 R4/R5 CODE Gate。
- [ ] **用户与发布验收**：用户按独立 `U-CONTENT/U-WORLD/U-R4/U-NET/U-GUI/U-FX/U-JEI/U-SYSTEM/U-FINAL` 执行；这些任务不阻塞 Agent，只由最终 `U-GATE` 与自动服务端/产物检查共同阻塞 M11。
- [x] **语言与生成数据一致性**：8语言文件已统一为620键；1.20.1 `loot_tables/` 与1.21.1 `loot_table/` 双路径持续由datagen验证。
- [x] 2026-07-21 运行期验证：两节点 `runServer`（服务端）+ `runClient`（客户端·抵标题屏零错）均确认加载进游戏（见 §7）。仅可视交互（创造页/进世界放置）需人工目视。
- [x] 2026-07-21 **M0（Stage A）收口**：PA-1..PA-5 全交付 + Gate A2/A3/A4 全过（两节点）。兼容层 / 注册 / 配置 / datagen / 最小竖切就绪，§2 契约冻结，解锁 Stage P/B。
- [x] 2026-07-21 **Stage A · PA-1** 兼容层 `platform/**` 已交付（Agent 1，11 类，两节点 build 绿）。
- [x] 2026-07-21 **PA-2** 注册聚合 `ModRegistries` + 空创造栏 `ModCreativeTabs` + 主类挂载已交付；`ModItems`/`ModBlocks` 等各 `Mod*` 随各自内容任务落地。
- [ ] 从 `docs/AbyssalCraft-1.12.2/` 逐步迁移方块 / 物品 / 维度 / 战利品等内容。
- [ ] 补全 `en_us.json` 翻译与基础贴图 / 模型。

## 9. 变更日志 (Changelog)

- **2026-07-26**：**RR-ADV-API 完成**。9项进度双schema修复并接入独立Progression书知识、登录回填与即时同步；`/acunlockallknowledge`权限/toggle/重连闭环；双端`IACPlugin` ServiceLoader/显式API和五类实体sink完成，Forge五项旧IMC兼容、13项旧key迁移文档完成。永久`RR_ADV_API_SELF_TEST_OK`、双端外部fixture、专服加载与Forge真实联网矩阵通过，临时夹具清零后做production JAR门禁。

- **2026-07-26**：**RR-CLIENT-FX（CR-73）实现与自动 Gate 收口**。四维 tinted 天空盒经新 `platform/DimensionSkyCompat`（renderSky 双端签名 fork + 1.20/1.21 即时顶点 fork）绘制，色取自 `ClientVars`；`ClientRitualEffects` 8 基座发 BlueFlame+smoke+ItemRitual（供品由 `RitualManifestCatalog` 重建），PEStream 归 RR-NET；`AbstractShoggoth` 补 shoggoth.step、修正 jzahar.shout 字幕键。永久 `ClientFxSelfTest` 双端 runData `RR_CLIENT_FX_SELF_TEST_OK skies=3 particles=2 sounds=45 ogg=106 subtitles=41 rituals=62`，双端 build/JAR 通过。T6.3b/T6.4b/T6.5b☑；**T6.3c 四维天空目视与 T6.5c 声音/字幕矩阵仍☐（人工）**。未据此勾 RR-NET/RR-CLIENT-GUI/R5-Gate或总发布 Gate。

- **2026-07-26**：**RR-ENTITY-BEHAVIOR（CR-72）完成，M3-CONTENT 收口**。63内容实体的全族专属行为、9 legacy 边角与持久状态完成；69旧loot审计为`32/21/16/0`并由单一provider生成97逻辑/194物理表。Forge/Neo行为、97表/69死亡路径、11场真实NaturalSpawner、5实体+5 owner重启矩阵、runData/build/JAR及无属性server reload全部通过；修复PotentialSpawns只读列表与SpawnerData身份问题，临时fixture/属性/快照残留为0。未据此勾选R4真人Portal、缺宿主BER、T5.9b/T9.5b/T10.5或总发布Gate。

- **2026-07-25**：**R3-Gate（CR-71）完成**。RR-ENERGY与RR-KNOWLEDGE均☑，同一最终源码双端runData同时通过两永久Gate，production build/JAR、专服与重启通过，验证残留清零，现解锁R4。所有二态拆出的真人实网/外部依赖/完整旧书/动态群系/复活消费/配置GUI待办继续☐。

- **2026-07-25**：**RR-KNOWLEDGE（CR-70）完成并二态收口**。实现necrodata协议v2/延迟同步、42 research/42 conditions/11 offerings、研究目录、三DamageType与plague/antidote/antimatter/Purge、食物/酿造、死亡快照生产和非标量配置reload。双端runData/build/server/JAR及关键效果smoke通过；Forge压缩NBT含`RRK_SNAPSHOT`和`ResurrectionRitualCrystalSize`并同世界重启成功。活玩家实网、完整旧书、动态Dreadlands、复活消费与配置GUI继续为独立☐，未借主车道勾选。

- **2026-07-25**：**RR-ENERGY 完成**。落地21网络块、7功能神像+兼容入口、32 charm、Idol of Fading、Basic/Totem/Archway三PoP，以及旧27扰动的22 MIGRATED + 5 BLOCKED审计闭包。双端`runData`永久标记、资源/语言审计、真实ServerLevel传输/阻挡/红石/物品/PoP矩阵和停服重启逐字段恢复通过；临时验证源码与世界已清理。GUI、PEStream/DisruptionMessage客户端FX、Depositioner Stone Tablet与5外部依赖扰动仍按任务边界后续完成。

- **2026-07-25**：**RR-RENDER-VISUAL 双端完成**。Forge/NeoForge 六区总场景均由用户确认无问题；护甲 alpha/宿主模型、Chagaroth/Goliath/Dreadguard 与 Gecko 持物等反馈已修复。临时入口再次清零，双端完整 production build、主 JAR required entries/贴图哈希/元数据互斥与七类残留=0均通过。仅缺现代宿主的`T4.6d/RR-RENDER-BER-HOSTS`继续独立`☐`。
- **2026-07-25**：**R2-AUTO-GATE / R2-Gate（CR-69）完成**。以真实relay callback建立registrar/provider/capability/Screen/renderer/BER/layer不变量，双端runData/build/无属性server+reload/client atlas/production JAR门禁通过；默认命令面无`acrrrender`，旧RR-DATA/Render临时探针为0。R2实现侧完成并解锁R3；Gate当时两条保真轨道尚待人工结论，同日后续 RR-RENDER-VISUAL 已完成，RR-WORLD-FIDELITY仍独立未完成。

- **2026-07-25**：**RR-WORLD（CR-68）自动切片完成**。完成四维现代混合保真、真实材料/carver、第六群系、固定 Dark Realm cavity、36+House 模板转换/布局、Chains、Mineshaft/Stronghold AC palette+loot。双端生产 JAR、专服与 Neo 重启通过；自动完成项和旧噪声 oracle/动态 marker/人工视觉未完成项已在总任务表二态拆分。
- **2026-07-25**：**RR-MENU-HOST（CR-67）完成**。Research Table 专用 GUI/方块行为、Sequential Brewing 完整 8 槽/自动化/hook/链式酿造/动态 GUI、Spirit Tablet 5 槽/过滤按钮/主副手菜单、全 BE Forge capability/Neo attachment 与 Spirit Altar 调度全部落地。双端 runData self-test、箱子重启、两级酿造、真实联网 Screen 点击/按钮/截图目视和生产 JAR 门禁通过；实际画面反馈修复 Research/Tablet 标签对比度，验证探针已清零。`T2.6b/T2.7b/T2.8c/T2.9b` 及三条 R2 队列改为 ☑，后续边界未越界。

- **2026-07-25**：**RR-RENDER-VISUAL 中途批次**。当时双端 legacy 各 9/9、Forge effect A1/A2 6/12、三项 Boss 动态锚点已通过。Compass Tentacle 的旧比例换算、Goliath/Sacthoth 的 Gecko bone rotation 与 Dreadguard carrier `Y=32` 已在该批次修复；后续同日继续完成 NeoForge 与其余 effect、动态层、护甲、Boss death、BER 总场景并形成上方最终完成记录。
- **2026-07-24**：**RR-RENDER-AUTO（CR-65）完成**。实现侧清零九 legacy placeholder 与 12 stand-in，恢复动态层/真实触发、Samurai/Depths/怪物甲、两现有 BER、Shadow alpha 与服务端权威 Boss death visual。Forge/Neo compile/build/runClient 和生产 JAR 审计通过；43 个 AC 自有渲染 PNG 引用全存在。该日自动阶段不以标题屏冒充视觉；RR-RENDER-VISUAL 后于2026-07-25完成，缺宿主 T4.6d 仍独立未完成。

- **2026-07-24**：**RR-DATA（Agent A，CR-66）完成**。建立401 crafting与53 smelting逐条四态审计和双版本生成器，结果分别为`305/61/35/0`与`51/1/1/0`；补齐177逻辑/343物理M1 tags和13矿忠实Silk/Fortune loot，清理静态重复owner及`dltplank_alt`。修复Forge自定义工具未进入`TierSortingRegistry`的根因，并统一Neo `incorrect_for_*`数据；双端真实服务器9镐×13矿共117项采集判定全部通过。Forge/Neo真实RecipeManager与`Block.getDrops`矩阵、热重载、runData、production build及JAR资源/JSON/探针零残留审计通过。35+1个BLOCKED条目保留具体未注册内容依赖，未伪报迁移。`T1.4b/T1.9b/T1.9c/T1.10b`及平行表`RR-DATA`现为☑；223机器配方、69实体loot、slab/进度与全仓数据审计仍属T9.5b/RR-DATAGEN。

- **2026-07-24**：**R1 总 Gate（Agent A）完成**。自动切片完成四级 Crystal Bag 独立菜单/Screen、Liquid Coralium source/flowing/block/bucket/capability、10 次 Transmutation Gem、26 crystal cluster 与 52 条双目录往返配方；恢复两 AC 树、四级工具矩阵、现代 25 个颜色名，以及装饰形状/草传播退化/muck/thorn/植物行为。双端真实联网玩家进一步完成 Bag/三机器逐槽、shift-click、Materializer 结果、网络取出、XP、工作态视觉、断线卸载与停服重启矩阵；Neo 与 Forge 的服务端 NBT/XP ledger 在重启前后逐字段一致，重开 Screen 比例精确对应。临时验证探针已全部删除。**最终验证**：双端 `compileJava`；双端 `runData` 输出 `R1_CONTENT_SELF_TEST_OK` / `RR_MACHINE_SELF_TEST_OK` / `RR_ENTITY_CATALOG_OK content=63 all_ac=64 eggs=48 placements=44`；同一 invocation 双端全量 `build`；无验证属性的两端 `runServer` 均抵 `Done` 并干净 stop，Neo 加载 1382 recipes。Forge 仍有 9 个 1.21 格式 advancement 解析错误，归后续 `RR-ADV-API`，非 R1。

- **2026-07-24**：**RR-ENTITY-CATALOG（Agent C）交付**。旧实体目录扩至 63 内容类型，补齐 17 蛋至 48、44 placement、九漏实体 loot 双目录与核心服务端行为；引入 9 对 Forge/Neo spawn modifier、PotentialSpawns 上下文候选及 1.21 EntityType 分类 tag。required TerraBlender 注入五 Darklands 与 surface/tree 生态。双节点 runData/build、全新专服/热重载、九实体 summon、五群系分布与 Abyssal Zombie 原子转化通过；Forge Dread 聚合/分裂通过。交付当时仍缺自然刷怪统计、第六群系、边角行为、69 loot 全量和九实体忠实渲染；这些后续分别由 RR-WORLD、RR-RENDER 与 RR-ENTITY-BEHAVIOR/CR-72 收口。

- **2026-07-23**：**RR-MACHINE-ENGINE 三机器忠实引擎交付（Agent A）**。完成三类正式 recipe/serializer 中央化、Crystallizer 四槽双输出、Materializer Bag/Book 动态目录、Transmutator 专用燃料，以及 XP ledger、active recipe、持久化、FACING/LIT、sided hopper 与双 loader item capability。新增四级 Crystal Bag 数据层和三种类型安全 JEI 分类；现有 27 条 datagen 迁为完整 schema。两端 `runData` 增强 self-test、server codec 加载、client 资源冒烟和全量 build 通过；Neo 实际加工双输出/嬗变与远区块无漂移重启恢复通过。活玩家矩阵、Bag 独立 GUI、缺失燃料内容、JEI fuel 分类/忠实资产仍显式未完成。

- **2026-07-23**：**移植计划状态审计与二态化**。对照现代代码、1.12.2 源码、任务表与本开发记录，重写总任务表：已交付部分收窄为可独立验收的 `☑` 基线，剩余玩法、保真与人工验证拆为显式 `b/c/d/e` 的 `☐` 任务；不再以 `◐` 表示当前状态。平行表新增 §3A 作为唯一认领队列，旧 CR/阶段日志降为只读历史证据。同步确认实体、配方、渲染、语言和 generated loot 的实际缺口；详见总任务表与平行表 §3A。

- **2026-07-23**：**法术施放 + 法杖（PE 第二消费者 · spell caster）**双端交付（有 CR-64）。承 CR-58 能量物品 + CR-59 statue 充能，建 PE 的**第二消费端**（法术）：新 `system/spell/SpellUtils`（PS-7 施放胶水，CR-58 ItemStack-NBT fork 解锁：**server 侧 raytrace** `ProjectileUtil.getEntityHitResult` 沿 eye+viewVector*range → 查/扣能量物品 PE `IEnergyContainerItem`[CR-58] → `castSpellOnTarget` 派发，替 1.12.2 client-raytrace + MobSpellMessage 往返[仍 stub]，功能等价）+ `content/item/staff/{StaffItem(extends TooltipCompat implements IEnergyTransporterItem，同 NecronomiconItem：holds PE[20000]+tooltip；右键 server 侧调 SpellUtils，无目标/PE 则 displayClientMessage fizzle),StaffSpells(bootstrap 注册 pilot LifeDrainSpell),StaffItems}`。pilot spell = LifeDrainSpell（`target.hurt(magic)`+`player.heal`，bookType0/50PE/4HP，fork-free）。主类 init +1 `StaffSpells.bootstrap` + `ModRegistries.ALL` +1 + creative items 页 + 4 lang（无 asset：物品模型/贴图归 PK，同 BookItems）。业务零 `//?`（`ProjectileUtil`/`Vec3`/`AABB`/`EntityHitResult`/`TooltipCompat` 两端同）。**faithful-简化（PS-7b）**：法杖硬编码单 pilot spell（非 Staff of the Gate scroll 选择/铭刻）；14 具体法术 + scroll/spellbook 物品 + GUI + glyph。**验证**：两节点 `compileJava` BUILD SUCCESSFUL + **两节点 `runData` selfTest（已还原）`found=life_drain req=50.0 range=15.0 ok=true`**（spell 注册 + EntityTargetSpell 参数核）+ **forge `runServer` `Done`（4.448s）**（法杖 item + spell bootstrap 注册无碰撞，无 eager client-classload）。实际施放（充能法杖→瞄 mob 右键→mob 掉血+玩家回血+PE 扣）=活客户端人工验。见平行表 CR-64 / PS-7b。

- **2026-07-23**：**存储块配方（Stage K · PK-5b 部分收尾）**双端交付（有 CR-63）。新增8静态recipe双目录，并实证1.21 Ingredient拒绝裸串：ingredient两端用对象式，唯result为1.20 `item`/1.21 `id`。当时仍延后的矿loot、M1 tags与401有效crafting/53 smelting已于2026-07-24由RR-DATA完成；仅slab/进度及非M1数据留在T9.5b。见平行表 CR-63 / PK-5b。

- **2026-07-23**：**仪式祭坛 + 基座（PE 消费者 · ritual multiblock 端到端）**双端交付（有 CR-62）。承 CR-58/59 能量链，建 PE 的**消费端**：新 `content/block/ritual/{RitualPedestal(接口),RitualAltarBlock,RitualAltarBlockEntity,RitualPedestalBlock,RitualPedestalBlockEntity,RitualBlocks,Rituals}`——右键 Necronomicon → 扫忠实 8 基座 ring（`RitualUtil.PEDESTAL_POSITIONS`）→ `RitualRegistry.find`（PS-6）→ 查/扣书 PE（CR-58 `IEnergyContainerItem`）+ 消 offerings → `ritual.complete`（pilot: nether_star+diamond→oblivion_catalyst）；基座右键放/取 offering（1-slot，`BlockEntityCompat`+`ContainerCompat` save/load fork）。`InfusionRitual` result 惰性化（`Supplier<ItemStack>`）。主类 init +1 `Rituals.bootstrap` + `ModRegistries.ALL` +3 + creative blocks 页 + assets（占位 obsidian/andesite）+ 6 lang。业务零 `//?`。**faithful-简化（PS-6b）**：定时 chant/particle ceremony + PE-over-time + sacrifice + research gate + disruption → pilot 即时完成；余 13 具体仪式 + 忠实模型贴图。**验证**：两节点 `compileJava` + **两节点 `runData` selfTest（已还原）`found=oblivion_catalyst req=100 ok=true`**（注册 + find order-free + 拒错误 offerings）+ **forge `runServer` `Done`（3.827s）**（altar+基座块+BE type 注册无碰撞）〔曾被并发 CR-61 PK-5 loot datagen `abyssalnite_ore.json` duplicate 短暂阻断 processResources，其收尾后重试通过〕。实际端到端（statue 充书→基座摆 offerings→右键 altar→产物）=活客户端人工验。见平行表 CR-62 / PS-6b。

- **2026-07-23**：**HUD PE meter（能量链可视化）**双端交付（无 CR，PH-6 own `client/hud/ACHud`）。承 CR-58 能量物品 + CR-59 statue 充能，实现 `ACHud.renderPeMeter`：读手持 `IEnergyContainerItem` → 画青色 PE 条（contained/max + 数字，仅持能量物品时显）——**在 deity statue 旁给 Necronomicon 充能现 HUD 可见**。`GuiGraphics.fill`/`drawString` + `getMainHandItem`/`getOffhandItem` 两端 fork-free。两节点 `compileJava` + **forge `runClient` 抵 `Sound engine started`**（overlay 注册 + ACHud classload 零错 + deity_statue 模型烘焙无 missing-model 警告）。实际 PE 条视觉（进世界持充能书）=人工目视。仍延后（PH-6b）：其余 HUD 内容（spirit path/dimension）+ Aklo 位图 + 全 clientvars 色 + keybind。见平行表 PH-6b。

- **2026-07-23**：**Stage K 资产 ◐ 部分推进（贴图/物品模型/语言，两节点编译 + forge `runData`）**。PK-4 语言 ☑（8 语言各 419 键，映射链+回退）+ PK-3 物品模型 ◐（`ItemModelGen`+`ModelItemData`，runData 174 模型）+ PK-1 贴图 ◐（+51 byte-verified）+ PK-2 建材块模型（PB-3 228 gen）。新 `platform/ItemModelGen` + `data/gen/ModelItemData` + 迁 51 贴图 + 7 非英 lang。复杂贴图/块模型/特殊物品模型分离为 PK-1b/2b/3b；**PK-5 方块战利品 ◐**（新 `data/gen/ACBlockLoot` raw-DataProvider，runData 生成 139 gap-fill 掉落表：dropSelf，经 existingFiles 跳过 PB-4 已交付矿石 loot；ACBlockTags 因与 PB-4 minecraft 标签冲突移除，标签/配方 → PK-5b，CR-61）。Stage 9 余量大、未完成。见平行表 CR-60/CR-61。

- **2026-07-23**：**能量 PE 源（pilot deity statue · 使能量物品可填）**双端交付（有 CR-59）。承 CR-58 能量物品，建 1.12.2 唯一充能路径（`transferPEToNearbyPlayers`）：新 `content/block/energy/{DeityStatueBlock,DeityStatueBlockEntity,EnergyBlocks}`——pilot deity statue（`Block implements EntityBlock` + `getTicker`→`serverTick`，同 Materializer 先例）每 120 tick 给 7 格内最近玩家 main/off 手持的 `IEnergyContainerItem` 充 5 PE（`level.players()`+`distanceToSqr` fork-free）+ 存 `DeityType` 供延后 deity 过滤。接 `ModRegistries.ALL` +3（BLOCKS/ITEMS/BLOCK_ENTITIES）+ `ModCreativeTabs` blocks 页 + blockstate/model（cube_all 占位）+ lang。业务零 `//?`。两节点 `compileJava` + lang JSON 校验 + **forge `runServer` `Done`（4.676s）**（块+BE type+item 注册无碰撞、tick 无玩家不崩）。**能量物品现可填（worship）** → SpellUtils 可扣 PE（PS-7b）+ HUD PE meter 有值（PH-6b）+ pedestal 有源可抽；实际充能=活客户端人工验。仍延后（PS-5b）：完整 manipulator（amplifier/collector/multiblock/disruption）+ 7 statue 全套 + pedestal/container 能量方块网络。见平行表 CR-59。

- **2026-07-22**：**能量物品（PE-source store 基座 · 解锁 pedestal/spell/HUD）**双端交付（有 CR-58）。item-side 能量接口 + ItemStack-NBT compat + Necronomicon 实现：新 `platform/ItemDataCompat`（**ItemStack mod-data 版本 fork**：1.20.1 `getTag`/`getOrCreateTag` ↔ 1.21 `DataComponents.CUSTOM_DATA`/`CustomData`——PS-7 SpellUtils 延后的 ItemStack-NBT fork）+ `system/energy/{IEnergyContainerItem,IEnergyTransporterItem}`（忠实 1.12.2：抽象 `getMaxEnergy` + NBT-backed get/set/add/consume via `ItemDataCompat`）+ `NecronomiconItem` 改 `extends TooltipCompat implements IEnergyTransporterItem`（PE 按 tier {5000/10000/20000/40000/100000}=1.12.2 值 + `appendTooltip` 显 tier+PE）+ 2 tooltip lang。业务零 `//?`（fork 全封 `ItemDataCompat`）。两节点 `compileJava`（neo `CustomData` API 核）+ **两节点 `runData` selfTest（已还原）`ok=true`**（PE round-trip add→1000/consume→700/overflow→clamp 5000，**证 1.20 tag↔1.21 CustomData fork 运行期真 work**）。解锁 pedestal PE 抽取 + SpellUtils PE 扣（PS-7b）+ HUD PE meter（PH-6b）；仍延后 PE 填充源（deity 崇拜=collector+statue）+ 消费者。见平行表 CR-58。

- **2026-07-22**：**necrodata 网络同步接线（知识分支根 · PS-1 handler + PH-5 触发）**双端交付（有 CR-57）。3 个 necrodata handler 从 stub→功能：`SyncNecromancyData`（S→C，client 经 `SideExecutor`→新 `ClientNecroSync.apply`→`NecroDataCapability.apply`）+ `ShouldSync`（S→C→client 请求）+ `PrepareSync`（C→S→server 回推 `save(sender)`）；`NecronomiconItem.use()` server 侧 `sendToPlayer(ShouldSync)` 触发（忠实 1.12.2 book-open 三消息 dance）。新 `client/necronomicon/ClientNecroSync`（client-only，隔离 `Minecraft`，只经 `SideExecutor` 到达）。简化跳过 `lastSyncTime` 陈旧检查。两节点 `compileJava` + **forge `runServer` `Done`（3.428s）**（message 注册不 eager-classload client 类，deferred 边界正确）。实际 sync 往返 = 活双端人工验。**解锁 PS-8b 同步根**——知识分支后续（research 内容 + 门控书章节 + gate 用 `KnowledgeGate.isUnlocked`）有同步基座。见平行表 CR-57。

- **2026-07-22**：**PH-5b（Stage H2 / T6.2）死灵之书常驻信息章节**双端交付（无 CR，PH-5 own + 追加 lang）。承 CR-55 书物品（书可打开），把 `ACNecronomicon.root()` 从 pilot（root+intro）扩为忠实信息树：Introduction 分类（AbyssalCraft / The Great Old Ones / Obtaining Knowledge）+ Dimensions 分类（Abyssal Wasteland / Dreadlands / Omothol / Dark Realm）共 8 常驻条目 + 19 lang 键（对照 1.12.2 `necronomicon.information.*` 结构，正文忠实摘要）。两节点 `compileJava` BUILD SUCCESSFUL + en_us.json `ConvertFrom-Json` 校验通过（19 gui 键）。仍延后：research 门控章节（需 PS-8b research + PS-1 necrodata sync）+ 完整页文本/书贴图（PK）；翻页/渲染观感 = 人工目视。见平行表 PH-5b。

- **2026-07-22**：**Gate J ◐ 部分过（Stage J 集成 JEI/配置/命令/进度，两节点编译 + forge `runServer`）**。PJ-2 配置 ☑（130 标量选项 → 355 行 toml）+ PJ-3 命令 ☑（`/acunlockallknowledge`，`CommandCompat` RegisterCommandsEvent fork，控制台证注册+执行）+ PJ-4 进度 ☑（9 dual-dir，runServer 零加载错）+ PJ-1 JEI ◐（3 机器 + anvil 分类，平台加 `DataRecipeCompat.allOfType`）。新 `system/command/ACCommands`·`platform/CommandCompat` + `config/ACConfig`（130 选项）+ 18 advancement JSON + `integration/jei/AnvilForgingCategory`。业务零 `//?`。3 硬阻塞延后分离为 PJ-1b/2b/4b（JEI rending·ritual·spell / 配置非标量 / IMC·IACPlugin）。见平行表 CR-56。

- **2026-07-22**：**上游内容解锁 · Necronomicon 书物品（→ PH-5）**双端交付（有 CR-55）。应「补上游前置内容真正解锁 Stage 6/7」建 5 忠实书物品（`content/item/book/{NecronomiconItem,BookItems}`，类型 0-4），`use()` 非潜行右键 → `ACNecronomicon.open()`（PH-5 屏幕，client 经 `SideExecutor` 延迟 classload）。接 `ModRegistries.ALL` + `ModCreativeTabs` items 页 + 5 lang。两节点 `compileJava` + **forge `runServer` `Done`（3.471s）**（5 书注册无碰撞 + server 端 Done 证 client-classload 正确延迟）。**解锁 PH-5b 书物品依赖**（死灵之书可在游戏内打开）；仍延后 20+ 章节内容 + 书贴图（PK）+ bookType 门控（PS-8b）；右键开书观感 = 人工目视。见平行表 CR-55。

- **2026-07-22**：**PS-9（Stage S-C / T7.5）扰动内容扩充**双端交付（有 CR-54）。从「框架先于内容」推进到 10 具体 vanilla-only 扰动：新 `Disruptions`（`bootstrap()` 注册，主类 init +1 行）+ `PlayerDisruption`（玩家列表 `BiConsumer` 施动）。忠实 1.12.2：lightning + 5 药水云（poison/slowness/weakness/wither vanilla `MobEffects` + coralium PS-4 `ACEffects.CORALIUM_PLAGUE`，600t/16 格 `LivingEntity`）+ 4 玩家定向（freeze=SLOWNESS amp14、randomTeleport=`randomTeleport`、famine×2 deity-tied=`setFoodLevel(0)`）。业务零 `//?`（effect 常量/ctor co-vary + `getFoodData`/`randomTeleport` 两端同签名）。两节点 `compileJava` + `runData` selfTest `count=10 ok=true`。仍延后（分离为显式 PS-9b）：实体 spawn/swarm、ooze（未移植块）、PE drain、corruption、fire/fireRain（ignite loader fork）、deity-image、网络反馈；实际扰动触发=活会话人工验。见平行表 CR-54。

- **2026-07-22**：**PH-4b（Stage H1 收尾 / T6.5）实体音效接线**双端交付（有 CR-53）。新增 `ModSounds.event(String id)` 查询 helper，把 5 类跨 owner 实体接到已注册 SoundEvent：`AbstractGhoul`(PD-5)→ghoul.normal.idle/hit/death、`AbstractShoggoth`(PD-5)→shoggoth.idle/hit/death、`EliteMob`(PD-7，新增 `EliteKind kind` 字段)→DREADGUARD dreadguard.idle/hit/death · REMNANT remnant.scream、`BossMob`(PD-7，新增 `BossKind kind` 字段)→SACTHOTH shadow.hit/sacthoth.death、`AntiPlayer`(PD-3)→antiplayer.hurt。均 protected `Mob` override（`getAmbientSound`/`getHurtSound`/`getDeathSound`，两版 fork-free）；无 bespoke 音的 kind 回落 `super` vanilla 默认=非回归。业务零 `//?`。两节点 `compileJava` BUILD SUCCESSFUL + 13 处接线 grep 核实。仍延后（依赖未移植行为）：shoggoth AI 音/boss 技能音/remnant 交易台词/depths-ghoul 彩蛋；音效实际播放=人工客户端听测。见平行表 CR-53。

- **2026-07-22**：**Gate H1 ◐ 部分过（Stage H1 客户端 GUI/天空/粒子/音效，两节点编译 + forge `runClient`）**。PH-4 音效 ☑ + PH-2 天空雾 ◐ + PH-3 粒子 ◐ + PH-1 机器屏幕（P2/PC-8 已交付）。新 `registry/ModSounds`（45 SoundEvent）+ `registry/ModParticles`（abyssal_fx）+ `client/sky/ACDimensionEffects`·`ACDimensionSkies`（4 维忠实雾色）+ `client/particle/ACFadeParticle`（TextureSheetParticle 重写 ACParticleFX）+ `platform/DimensionEffectsCompat`·`ParticleCompat`（2 client loader-fork compat）+ 资产（sounds.json/106 .ogg/42 subtitle/particles 描述符+贴图/3 天空贴图）。接线 `ModRegistries.ALL` +2 + `ACClientSetup.registerParticles` + 主类 client block +2 attach。两节点 `compileJava` EXIT=0 + forge `runClient` 进世界干净退出（sounds.json 零解析错 + abyssal_fx 进 particles atlas + 4 维 effects 全绑定无 fallback）。延后：自定义天空盒渲染（1.20.1↔1.21 即时模式差异）、PEStream/ItemRitual 粒子（近似 vanilla + 未移植 ritual/PE）、物品容器屏幕、实体接声音（PH-4b/CR-53 已收）；渲染观感=人工目视。见平行表 CR-52。

- **2026-07-22**：**Gate H2 ✅ 已过（Stage H2 收口）**。PH-5..6 全 ☑ —— 死灵之书 GUI（PH-5，`NecronomiconScreen` 递归 entry 树 + PS-8 只读门控）+ HUD/字体/clientvars（PH-6，`AkloFont` + `ClientVars` 热重载 + `ClientHooksCompat` HUD overlay/reload fork + pilot）。PH-5 无 CR（纯新增 `client/necronomicon/**`）；PH-6 有 CR（`platform/ClientHooksCompat` overlay/reload fork[javap 双 jar 核] + 主类 client block +2 行）。两节点编译 + `runClient` 抵标题屏（aklo.json/clientvars.json 解析零错 + HUD 注册零错 + Screen 类加载零错）。翻页/HUD/字体渲染 + 热重载 = 人工目视。见平行表 Gate H2。
- **2026-07-22**：**PH-6（Stage H2 / T6.6）HUD/字体/clientvars 框架**双端交付（有 CR）。字体 `client/font/AkloFont`+aklo.json（reference 占位）+ clientvars `client/hud/{ClientVars,ClientVarsManager 热重载}`+clientvars.json + HUD `platform/ClientHooksCompat`（overlay `IGuiOverlay`↔`LayeredDraw.Layer` + reload fork，javap 双 jar 核）+ `client/hud/ACHud` pilot + 主类 client block +2 行。两节点编译 + runClient 抵标题屏（资源解析 + HUD 注册零错）。HUD 内容/Aklo 位图/全 clientvars 色/keybind 延后（未移植能量/staff 物品 + PK 资产）。见 `docs/spec/hud-font-clientvars-subsystem.md`。
- **2026-07-22**：**PH-5（Stage H2 / T6.2）死灵之书 GUI 框架**双端交付（无 CR）。新 `client/necronomicon/{NecronomiconEntry(递归树+researchId 门控),NecronomiconScreen(category 导航+PS-8 只读门控+font.split 正文),ACNecronomicon(pilot+open)}`；`renderBackground` 分叉走既有 `ClientScreenCompat.background`（render 顺序坑：自定义文字放 super.render 后）。**读 PS-8/PS-2**（necrodata 门控书 entry 显示）。两节点编译 + runClient 抵标题屏（Screen 类加载零错）。20+ 具体章节/页 + 书贴图 + 书物品延后（未移植 research/物品/贴图）；翻页/渲染 = 人工目视。见 `docs/spec/necronomicon-gui-subsystem.md`。
- **2026-07-22**：**PG-6（Stage G2 / T5.7）传送门 teleport 框架 ◐（两节点 `runServer` 全 runtime）**。新 `platform/TeleportCompat`（跨维 `changeDimension` fork：1.20.1 `ITeleporter`/`PortalInfo` ↔ 1.21 `DimensionTransition`，Mojang 1.21 重写传送）+ `world/portal/DimensionTeleport`（落点缩放/border/地表）+ 接线 PD-6 `DimensionPortal`（destination+NBT+tick 传送、singleUse discard）。业务零 `//?`（fork 全封 `TeleportCompat`）。两节点 `compileJava` + `runServer`：`summon portal{Destination:AW}`+cow → cow 跨维 overworld→abyssal_wasteland 双端（FORGE/NEO_TELEPORT_OK）。延后：portal_anchor 方块+激活 ritual（PS-6）/gateway key、目标 mob 刷怪、portal-frame 建造、home/dark-realm teleporter、渲染（E）。见 `docs/spec/worldgen-subsystem.md` §12，见平行表 CR-51。
- **2026-07-22**：**PG-7（Stage G2 / T5.8·T5.9）群系特征放置（两节点 `runServer` 全 runtime）**。14 feature biome_modifier（7 feature × forge/neoforge `{forge,neoforge}:add_features`）挂 PG-4 placed_feature 到各维度 biome（stalagmite/monolith/tree/lake→AW/dreadlands/omothol/dark_realm，忠实 §5 ID）。纯数据包无 Java/relay/main/lang。forge/neo 均 `Done` 零 biome_modifier 加载错 + neo omothol gen shoggoth_monolith（monolith_stone×12/100chunk 采样实证）。移出：carver 挂 biome（biome-JSON 1.20↔1.21 分叉 / 无 forge carver modifier）→ 待办；Darklands 主世界覆盖 → 待 Darklands 任务。**T5.9 worldgen 验证收口**（4 维度+结构+特征+teleport 双端 runServer 全绿）。见 `docs/spec/worldgen-subsystem.md` §11，见平行表 CR-50。
- **2026-07-22**：**Gate S-C ✅ 已过（Stage S-C 收口 = M7 系统层收尾）**。PS-9..11 全 ☑ —— 扰动（PS-9）+ Places of Power（PS-10）+ 存档/知识钩子（PS-11）。PS-9/10 无 CR（纯新增 `system/energy/{disruption,structure}/**`）；PS-11 有 CR（`platform/{SavedDataCompat,GameHooksCompat}` + 主类 `GameHooksCompat.attach()`）。两节点编译 + selfTest（已还原）forge/neo PASS；PS-11 另两节点 `runServer` `Done`。跨任务契约：PS-9/10 读 PS-5、PS-11 写 PS-2 NecroData→PS-8 读（知识环路闭合）。**M7 全系统层 PS-1..11 框架交付完毕**。见平行表 Gate S-C。
- **2026-07-22**：**PS-11（Stage S-C / T7.11）存档 + 知识钩子框架**双端交付（有 CR）。新 `platform/SavedDataCompat`（`SavedData.save`/`computeIfAbsent` 1.20↔1.21 fork）+ `system/data/NecromancyData`（≤20 复活快照 + save/load round-trip）+ `platform/GameHooksCompat`（game-bus `LivingDeathEvent`/`PlayerChangedDimensionEvent` 订阅 fork）+ `common/handlers/KnowledgeHooks`（杀怪/换维度→PS-2 `NecroData` 触发，PS-8 读）+ 主类 `GameHooksCompat.attach()`。两节点编译 + selfTest（已还原）+ `runServer` `Done`。坑：`PlayerChangedDimensionEvent.getEntity()` 已返 Player→去多余 instanceof。Plague/Purge 钩子 + 复活消费 + 同步接线延后。见 `docs/spec/saveddata-hooks-subsystem.md`。
- **2026-07-22**：**PS-10（Stage S-C / T7.4）Places of Power 框架**双端交付（无 CR）。新 `system/energy/structure/{IPlaceOfPower,IStructureComponent,IStructureBase,StructureHandler,SimplePlaceOfPower}`：忠实 1.12.2 `api.energy.structure.*`（PoP 接口 + 多块 BE marker + 注册表 bookType 门控 + pilot）。Reads PS-5 `AmplifierType`；`IResearchItem` 门控解耦为 `researchId`（PS-8 读）。两节点编译 + selfTest（已还原）双端 PASS。具体 PoP 多块 + construct/renderData 延后（依赖未移植能量块/statue）。见 `docs/spec/places-of-power-subsystem.md`。
- **2026-07-22**：**PS-9（Stage S-C / T7.5）扰动系统框架**双端交付（无 CR）。新 `system/energy/disruption/{Disruption,DisruptionHandler,PotionDisruption,LightningDisruption}`：忠实 1.12.2 `api.energy.disruption.*`（扰动基 + handler register/去重/deity 过滤随机 + potion/lightning pilot）。Reads PS-5 `DeityType`。业务零 `//?`（`new MobEffectInstance(MobEffects.POISON,dur)` co-var fork-free）。两节点编译 + selfTest（已还原）双端 PASS。~27 具体扰动 + deity-image 检测 + 网络反馈延后（依赖未移植实体/能量块）。见 `docs/spec/disruption-subsystem.md`。
- **2026-07-22**：**Gate S-B ✅ 已过（Stage S-B 收口）**。PS-5..8 全 ☑ —— PE 能量网络（PS-5）+ 仪式（PS-6）+ 法术（PS-7）+ 知识（PS-8，读 PS-2 necrodata）。四子系统框架先于内容、无 relay/platform/main/lang 改 → 全无 CR；每任务两节点 `compileJava` + selfTest（已还原）forge/neo 均 PASS。跨任务契约成立（PS-6/7 energy→PS-5、researchId 解耦 PS-8、PS-8 消费 PS-2 NecroData）。延后：能量方块/采集链、altar+13 仪式+产物、SpellUtils+14 法术+GUI、书 GUI+20+ 条件+触发钩子+同步 handler（依赖未移植块/物品/实体/书）。**解锁 Stage S-C（PS-9/PS-10/存档钩子）**。
- **2026-07-22**：**PS-8（Stage S-B / T7.8）知识/死灵之书数据层框架**双端交付。新 `system/knowledge/{IResearchItem,ResearchItem,ResearchRegistry,KnowledgeGate}` + `condition/{IUnlockCondition,UnlockCondition,EntityCondition,DimensionCondition,MiscCondition,MandatoryMultiEntityCondition,IConditionProcessor,ConditionProcessorRegistry}`：忠实 1.12.2 `api.knowledge.*`（研究注册表 + 研究条目 + 解锁条件 + 条件处理器注册表[构造期 10 个 fork-free 处理器 type 0-4/7-11]+ `KnowledgeGate.isUnlocked` 条件/研究双版 + 研究 auto-complete）。**读 PS-2** `NecroData`（7 触发列表+completedResearches，PS-2 只存/PS-8 判）。无 CR（纯新增自有 `system/knowledge/**`）。两节点编译 + selfTest（已还原）双端 PASS。Necronomicon 书 GUI + 20+ 具体条件 + type 5/6 谓词 + 触发事件钩子 + 同步 handler 接线延后（依赖未移植实体/群系/维度/书；PS-1 necrodata 消息 stub、PS-2 save/apply 就绪）。见 `docs/spec/knowledge-subsystem.md`。
- **2026-07-22**：**PG-5（Stage G1 / T5.6）结构迁移程序化子集 ☑（两节点 `runServer` 全 runtime）**。4 Java `world/structure/{StructureKind,ACStructure,ACStructurePiece}` + `platform/StructureCompat`（2 fork：`structureType` MapCodec↔Codec + `setChestLoot` `ResourceLocation`↔`ResourceKey<LootTable>`）+ `ModWorldgen` STRUCTURE_TYPE/PIECE registrar + 15 JSON（3 structure + 3 structure_set random_spread + 3 has_structure biome tag + 6 loot table 双目录）。relay `ModRegistries.ALL` +2。`ACStructurePiece` 建 5×5 floor+4 角柱+中心 loot 箱（load-ctor SAM=`StructurePieceSerializationContext`）。两节点 `compileJava` + `runServer` `Done` + `/place structure` 建箱体双端 + `data get block LootTable`=`abyssalcraft:chests/<kind>`（forge dark_shrine / neo graveyard，证 setChestLoot 分叉双端）+ `/locate structure` AW 自然生成双端（forge graveyard 160 / neo abyruin 441 blocks）。**移出本任务（不阻塞收口）**：36 二进制 `.nbt` jigsaw（需游戏内格式重存+调色板重映射，非 headless）+ 其余程序化结构 → **T5.6b**；箱 AC 专属 loot 物品 → **PK-5**；has_structure biome 交叉引用 → **Gate G1/PG-7**。细节见 `docs/spec/worldgen-subsystem.md` §10，见平行表 CR-48。
- **2026-07-22**：**PG-4（Stage G1 / T5.5）特征迁移（两节点 `runServer` 全 runtime）**。3 代码 `Feature`（`world/feature/{Stalagmite,Monolith,DeadTree}Feature`，fork-free）+ 29 JSON（8 configured_feature + 8 placed_feature + 3 configured_carver + 2 additive `overworld_carver_replaceables` tag）。触碰 G0 `ModWorldgen.FEATURES` +3 register（无 relay/main/lang）。石笋/monolith 自定义 `Feature`；树/湖=`minecraft:tree`/`minecraft:lake` 数据驱动（lake=water 占位待流体）；carver 用**纯 float 避 value-wrapper 分叉**；worldgen 单目录。两节点 `compileJava` + `runServer` `Done` 零加载错 + forge `/place feature abyssal_stalagmite`→abyssal_stone + neo `shoggoth_monolith`→monolith_stone + `darklands_tree`→darklands_oak_log。延后：lake 流体/dead_tree_log 占位 darklands_oak_log/特征挂 biome=Gate G1。细节见 `docs/spec/worldgen-subsystem.md` §9，见平行表 CR-47。

- **2026-07-22**：**PS-7（Stage S-B / T7.7）法术系统框架**双端交付。新 `system/spell/{ScrollType,IScroll,Spell,SpellRegistry,EntityTargetSpell,LifeDrainSpell}`：忠实 1.12.2 `api.spell.*`（Spell 基 order-free reagent matches + 抽象 canCast/castServer/castClient + SpellRegistry register[bookType 0-4 校验+去重]/find[reagent+bookTier+scrollType 门控] + EntityTargetSpell/LifeDrainSpell pilot）。`IResearchable` 门控解耦为可选 `researchId`（PS-8 读）。无 CR（纯新增自有 `system/spell/**`）。两节点编译 + selfTest（已还原）双端 PASS。SpellUtils（PE 自能量物品 + NBT + raytrace + cast 派发）+ 14 具体法术 + scroll/staff/spellbook GUI + glyph + MobSpell 网络延后（依赖未移植试剂/物品/内部 handler）。见 `docs/spec/spell-subsystem.md`。
- **2026-07-22**：**PS-6（Stage S-B / T7.6）仪式系统框架**双端交付。新 `system/ritual/{Ritual,RitualRegistry,InfusionRitual}`：忠实 1.12.2 `NecronomiconRitual`（offerings/energy[PS-5]/sacrifice + 抽象 complete + order-free matches + registry find）+ InfusionRitual pilot。`IResearchable` 门控解耦为可选 `researchId`（PS-8 读）。无 CR（纯新增自有 `system/ritual/**`）。两节点编译 + selfTest（已还原）双端 PASS。altar/pedestal 块 + 13 具体仪式 + 产物延后（依赖未移植块/物品/实体/Necronomicon）。见 `docs/spec/ritual-subsystem.md`。
- **2026-07-22**：**PS-5（Stage S-B / T7.3）PE 能量网络框架（两节点编译 + selfTest）**。能量框架（内容-无关，同 PD-2/PC-4 先例）——新 `system/energy/{DeityType(7 神),AmplifierType(3),IEnergyContainer,IEnergyCollector,IEnergyManipulator,IEnergyTransporter,PEUtils}`。忠实 1.12.2 `api.energy` 核心：能量 BE 接口（fork-free，同 PC-4 `ItemTransferHost`）+ `PEUtils` 能量算术（addEnergy 溢出/consume/transfer）+ manipulator→collector BE 网络。仅 vanilla `Level`/`BlockEntity`/`BlockPos`，业务零 `//?`，无 relay/platform/main/lang 改（无 CR）。两节点 `compileJava` BUILD SUCCESSFUL + 临时 `PEUtils` selfTest（已还原）双端 PASS。延后内容：能量方块（5 型×4 tier）+ BE + idol/charm + 采集(statue)→传输(Necronomicon)→PE stream(PS-1)——依赖未移植块/物品/书，复用本框架。细节见 `docs/spec/energy-subsystem.md`。
- **2026-07-22**：**Gate S-A ✅ 已过（Stage S-A 收口）**。PS-1..4 全 ☑ —— 网络（PS-1 self-test 24/24）+ necrodata 能力（PS-2 NBT round-trip）+ 5 附魔（PS-3 `/enchant` 双端）+ 5 药水效果/7 药水（PS-4 `/effect`「antimatter 杀牛」双端）。四 compat 新增/消费：`NetworkChannel`(消费)/`PlayerDataCompat`/`EnchantmentCompat`/`MobEffectCompat`。两节点 `compileJava` + `runServer` `Done` + `runClient` 抵标题屏全绿。延后下游：消息 handler(各 PS-X)、necrodata isUnlocked/同步(PS-8)、附魔+药水效果 event hook(PS-11)、brewing(PC-8)、活玩家/实网(人工)。**解锁 Stage S-B（PS-5..8）**。
- **2026-07-22**：**PS-4（Stage S-A / T7.10）药水效果（两节点编译 + runServer `/effect` 探针 + runClient）**。5 MobEffect（coralium/dread plague + antimatter 带伤害 tick + 2 antidote）+ 7 brewable Potion（cplague/_long、dplague/_long/_strong、antimatter/_long）双端交付。**MobEffect 两端代码化**（异于 1.21 附魔），注册 fork-free 用 `ModRegistrar`；2 fork 落 `platform/MobEffectCompat`：`ACMobEffect`（`applyEffectTick` void/boolean + `isDurationEffectTick`/`shouldApplyEffectTickThisTick` 重命名 → fork-free `Tick` 回调）+ `effectInstance`（`MobEffectInstance` raw / `Holder` via `wrapAsHolder`）。**PotionBuilder 等价** = `ACMobEffect`+`Tick`。新 `system/effect/ACEffects`（fork-free）+ `ModRegistries.ALL` +2 + 12 lang。忠实颜色/duration。javap 双 jar 核。业务零 `//?`。两节点 `compileJava` BUILD SUCCESSFUL；两节点 `runServer` `Done`（MOB_EFFECT+POTION 冻结无崩，neo wrapAsHolder 无崩）+ **`/effect` 探针**（summon 牛 + antimatter → `Applied effect Antimatter to Cow` + 牛 10HP→死 = 伤害 tick 双端）+ `runClient` 抵标题屏。效果（plague 传播/免疫/antidote 治愈/antimatter→anti 转化）延后 **PS-11**（`magic()` 占位伤害源）+ brewing → **PC-8**；potion `/give` 需活玩家。细节见 `docs/spec/potion-subsystem.md`，见平行表 CR-46。
- **2026-07-22**：**PS-3（Stage S-A / T7.9）附魔（5）双实现（两节点编译 + runServer `/enchant` 探针 + runClient）**。5 附魔（blinding_light/iron_wall/light_pierce/multi_rend/sapping）双端可获得。**硬 fork（1.20 类 / 1.21 数据驱动）**：新 `platform/EnchantmentCompat`（forge `DeferredRegister<Enchantment>` + 通用 `AbyssalEnchantment` 参数化 metadata / neo `bootstrap` no-op，数据驱动）+ `system/enchant/ACEnchantments`（fork-free 5 `ResourceKey<Enchantment>`）+ 5 datapack JSON `data/abyssalcraft/enchantment/*.json` + 空 tag `#abyssalcraft:enchantable/staff_of_rending` + en_us 5 键 + 主类 `EnchantmentCompat.bootstrap`。忠实 1.12.2 metadata（rarity/cost/maxLevel/slot/supported items）；multi_rend/sapping 用未移植 Staff 分类（1.20.1 `EnchantmentCategory.create(→false)` / 1.21 空 tag）。javap 双 jar 核 1.20.1 `Enchantment` ctor/cost/`EnchantmentCategory.create`；subagent 核 vanilla 1.21 `fortune`/`sharpness` JSON schema。业务零 `//?`。两节点 `compileJava` BUILD SUCCESSFUL；neo `runServer` `Done` = 5 datapack 附魔 JSON 解析入注册表零 schema 错；forge `runServer` `Done` = 5 代码附魔注册无崩；**两节点 `/enchant` 探针**（summon 僵尸持钻剑/钻胸甲）`Applied enchantment {Light Pierce III/I, Iron Wall} to Zombie's item`（注册+applicable+tag+lang 四通双端）；两节点 `runClient` 抵标题屏。附魔效果（iron_wall 零动量/blinding_light/light_pierce vs shadow）延后 **PS-11** event hook；multi_rend/sapping 待未移植 Staff。细节见 `docs/spec/enchantment-subsystem.md`，见平行表 CR-45。
- **2026-07-22**：**PG-1（Stage G1 / T5.2）Abyssal Wasteland 维度（两节点 `runServer` 全 runtime）**。AW 5 群系维度（R3「多群系用 `multi_noise` 数据包」）——8 数据包 JSON、无 Java/relay/main/lang：`abyssalcraft:abyssal_wasteland` dimension/dimension_type/noise_settings + 5 biome。忠实 1.12.2：dimension_type has_skylight/无 ceiling/`ambient_light:0.25`/`fixed_time:18000`/`bed_works:false`；default_block=`abyssal_stone`、sea `minecraft:water`@55(忠实=liquid_coralium 待流体)、biome-conditioned surface_rule(plateau→coralium_stone / wastelands·swamp→fused_abyssal_sand+abyssal_sand / 余→abyssal_sand)；5 群系 `multi_noise` 编造 temp/humidity 带逼近 `GenLayerBiomesAW` 均匀分布。跨版本安全 `monster_spawn_light_level` 纯 int；接入 ACDimensions=G0 冻结 `ABYSSAL_WASTELAND`（零码）。两节点 `runServer` `Done` 零 worldgen 错 + `/execute in abyssalcraft:abyssal_wasteland` 进入 + 5 群系 locate 双端(forge 全 5 命中 0/115/704/747/320、neo plateau 32) + abyssal_stone 地形双端 + surface 双端(forge abyssal_sand 1111/wastelands fused_abyssal_sand 256=1柱、neo plateau coralium_stone 542)。延后：sea 流体/特征(PG-4)/刷怪(D2a·PL-4)/天空雾(T6.3)/per-biome 高度/人工目视观感；darklands-in-AW 怪癖不移植。细节见 `docs/spec/worldgen-subsystem.md` §8.3，见平行表 CR-44。
- **2026-07-22**：**PG-2（Stage G1 / T5.3）Dreadlands 维度（两节点 `runServer` 全 runtime）**。Dreadlands 4 群系维度（R3「多群系维度用 `multi_noise` 数据包」）——7 数据包 JSON、无 Java：`abyssalcraft:dreadlands` dimension/dimension_type/noise_settings + 4 biome（dreadlands/forest/mountains/ocean）。异于 PG-3 简单 fixed：4 群系分布用 `minecraft:multi_noise`（climate 点按 continentalness 三段 ocean -0.85 / base+forest 0 / mountains 0.85 + humidity 分 forest 0.75；noise_router climate 引 vanilla `overworld/{continents,erosion,ridges}` + inline `shifted_noise`(temperature/vegetation)，避全 0 塌缩）。忠实 1.12.2（`ChunkGeneratorDreadlands` + 4 biome + `BiomeProviderDreadlands`）：dreadstone 基岩 / 无海 lava(sea_level 31) / noise-hilly(`overworld/base_3d_noise`) / surface_rule 按 biome 换块(forest grass·dirt / ocean muck / base·mountains dreadstone) / dread 红观感(0x910000/fog 0x330808) / nether-like(`the_nether` + fixed_time 18000)。跨版本安全 `monster_spawn_light_level` 纯 int。**验证**：forge/neo 均 `runServer` `Done` 零 worldgen 加载错 + `/execute in abyssalcraft:dreadlands` 进入 + **4 群系全 `locate biome` 命中双端**（forge 0/0/128/1538、neo 181/0/731/430 blocks = 群系分布双端成立）+ forceload 生成 + dreadstone 地形。延后：dreadwood 树/dread 矿/石笋特征(PG-4)、mineshaft/lair 结构(PG-5)、mob 刷怪(D2a/PL-4)、逐 biome 真高度差、darklands 第 5 群系、忠实天空雾(T6.3)。细节见 `docs/spec/worldgen-subsystem.md` §8.2，见平行表 CR-43。
- **2026-07-22**：**PS-2（Stage S-A / T7.2）necrodata 玩家能力（两节点编译 + runServer/runClient 实证）**。item transfer 能力已由 PC-4 `ItemTransferHost` 覆盖；本任务交付 necrodata 玩家能力。新 `platform/PlayerDataCompat`——每玩家持久数据的最大 loader 分叉：**Forge 实体能力**（`Capability<Holder>` + `RegisterCapabilitiesEvent` + 游戏总线 `AttachCapabilitiesEvent` + `PlayerEvent.Clone` 死亡拷贝）↔ **NeoForge 数据附着**（`AttachmentType<CompoundTag>` + `serialize(CompoundTag.CODEC)` + `copyOnDeath`），存中性 `CompoundTag`（业务只读写 vanilla NBT，javap 双 jar 核全 API）。+ 3 业务文件 `system/cap/necrodata/{KnowledgeType,NecroData,NecroDataCapability}`（忠实 1.12.2 数据层：7 触发列表 + 研究 + 全知识标志 + 等级 + 5 枝知识点，fork-free NBT，dimension int→String）。主类 `init` +1 `PlayerDataCompat.bootstrap(modBus)`。业务零 `//?`。坑：neo `AttachmentType.builder(CompoundTag::new)` 重载歧义 → 显式 `() -> new CompoundTag()`。两节点 `compileJava` BUILD SUCCESSFUL；临时 selfTest（已还原）NecroData NBT round-trip forge/neo 均 PASS；两节点 `runServer` Done + 干净 stop（**neo 现抵 Done**，得益 G0/CR-39 修 `mini.json`）+ `runClient` 抵标题屏（能力/附着注册双端无崩）。`isUnlocked`/条件处理 + 客户端同步消息 handler 接线归 PS-8（供 `NecroDataCapability.{get,save,apply}`）；活玩家逐次 attach/持久化/同步 = 人工会话。细节见 `docs/spec/capability-subsystem.md`，见平行表 CR-41。
- **2026-07-22**：**PG-3（Stage G1 / T5.4）Omothol + Dark Realm 维度（两节点 `runServer`）**。2 简单纯数据包维度（R3）——8 数据包 JSON、无 Java：`abyssalcraft:{omothol,dark_realm}` 各 dimension/dimension_type/noise_settings/biome（复用 G0 noise_settings 模板）。忠实换块 Omothol=`omothol_stone` / Dark Realm=`darkstone`（一手核 `ChunkGenerator*`/`Biome*` top+filler）；暗天 `effects=minecraft:the_end` + `has_skylight:false` + `natural:false` + `bed_works:false` + ambient 0.15/0.05；单群系 `minecraft:fixed`；`default_fluid=air`；`monster_spawn_light_level` 纯 int（承 G0 R1 跨版本安全）。接入 ACDimensions = G0 冻结 `OMOTHOL`/`DARK_REALM` `ResourceKey<Level>` + dim id 对齐（零码）。两节点 `runServer` 均 `Done` 零 worldgen 加载错；forge 全 runtime（两维度 `/execute in` 进入 + 地形 omothol_stone/darkstone@y10 + locate biome 0）；neo `Done` + omothol `/execute in` 进入 + forceload crashless 生成（余显式探针未获 = neo runServer 空闲退出/并发环境阻，据 neo Done + omothol chunk-gen crashless + G0 已在 neo 全证同一 vanilla noise 机制 + forge 全探针，诚实）。延后：结构(PG-5)/自然刷怪(D2a/PL-4)/自定义天空雾(T6.3)/浮空岛真形。细节见 `docs/spec/worldgen-subsystem.md`，见平行表 CR-40。
- **2026-07-22**：**PG-0（Stage G0 / T5.1）worldgen 框架 + 迷你维度竖切 + R1/R2/R3 调研（两节点 `runServer` 实证）**。框架 `world/ACDimensions`※（4 维度 + `MINI` 的 `ResourceKey<Level>` 冻结契约，fork-free）+ `world/feature/MiniPillarFeature`（示例代码 `Feature`，证 PG-4 路径）+ `registry/ModWorldgen`（`Registries.FEATURE` registrar，接 `ModRegistries.ALL`）。竖切 6 数据包 JSON（两端共享单目录，worldgen 路径跨版本不分叉）`abyssalcraft:mini` 全链路（dimension_type + noise_settings[自定义 `final_density` via `y_clamped_gradient` + `stone_depth` surface_rule，无自定义 DensityFunction codec] + biome + dimension[`minecraft:noise`+`fixed`] + configured/placed_feature）。跨版本坑：`dimension_type.monster_spawn_light_level` IntProvider 1.20.1（min/max 套 `value`）↔1.21（顶层）分叉 → 共享 JSON 用纯 int（**顺解 PS-1/CR-38 记录的 neo `runServer` `dimension_type/mini.json` 崩**）。两节点 `compileJava --rerun-tasks` + `runServer`（隔离 level-name/port）：forge/neo 均 `Done`、`/execute in abyssalcraft:mini` 生成地形（stone@y10）+ `locate biome` 0 + 特征 `fill replace` = 8 glowstone（2 柱×4，双端一致）。§5 定稿冻结 R1（数据驱动 schema 全链路）/R2（36 nbt 可 jigsaw 但需格式重存+调色板重映射、程序化退 StructureType）/R3（简单维度纯数据包、多群系维度可能退代码 BiomeSource）+ 维度/群系/特征/结构 ID 命名。**解锁 Stage G1**。细节见 `docs/spec/worldgen-subsystem.md`，见平行表 §5 + CR-39。
- **2026-07-22**：**PS-1（Stage S-A / T7.1）网络层双实现（两节点编译 + 4 启动实证）**。`platform/NetworkChannel` 首个运行期消费者：新 `net/ACNetwork`（注册全 23 消息 id 0-22 + `bootstrap`/`sendToServer`/`sendToPlayer`/`sendToAll` 便捷）+ 23 消息（11 server C→S + 12 client S→C，impl `NetworkChannel.ACPacket`）忠实 primitive 序列化（BlockPos/UUID/NBT/UTF/varint/long/boolean）。主类 `init` +1 行 `ACNetwork.bootstrap(modBus)` + 1 import（mod-bus 站点）。**ItemStack fork**：`FriendlyByteBuf.writeItem/readItem` 仅 forge、neo 1.21 删（需 `RegistryFriendlyByteBuf`）→ `TransferStackMessage` 的 ItemStack 用 item-id+count fork-free（neo 字节码核 `getKey`→`writeUtf`+`writeVarInt` 无 `writeItem`）。**handler 全为延后 stub**（各注目标：ritual→PS-6/spell→PS-7/PE→PS-5/necromancy→PS-8/disruption→PS-9 + 未移植 items/blocks/menus）；登记 `Context.player()`=发送方（client-bound handler 需 `SideExecutor` 取客户端玩家）。业务零 `//?`（loader 网络 API 全封 `NetworkChannel`）。两节点 `compileJava --rerun-tasks` BUILD SUCCESSFUL；**临时 selfTest（验证后已还原，同 CR-22 先例）forge/neo 均 `network self-test: 24/24 messages round-tripped byte-stable`**（全 23 消息 write→decode→write 字节稳定）；forge `runServer` `Done (6.816s)`+干净 stop、forge/neo `runClient` 抵标题屏（bootstrap SimpleChannel/Payload 双端双向注册无崩）。neo `runServer` 未抵 Done = **无关既存** `dimension_type/mini.json` IntProvider 格式崩（非 `net/**`、属维度任务、已登记协调项；PS-1 网络在其前跑通）。实网 C↔S 逐消息同步 = 人工会话。细节见 `docs/spec/network-subsystem.md`，见平行表 CR-38。
- **2026-07-22**：**PE-5（Stage E2）标准护甲穿戴层（两节点 `compileJava` + neo `runClient` 实证）**。7 材料 × 4 = 28 护甲（PB-7 注册）穿戴经 **vanilla `HumanoidArmorLayer`** 渲染 → 标准护甲无需自定义 `client/render/armor/` 代码。接线全在 `platform/ArmorCompat`（PB-7 冻结文件的 E5 集成、其 note 已预告「穿戴层延 E 渲染」）：1.20.1 材质 `getName()` 返 namespaced `abyssalcraft:<name>`（Forge armor-location patch 拆 namespace）、1.21 材质带单元素 `Layer` 列表 `new ArmorMaterial.Layer(ACRef.id(name))` → 两端均解析到 `abyssalcraft:textures/models/armor/<name>_layer_{1,2}.png`。迁 14 贴图（7 材料 × 2 层：abyssalnite/refined_coralium/plated_coralium/depths/dreadium/dreadium_samurai/ethaxium）从 1.12.2 `textures/armor/*` 到 `textures/models/armor/*`。两节点 `compileJava` BUILD SUCCESSFUL + neo `runClient` 抵标题屏 **0 crash / 0 armor-tex-missing**（材质带层构造正常、客户端加载不崩）。延后：自定义护甲模型（武士甲专属外形/depths 发光）、怪物护甲层（ghoul/goliath `LayerGhoulArmor` → 协调 PE-3/4、贴图未迁）、穿戴视觉人工目视。细节见 `docs/spec/entity-subsystem.md` §12.3，见平行表 CR-35。
- **2026-07-22**：**PE-3（Stage E2）ghoul + shoggoth 渲染（两节点 `runClient` 实证）**。8 实体（ghoul 5 + shoggoth 3）faithful 渲染。异于 PE-2（ghoul/shoggoth 贴图绑专属 UV，vanilla biped 会错位）→ 新 `client/model/entity/{GhoulModel,ShoggothModel}`（`HierarchicalModel` 简化-忠实主体，真 texOffs/坐标）+ `client/render/entity/{GhoulRenderer,ShoggothRenderer,GhoulShoggothRenderers,layers/SimpleEyesLayer}` + 迁 22 贴图。发光眼用 vanilla `EyesLayer` 子类（避 `renderToBuffer` 1.20↔1.21 fork）。接 PE-1 `ACEntityRenderers`（handled-set + 2 自定义 `ModModelLayers.{GHOUL,SHOGGOTH}` 层）。业务零 `//?`。两节点 `compileJava`+`runClient`：各抵标题屏、0 missing-renderer/0 layer-bake/0 validateRegistrations/0 crash。诚实：headless `runClient` 只验注册/烘焙/加载不崩、不能目视模型 → 精确视觉=人工目视；细节 cube/armor(→PE-5)/TYPE 彩蛋/半透明/动画延后。细节见 `docs/spec/entity-subsystem.md` §13，见平行表 CR-34。
- **2026-07-22**：**PE-2（Stage E2）anti + demon 渲染（两节点 `runClient` 实证）**。19 实体（anti 11 + demon 8）faithful 渲染：新 `client/render/entity/{ACTexturedRenderer(通用 vanilla 模型 + 固定贴图),AntiDemonRenderers(注册 19)}` + 迁 18 贴图（13 anti + 5 demon）。忠实 1.12.2 `RenderAnti*`/`RenderDemon*`：anti/demon = vanilla 模型 + AC 贴图，**evil = vanilla 模型 + vanilla 贴图**（长得像正常动物）。bipeds→`HumanoidModel`、动物→各 vanilla 动物模型，`bakeLayer(ModelLayers.X)` 复用 vanilla 模型层（无新 `ModModelLayers`）。接 PE-1 `ACEntityRenderers`（`handled`-Set 分发覆盖 E1 占位）、无 main/lang 改。业务零 `//?`（坑：`BatModel` 非泛型）。两节点 `compileJava` BUILD SUCCESSFUL + `runClient`：各抵标题屏、**0 missing-renderer/0 missing-texture/0 duplicate/0 crash**。延后：自定义 `ModelGhoul`/sheep wool/spider eyes/armor(→PE-5)/creeper charge 层、精确视觉人工目视。细节见 `docs/spec/entity-subsystem.md` §12，见平行表 CR-33。
- **2026-07-22**：**Gate E1 ✅ 渲染注册框架（Stage E1 / PE-1，两节点 `runClient` 实证）**。新 `platform/EntityRendererCompat`（`EntityRenderersEvent` forge↔neo 仅 import 分叉、中性 `Renderers`/`Layers` sink）+ `registry/ModModelLayers`（`PLACEHOLDER` 层）+ `client/render/entity/ACPlaceholderRenderer`（cube 占位）+ `client/render/ACEntityRenderers`（遍历 `BuiltInRegistries.ENTITY_TYPE` 按 namespace 给**全 54 实体**注册占位渲染器）+ 主类 `runWhenClient` `attach`（无 relay/lang 改）。此前 54 实体缺渲染器 → `runClient` `EntityRenderers.validateRegistrations` 硬崩；E1 全注册解之。业务零 `//?`（分叉仅 compat import；`ACPlaceholderRenderer` 用版本稳定渲染 API；坑：`PartPose` 在 `client.model.geom` 非 `.builders`）。两节点 `compileJava` BUILD SUCCESSFUL + `runClient`：**0 missing-renderer / 0 crash**、各抵标题屏（ResourceManager 过验证 → OpenAL/Sound engine → atlases → title）。解了 D2a/D2b 全程「缺渲染器 `runClient` 崩」。faithful 每族/护甲/BE 渲染 → E2(PE-2..6)；占位视觉需人工目视。**解锁 Stage E2**。细节见 `docs/spec/entity-subsystem.md`，见平行表 CR-32。
- **2026-07-22**：**Gate D2（= D2b）✅ 已过（收口 D 阶段，两节点实证）**。PD-7 交付 12 BOSS 后，D2a 全族(42) + D2b BOSS(12) = **54 EntityType 全落地**；两节点 `compileJava`+`runServer` 全绿，所有实体 `/summon` 成活、4 boss 血条 `customServerAiStep` tick 零 exception + 干净 stop。**解锁 Stage E1（PE-1 渲染框架）**。见平行表 Gate D2。
- **2026-07-22**：**PD-7（Stage D2b）BOSS 家族（Agent 16，两节点 `runServer` 全绿）**。12 BOSS EntityType（4 血条 boss chagaroth/jzahar/shadowboss=Sacthoth/dragonboss + 8 elite/minion dreadguard/gskeleton/remnant/shuboffspring/jzaharminion/chagarothfist/chagarothspawn/dragonminion）。**新 boss-bar 框架** `content/entity/boss/ACBossMob extends ACMob`（vanilla `ServerBossEvent`：`customServerAiStep`→setProgress + 三段变色、`start/stopSeenByPlayer`→add/removePlayer；javap 双 jar 核 fork-free）。enum 塌缩（同 PD-4）：`BossKind`(4)/`BossMob` + `EliteKind`(8)/`EliteMob`——忠实 1.12.2 属性 + 标准敌对 goal + 12 刷怪蛋（`SpawnEggCompat`）。接 `ModRegistries.ALL`(+2) + en_us(+24)、无主类改动、boss 非自然刷怪无 SpawnPlacement。两节点 `compileJava`+`runServer`：12 boss 各 `/summon` 成活（全显 lang 名）+ 属性精确（Chagaroth `1000.0f` 双端/Jzahar 500/Dreadguard 60）+ 4 血条 boss `customServerAiStep` 逐 tick setProgress/变色**零 exception**+ 干净 stop。延后：boss 招牌多阶段技能/dragon 多部件飞行/Remnant 商人交易+剪羊毛/boss 战利品（依赖未移植 config/API/维度/药水/物品）、血条视觉 + 全实体渲染（Stage E）。细节见 `docs/spec/entity-subsystem.md`，见平行表 CR-31。
- **2026-07-22**：**Gate D2a ✅ 已过（两节点集成实证）**。PD-3..6 四族全 ☑（anti 11 / demon-evil 8 / ghoul 5 + shoggoth 3 / misc 10 + projectile 5 = **42 EntityType**）。**两节点 `compileJava` BUILD SUCCESSFUL**——PD-5 修好 `platform/SpawnPlacementCompat` 的 `SpawnPlacements.Type`→1.21 `SpawnPlacementTypes` 分叉、**解阻此前 PD-3/PD-4 的 neo 编译**。**两节点 `runServer` 集成实证**（unique 端口避并发）：forge `Done 2.376s` / neo `Done 1.188s`（`Loaded 1322 recipes`），跨族 `/summon` 各 `Summoned new X`（Ghoul/Shoggoth/Demon Cow/Black Hole/Anti-Ghoul 两端）+ 属性精确（Ghoul `30.0f` / Anti-Ghoul `45.0f` 双端）+ 干净 stop + BUILD SUCCESSFUL、**全程零 exception/stacktrace**。延后至后续 Gate：自然刷怪 biome_modifier 群系赋值（Darklands G2 / T3.9）、neo 刷怪双份（PL-4）、招牌特殊 AI/爆炸/变身/potion（未移植 config/API/方块/维度）、渲染 E、蛋模型 PK。**解锁 Stage D2b（PD-7 BOSS）**。见平行表 Gate D2a。
- **2026-07-22**：**PD-6（Stage D2a）misc + projectile 家族（Agent 15，两节点 `runServer` 全绿）**。15 非生物 EntityType（5 projectile + 10 misc），非 LivingEntity → 无属性/loot/蛋。**2 platform fork-base 吸收 `Entity.defineSynchedData` 分叉**（forge `()` ↔ neo `(SynchedEntityData.Builder)` abstract）：`platform/ACSimpleEntity extends Entity` + `platform/ACThrowableProjectile extends ThrowableProjectile`。5 projectile：`AcidProjectile`/`DreadSlug`/`InkProjectile`(extends ACThrowableProjectile，onHitEntity→thrown 6/6/2)、`CoraliumArrow`(extends 具体 `Arrow` 避 `AbstractArrow` abstract pickup 分叉)、`DreadedCharge`(extends `AbstractHurtingProjectile`)。10 misc(8 类，ODB×2 / Portal×2 塌缩)：`BlackHole`/`Implosion`(引力拉拽)、`PrimedODB`(fuse→`level.explode`→discard)、`CompassTentacle`(忠实)、`PSDLTracker`/`DimensionPortal`/`SpiritItem`/`GatekeeperEssence`(占位)。2 registrar `ProjectileEntities`+`MiscEntities`(`MobCategory.MISC`、忠实 1.12.2 id)。接 `ModRegistries.ALL`(+2)+en_us(+15)、无主类改动。业务零 `//?`（javap 双 jar 核 4 base ctor + onHit/onHitEntity + `DamageSources.thrown` + `Level.explode(...,ExplosionInteraction)` 双端同；避 `AbstractArrow` pickup + `MobEffectInstance` holder 分叉）。两节点 `compileJava`+`runServer`：15 各 `/summon`→`Summoned new X`（lang 解析、零异常）+ primedodbcore fuse→explode→discard 双端实证 + blackhole tick 实证 + 干净 stop。PD-5 修好 `SpawnPlacements.Type` bug → neo 不再受阻、双端运行期齐证。延后：自定义伤害源/potion（药水系统）、酸蚀方块/群系转化/hardcore（ACConfig/ACBlocks/ACBiomes）、Jzahar 关联（PD-7）、维度传送/spirit 路径/essence 物品（各未移植系统）、渲染 E。细节见 `docs/spec/entity-subsystem.md`，见平行表 CR-30。
- **2026-07-22**：**PD-5（Stage D2a）ghoul + shoggoth 家族（两节点 `runServer` 全绿）**。5 ghoul（ghoul/depths/dreaded/omothol/shadow，`AbstractGhoul` extends PD-1 `ACMob`）+ 3 shoggoth（lesser/shoggoth/greater，`AbstractShoggoth`：PD-2 `ACWallClimberNavigation` 爬墙+击退抗性+水滑行）+ 各族 registrar（8 `EntityType`+8 刷怪蛋经 PD-3 `SpawnEggCompat`+属性经 PD-1 `EntityAttributeCompat`+`SpawnPlacementCompat` 地面刷怪放置）。**新增 `platform/SpawnPlacementCompat`**（forge `SpawnPlacementRegisterEvent`↔neo `RegisterSpawnPlacementsEvent`，`SpawnPlacements.Type`→1.21 `SpawnPlacementTypes` 常量分叉封兼容层、业务经 `registerGroundMonster` 零分叉；主类挂一行 `attach`）——**并修好此前阻断 PD-3/PD-4 neo 编译的 `SpawnPlacements.Type` bug（neo 现两端全绿）**。跨主：PB-2 `MiscItems` +5 shoggoth flesh（1.12.2 非食用 `ItemACBasic`、PB-2 漏移、shoggoth loot 需之）。接 `ModRegistries.ALL`(+4)+en_us(+21)+16 战利品双目录（flesh `set_count` 0-2、避 1.21 改名 `looting_enchant`、默认表 id 自动派生）+2 forge biome_modifier（`spawn_ghoul` ghoul→plains/desert/taiga/savanna 忠实、`spawn_shoggoth` lesser→`#minecraft:is_overworld` 占位待 AC 群系 G）。omothol on-hit slow/blind/nightvis（vanilla effect fork-free）+火免/毒免；蛋色取自 1.12.2 `registerEntityWithEgg`。业务零 `//?`（`MobEffectInstance`/`isPushedByFluid`/`onClimbable`/`doHurtTarget`/`createNavigation`/`canBeAffected` javap 双 jar 核 fork-free）。两节点 `compileJava --rerun-tasks`+`runServer`：8 实体各 `/summon` 成活（无属性即崩→成活即证）+ ghoul `attribute max_health base get`=30.0（值精确）+ `/loot spawn abyssalcraft:entities/{ghoul,shoggoth}` 双端掉 Ghoul/Shoggoth Flesh（运行期解析+双目录路径两端通）+ 干净 stop（forge Done 3.280s/neo 1.028s）。延后：水下呼吸（`canBreatheUnderwater` 1.21 final）、coralium/dread plague on-hit（药水 MobEffect）、shoggoth 招牌 AI（酸击/喷酸 `EntityAcidProjectile`/建巨石 `WorldGenShoggothMonolith`→G/ooze/进食/多部件/膜拜——依赖未移植、PD-2 框架预留）、5 TYPE 变体、音效/粒子、硬核/精英（config）、depths `dghead` 稀有掉落、维度族自然刷怪（AC 群系→G）、neo 自然刷怪（PL-4）、渲染 E。细节见 `docs/spec/entity-subsystem.md`，见平行表 CR-29。
- **2026-07-22**：**PD-3（Stage D2a）anti 反物质族（forge `runServer` 实证 / neo 待 PD-5 解阻）**。11 anti 实体：8 直 extends vanilla（`Zombie`/`Creeper`/`Skeleton`/`Spider`/`Cow`/`Pig`/`Chicken`/`Bat` 白拿正确行为）+ `AntiGhoul`/`AntiPlayer`(无 vanilla 等价) extends PD-1 `ACMob`+显式 goals（1.12.2 把 vanilla AI 抄进 `EntityMobBase`；现代直承 vanilla 更忠实+省重造）。`content/entity/anti/{AntiEntity(marker),11 实体,AntiEntities(registrar)}` + 新 `platform/SpawnEggCompat`（forge `ForgeSpawnEggItem`↔neo `DeferredSpawnEggItem` 类名分叉，供 D 家族刷怪蛋复用）；属性经 PD-1 `EntityAttributeCompat`（复用 attach、无主类改动）。接 `ModRegistries.ALL`(+2) + en_us(+22) + 9 战利品表双目录（`set_count` uniform、默认表 id 自动派生）。业务零 `//?`（分叉全在 `SpawnEggCompat`）。forge `compileJava`+`runServer` 11 anti 各 `/summon` 成活（全显 lang 名，Anti-Ghoul `45.0f`/Anti-Creeper `30.0f`）+ 干净 stop 零异常。neo：我 13+1 文件零错（报错 100% 在并发 PD-5 `platform/SpawnPlacementCompat` 的 `SpawnPlacements.Type`→1.21 `SpawnPlacementType` bug）、`DeferredSpawnEggItem` 一手核在 neoforge-21.1.193.jar、全绿待 PD-5 修其文件。延后：湮灭爆炸(config+`ExplosionUtil`)/刷怪放置+biome(PD-5+G2/T3.9)/蛋模型(PK)/`getBreedOffspring`(现繁殖出 vanilla)/AntiGhoul→OmotholGhoul 变身(PD-5)/渲染 E。细节见 `docs/spec/entity-subsystem.md`，见平行表 CR-28。
- **2026-07-22**：**PD-4（Stage D2a）demon/evil 农场动物族（Agent 14，forge `runServer` 实证 / neo 编译绿）**。4 demon + 4 evil 敌对农场动物（chicken/cow/pig/sheep 各一对）。**10 类塌缩 2 类 + 1 enum**：`content/entity/demon/{AnimalKind,DemonAnimal,EvilAnimal}`（extends PD-1 `ACMob`，kind 烘进 EntityType 工厂 lambda `(t,l)->new DemonAnimal(t,l,kind)`）+ registrar `DemonEntities`（`EnumMap<AnimalKind,Supplier<EntityType>>` 供替身查型、`.fireImmune()`、`static{}` 登记 8 属性；chicken 10HP·cow/pig 15HP·sheep 12HP）。`EvilAnimal.die()` 死亡时同 kind demon 就地替身——`create(Level)`+`moveTo`+`addFreshEntity`（**fork-free 避 `finalizeSpawn` 分叉**）；AI 复用 PD-1/vanilla。接 `ModRegistries.ALL`(+1 `DemonEntities.ENTITIES`) + en_us(+8 `entity.abyssalcraft.{demon,evil}_{chicken,cow,pig,sheep}`) + 16 战利品表（`{loot_table,loot_tables}/entities/*.json` 双目录同容：demon→rotten_flesh 0-2、evil→vanilla 农产、默认表 id 自动派生免 override）。业务零 `//?`（javap 双 jar 核 `EntityType.create(Level)`/`Mob.convertTo` 同签名）。forge `runServer` 8 实体各 `/summon` 成活（属性链通、lang 解析）+ evil→demon 替身实证（kill demon_cow 清场 → kill evil_cow → `execute if entity @e[type=…demon_cow] run say` 命中）+ 干净 stop + BUILD SUCCESSFUL；neo 4 `.class` 已编入 build output（一手核）+ 早前双端 `compileJava` 绿、本次 neo `runServer` 被并发 PD-3 `platform/SpawnPlacementCompat` 未修 neo 分支（`SpawnPlacements.Type`→1.21 `SpawnPlacementType`）暂阻、PD-4 码不在报错集（瞬态并发中断）。刷怪蛋 + 群系自然刷怪延 T3.9/PL-4；剪毛/火焰蔓延/硬核穿甲/新月限制/evil-sheep 网络依赖未移植 config/API 延后；渲染 E。见平行表 CR-27。
- **2026-07-22**：**PD-1（Stage D1）实体注册框架 + 基类（两节点）**。`registry/ModEntities`（`ModRegistrar<EntityType>` + `pilot_mob` 示例 = `ACMob` 直注册同 PC-1 block-less smoke test）+ 新 `platform/EntityAttributeCompat`（mod-bus `EntityAttributeCreationEvent` 的 forge↔neo import fork，`register`/`attach` 分离供 D2a 各族登记属性）+ `content/entity/base/ACMob`（忠实承 1.12.2 `common.entity.base.EntityMobBase`=`extends EntityMob`→`Monster`，`createAttributes()` 默认 vanilla monster 模板供子类覆写；自定义导航→PD-2、硬核伤害依赖未移植 config 延后）。接 `ModRegistries.ALL`(+1) + 主类 `attach`(+1) + en_us(+1 `entity.abyssalcraft.pilot_mob`)。业务零 `//?`（分叉全在 `EntityAttributeCompat`；编译核 `EntityType.Builder.<T>of(f,MobCategory).sized(w,h).build(String)` 1.20.1↔1.21.1 同签名、注册本体 fork-free 无需 `EntityCompat`）。两节点 `compileJava` BUILD SUCCESSFUL + forge `runServer`：`summon abyssalcraft:pilot_mob {PersistenceRequired:1b,NoGravity:1b}`→`Summoned`、`data get ... Health`→`20.0f`（无属性 summon 当场崩→成功即证属性链通）、持久存活零异常、干净 stop。踩坑：敌对 `Monster` 无 128 格内玩家即刻 despawn（验成活须 `PersistenceRequired`）；无渲染器 `runClient` 启动 `EntityRenderers.validateRegistrations` 崩（Stage E 前只 `runServer` 验）。刷怪放置注册（`SpawnPlacements` forge/neo 事件双分叉）延 D2a + T3.9、具体实体族 D2a/D2b、渲染 E。细节见 `docs/spec/entity-subsystem.md`，见平行表 CR-26。
- **2026-07-22**：**PD-2（Stage D1）AI goals + pathfinding（Agent 13，两节点）**。可复用 AI/寻路框架（解耦具体实体、用 vanilla `Mob`/`PathfinderMob`）。一手比对 1.12.2 的 6 AI + 2 pathfinding → 多被现代 vanilla 吸收；交付 2 个无 vanilla 等价的自定义可复用 Goal：`content/entity/ai/WorshipGoal`（膜拜）、`content/entity/ai/{SwellGoal<T extends Mob & SwellingMob>,SwellingMob}`（膨胀，解耦 vanilla `SwellGoal` 的 `Creeper` 约束）+ 2 canonical 导航 `content/entity/pathfinding/{ACGroundPathNavigation,ACWallClimberNavigation}`（薄扩展点，1.12.2 spinning patch 现 vanilla-native）。远程→vanilla `RangedBowAttackGoal`、近战 reach→vanilla（现代 `Mob.getMeleeAttackRangeSqr` 已 width-based）文档化不重造；Shoggoth acid-melee/建碑延 D2a。业务零 `//?`（避开 `MeleeAttackGoal.getAttackReachSqr`(1.20.1)↔`canPerformAttack`(1.21) fork）。两节点 `compileJava --rerun-tasks` 绿（framework 仅编译证明；泛型签名证可复用）。无 CR（仅新增自有 dir）。见平行表 PD-2。
- **2026-07-21**：**PC-9（Stage C2b）机器配方数据 datagen（Agent 12，两节点）**。新 `data/gen/MachineRecipeData`（`DataProvider`，接 `ACDataGenerators`）生成 3 合成机器 27 条示例配方（crystallization 7 / transmutation 11 / materialization 9，`ProcessingRecipe` 形状、忠实 1.12.2 `AbyssalCrafting` 裁剪现存物品、全用一手核对 id）。业务零 `//?`（`DataProvider.run`/`saveStable(...,JsonElement,Path)` 双端同签名）。坑：自定义配方 datagen 1.20↔1.21 目录（`recipes/`↔`recipe/`）+ result key（`"item"`↔`"id"`）双分叉 + 两 loader 共享 `src/main/generated` 致跨 loader HashCache clobber → 每 runData 生成两目录两格式、各读自己目录。neo `runServer` `Loaded 1322 recipes`（1295+27）零 parse 错加载；forge `runServer` `Loaded 12`（27 在 build/resources→jar/生产正常，forge-dev runServer 不挂 `src/main/generated` = PC-2 已记限制）。细节见 `docs/spec/machine-subsystem.md`，见平行表 CR-25。
- **2026-07-21**：**PC-8（Stage C2a）研究桌 + 顺序酿造台（Agent 6，两节点）**。2 机器复用 PC-1 BE 框架 + PC-3 菜单 + MP 模板（业务零 `//?`）。研究桌 = 空壳 GUI（`ResearchTableBlockEntity` extends `ACBlockEntity`，1.12.2 原 TE 即空、知识 hook 延 S-B）。顺序酿造台 = PC-1 `InventoryBlockEntity`(8 槽)+`TickingBlockEntity`，vanilla 酿造 + 每 20t 传出槽→facing 邻居输入槽（**酿造顺序链**）；`FACING` blockstate 块。新 `platform/PotionBrewingCompat`（1.20.1 静态 `PotionBrewing.isIngredient/hasMix/mix` ↔ 1.21 `level.potionBrewing()` 实例，编译双端核）。接 `ModRegistries.ALL`(8 registrar) + `ACClientSetup`(2 屏幕) + en_us 4 键 + 6 vanilla 占位资产。两节点 `compileJava`+`runServer`（BLOCK/BE/MENU 冻结零错、抵 Done forge 2.330s/neo 1.037s）+ forge `runClient`（2 机器模型烘焙零告警、屏幕注册、进世界 JEI 1896、干净退出）。PC-5..7 三合成机器随框架泛化一并回归全绿、零改动。右键开界面 + 酿造链需人工目视；faithful 贴图/药水进度/槽校验延 PK。细节见 `docs/spec/machine-subsystem.md`，见平行表 CR-24。
- **2026-07-21**：**PC-2（Stage C1）自定义配方框架（Agent 11，两节点）**。T2.2 的 5 类自定义配方：crystallization/materialization/transmutation 由 MP 试点机器自持（简化 `ProcessingRecipe`、frozen），本任务补 `anvil_forging`（2 输入+`price`+`forging_type`，仿 1.12.2 `api.recipe.AnvilForging`）+ `rending`（`name`+`max_energy`+`entity`→essence，仿 `api.rending.Rending`）2 类真实形状。新 `registry/ModRecipes`（catalog：owns anvil/rending + alias 3 试点类型）+ `content/recipe/{anvil,rending}/*`（fork-free）+ `platform/{DataRecipeCompat（抽象 `Recipe<Container↔RecipeInput>` fork base）,AnvilForgingRecipeSerializer,RendingRecipeSerializer（`fromJson`/`fromNetwork`/`toNetwork`↔`MapCodec`/`StreamCodec` 分叉，javap 双 jar 核 `ByteBufCodecs.STRING_UTF8`+`StreamCodec.composite`）}`。4 示例 JSON 双目录双格式（forge `recipes/` 用 `"item"` / neo `recipe/` 用 `"id"`）。接 `ModRegistries.ALL`；业务零 `//?`。两节点 `compileJava --rerun-tasks`+`runServer`：forge `Loaded 12 recipes`（可复现）+ `/reload` 零 parse 错、neo `Loaded 1295 recipes`，anvil/rending 两端零 parse 错加载。forge 12 vs neo 1295 = vanilla 配方在两 dev 数据包可用性的可复现差异（与 PC-2 无关、与 CR-18 记 forge 1293 不符）；3 试点类型仍机器自持、集中化+完整形状升级延 C2a。见平行表 CR-23。
- **2026-07-21**：**PC-4（Stage C1）物品转移能力（Agent 3，两节点）**。新增 `system/transfer/{ItemTransfer(转移引擎),ContainerItemView(vanilla `Container`→中性 `ItemView` 适配),ItemTransferConfiguration(1.12.2 同名路由+5槽过滤数据面),ItemTransferHost(1.12.2 `IItemTransferCapability` 现代化为 BE 实现接口，`instanceof` 查询免自定义能力注册机制)}`；增强 `platform/CapabilityAccess` 加中性 `ItemView`/`itemView()`（隐藏 forked `IItemHandler`）+ `platform/ContainerCompat` 加 `canStack()`（`isSameItemSameTags`↔`isSameItemSameComponents`）。业务零 `//?`（forked `IItemHandler`/item-stacking 全在 platform）。两节点 `compileJava`+`runData`（主类临时自测已还原，同 PA-3 先例）：`ItemTransfer self-test moved=40 src=24 dst=40 PASS` 双端一致——转移逻辑运行期实证。state transformer / rending pedestal / spirit altar 宿主方块延 C2/S（复用框架）。见平行表 CR-22。
- **2026-07-21**：**PC-1（Stage C1）BlockEntity 框架（Agent 6，两节点）**。泛化 PP-1 机器 BE 为通用 BE 框架（PP-1 `MachineBlockEntity` 原样保留）。新增 `content/blockentity/base/{ACBlockEntity(通用基类 + `markUpdated`),DirectionalBlockEntity(= 1.12.2 `TEDirectional`，存 facing),InventoryBlockEntity(可复用 `Container`，size=1 = `ISingletonInventory` 单例库存 / size>1 = 箱笼),TickingBlockEntity(= `ITickable`，`serverTicker()` 泛化 `MachineBlockEntity::serverTick`)}` + `registry/ModBlockEntities`（BE 类型注册器 + directional/inventory 两 block-less smoke 类型）。业务零 `//?`（版本分叉全在 `BlockEntityCompat`/`ContainerCompat`）。接 `ModRegistries.ALL`。两节点 `compileJava --rerun-tasks`+`runServer` 绿（`BLOCK_ENTITY_TYPE` 冻结零错、抵 Done，forge 2.342s/neo 0.841s；neo 首跑撞并发 agent 25565 端口 `BindException` → 换 25580 重跑抵 Done，与代码无关）。tick 机制 compile 核 + 由 P2 机器运行期实证；非机器 BE 消费延 C2/S。细节见 `docs/spec/machine-subsystem.md`，见平行表 CR-21。
- **2026-07-21**：**PC-3（Stage C1）菜单框架（Agent 3，两节点）**。新 `registry/ModMenus`（canonical MenuType 注册器）+ `content/menu/base/{ContainerMenuBase(可复用抽象基类:玩家背包布局+通用 shift-click),ItemContainerMenu(变长物品容器示例 = crystal bag/spirit tablet/spellbook 共享基类)}`；PP-1 `MachineMenu`/`MachineMenus` 原样保留（机器专用）。泛化 `platform/MenuCompat.open` 加 `Consumer<FriendlyByteBuf>` 任意 buffer-writer 重载（物品容器传 size；neoforge 1.21 `RegistryFriendlyByteBuf` 经 `::accept` 适配；BlockPos 重载委托、3 机器不受影响）。接 `ModRegistries.ALL`。两节点 `compileJava`+`runData`（RegisterEvent 注册 `item_container` MenuType 零错、干净退出）；实际开屏 via C2 消费者、屏幕延 M6。见平行表 CR-20。
- **2026-07-21**：**Gate B（Stage B2 集成）创造栏填充（Agent 6，两节点）**。全部 M1 内容按 1.12.2 `ACTabs` 忠实还原为 **7 创造栏分类页**（blocks/items/tools/combat/food/decorations/crystals），全在 relay `registry/ModCreativeTabs`，vanilla fork-free `displayItems` 拉各内容 list。增强 `platform/ModRegistrar` 加 `entries()`（供 relay 遍历填页，免各内容文件各暴露 list）；剑靠 `_sword` 后缀分入战斗页；`acceptAll` 加 `asItem()!=AIR` 守卫防崩。删 PB-1/PB-4 自带 `materials`/`ores` 页并入集中 7 页；`MaterialItems` 加 `BASICS`。`ModRegistries.ALL` 删 2 tab 行；en_us 加 7 页标题键。两节点 `compileJava --rerun-tasks`+`runData` 绿 + 两节点 `runClient` 进世界：JEI 成分表 forge 1896/neo 2026 含全部 AC 内容、无 AC 页被标空（仅 vanilla Operator Utilities 空）、无 `displayItems` 错、干净退出。创造栏视觉分组/图标需人工目视。细节见 `docs/spec/item-content.md`，见平行表 CR-19。
- **2026-07-21**：**PB-4（Stage B2）矿石方块（Agent 6，两节点）**。`content/block/ore/OreBlocks`注册13矿与BlockItem，硬度/抗性取自旧`BlockHandler`。当时基础loot/tag留下的tier与实际挖掘余量已于2026-07-24由RR-DATA以单一provider、双端117项tier矩阵和真实`Block.getDrops`矩阵收口。模型layered overlay仍归T9.2b。细节见`docs/spec/item-content.md` §5e/§5f，历史CR见平行表CR-18。
- **2026-07-21**：**PB-8（Stage B2）建材方块分叉构造变体（Agent 10，两节点）**。串行接 PB-3，**16 块 = button×5 + pressure_plate×5 + sapling×2 + door×2 + fence_gate×2**（石家族用 `BlockSetType.STONE`、木家族用 `OAK`）。新增 `platform/BlockFactory` 承 5 类 vanilla ctor 的 1.20.1↔1.21 分叉（`ButtonBlock`/`PressurePlateBlock`(`Sensitivity` 1.21 删)/`DoorBlock`/`FenceGateBlock` 参序、`SaplingBlock` `OakTreeGrower()`↔`TreeGrower.OAK`；javap 双 jar 逐一核，业务零 `//?`）。顺序扩 PB-3 冻结的 `BaseBlocks`/`BaseBlockData`/`BlockModelGen`（未改既有行）；`BaseBlocks.BLOCKS/ITEMS` 已在 `ModRegistries.ALL` → 本任务零注册改动。8 张专属贴图（6 门 + 2 sapling）从 1.12 资产改名迁入 + en_us 16 键。两节点 `compileJava --rerun-tasks`+`runData`（`ExistingFileHelper` 严格校验贴图存在）+`runServer`（注册冻结零错，forge 2.304s/neo 1.153s，干净 stop）。sapling grower 占位 vanilla oak 待 G、dlts/drts sapling 贴图 best-guess 待 PK；放置/门开合/plate 触发目视 + 模型精修延 PK。细节见 `docs/spec/item-content.md`，见平行表 CR-17。
- **2026-07-21**：**PB-7（Stage B2）护甲（Agent 3，两节点）**。`content/item/armor/ArmorItems`——**7 护甲材料 × {helmet/chestplate/leggings/boots} = 28 件**（abyssalnite/refined_coralium/plated_coralium/depths/dreadium/dreadium_samurai/ethaxium）；护甲值/耐久/附魔/韧性取自 1.12.2 `AbyssalCraftAPI`（defense `{boots,legs,chest,helmet}` 槽序），修理料只读 PB-1 锭/板（`registry.get(id)` lazy）。新增 `platform/ArmorCompat` 承 1.21 护甲大改（`ArmorMaterial` 接口→record+`Holder`、耐久搬到物品 `Properties`；1.20.1 匿名接口 / 1.21 record+`Holder.direct`+空 layer），业务零 `//?`。两节点 `compileJava`+`runData`（mod 构造逐一 build 28 件、RegisterEvent 零错、干净退出）。穿戴/数值目视 + 物品模型/贴图延 PK、穿戴层延 E 渲染、lang 延 PK-4。见平行表 CR-16。
- **2026-07-21**：**PB-6（Stage B2）工具（Agent 9，两节点）**。`content/item/tool/ToolItems`——20 工具 = 4 tier（abyssalnite/refined_coralium/dreadium/ethaxium）× {pickaxe,axe,shovel,hoe,sword}；tier 数值（uses/speed/attack/enchant/harvest level）取自 1.12.2 `AbyssalCraftAPI`，修理料只读 PB-1 锭（`registry.get(id)` lazy）。新增 `platform/ToolCompat` 承 1.21 工具大改（`Tier.getLevel()`↔`getIncorrectBlocksForDrops()` tag；工具 ctor `(Tier,dmg,speed,Props)`↔`(Tier,Props.attributes(createAttributes(...)))`，javap 双 jar 核），业务零 `//?`。两节点 `compileJava`+`runServer` 绿（`ITEM` 冻结零工具错，forge 2.522s/neo 0.922s）。特殊工具延 M7、贴图延 PK。**如实标注**：neoforge `runServer` 另有 pre-existing 报错（committed 矿石 loot/tag 引用 **PB-4 未注册矿石**，非致命、非 PB-6，待 PB-4 协调）。细节见 `docs/spec/item-content.md`，见平行表 CR-15。
- **2026-07-21**：**PB-3（Stage B1）建材方块族（Agent 3，两节点）**。`registry/BaseBlocks` + `data/gen/BaseBlockData` + 新增 forked `platform/BlockModelGen`（`BlockStateProvider` 兼容层）——**86 块 / 11 家族**（darkstone/abyssal/dreadstone/elysian/coralium/ethaxium/dark_ethaxium/omothol/monolith 石系 + darklands_oak/dreadwood 木系）datagen 铺 blockstate/方块模型/物品模型；49 贴图按 1.12.2 老 model JSON 映射复制（6 个 layered 砖 base+overlay System.Drawing 合成）。**基建修复**：`build.gradle.kts` 接 `src/main/generated`→resources（PA-4 datagen 产物原为死文件）+ `platform/DataGenCompat.Gen` 增 `packOutput`/`existingFiles`。方块只用具体 vanilla 类避 1.21 抽象 `codec()`。两节点 `compileJava`+`runData`+`runClient` 全绿、模型烘焙对本任务块零告警。建材砖块改复数 `ethaxium_bricks` 避与 PB-1 合成物品 `ethaxium_brick` 撞 `ITEM` 注册名。剩余分叉构造变体（button/pressure_plate/sapling/door/fence_gate，需 `platform/BlockFactory`）挪至 **PB-8**、掉落/配方/标签挪至 **PK-5**。细节见平行表 CR-12。
- **2026-07-21**：**PB-1（Stage B1）材料/晶体物品（Agent 6，两节点）**。`content/item/material/MaterialItems`——117 物品 = 39 基础材料 + 78 元素晶体（26 元素 × crystal/shard/fragment，灰度贴图按元素 `ItemColor` 染色）；1.12.2 真实贴图按原模型 `layer0` 映射复制。新增 `platform/ClientColorCompat`（`RegisterColorHandlersEvent.Item`，javap 双端核 `register` 同签名、仅事件包分叉）+ 自带 `materials` 创造页（vanilla `displayItems` 零分叉）。手写 117 模型 + 48 贴图 + 118 lang。接 `ModRegistries.ALL`（ITEMS+TABS）。两节点 `build`+`runClient` 117 模型/贴图零告警、颜色注册零错、抵标题屏（曾被并发 PB-5 缺 1.21 `codec()` 短暂卡 neoforge，修后两端全绿）。datagen/配方/标签延 PK/T1.9。细节见 `docs/spec/materials-subsystem.md`，见平行表 CR-14。
- **2026-07-21**：**PB-5（Stage B1）装饰/素功能方块（Agent 8，两节点）**。`content/block/deco/{DecoBlocks,DecoFacingBlock}`——29 块 + 各 BlockItem（17 FACING 装饰雕像/mural/墓碑 + 10 素块 + 2 植物）。`DecoFacingBlock extends Block` 复用 `HorizontalDirectionalBlock.FACING`，规避 **1.21 抽象 `codec()`**（`HorizontalDirectionalBlock`/`BushBlock` 在 1.21 把 codec() 重声明 abstract → 直接子类编译失败；解了平行表 CR-12 记录的 neoforge 阻塞）。接 `ModRegistries.ALL` + en_us 29 键。两节点 `compileJava --rerun-tasks`+`runServer` 绿（`BLOCK`+`ITEM` 冻结零错，forge 4.460s/neo 1.660s，干净 stop）。模型/贴图延 PK；monolith_pillar(PE)/召唤 statue(仪式)/crystal cluster(晶体) 延各系统。细节见 `docs/spec/item-content.md`，见平行表 CR-13。
- **2026-07-21**：**PB-2（Stage B1）食物/杂项物品（Agent 7，两节点）**。`content/item/misc/MiscItems`——15 食物 + 9 杂项 shell（clean snake_case id）；食物 nutrition/saturation 取自 1.12.2 `ItemHandler`。新增 `platform/FoodCompat` 承 `FoodProperties.Builder` 的 `saturationMod`↔`saturationModifier` 跨版本分叉（javap 双 jar 核，业务零 `//?`）。接 `ModRegistries.ALL` + en_us 24 键。两节点 `compileJava --rerun-tasks`+`runServer` 绿（`ITEM` 注册冻结零错，forge 2.954s/neo 1.050s，干净 stop）。**进食效果延 T7.10、贴图/模型延 PK**（客户端图标/实际进食需人工目视）。细节见 `docs/spec/item-content.md`，见平行表 CR-11。
- **2026-07-21**：**PP-5（Stage P3）JEI 试点（Agent 6）**。`integration/jei/{ACJEIPlugin,MachineRecipeCategory}`——三机器 JEI 分类 + 方块催化剂 + pilot_fuel 信息页，业务零 `//?`（javap 双 jar 核 JEI 15/19 所用 API 同签名，唯一版本分叉落 `platform/RecipeCompat.allOfType`）。JEI = `modCompileOnly`(API) + `modLocalRuntime`(dev 全 jar，不入发布)，`@JeiPlugin` 自动发现 → 缺 JEI 不崩（grep 核无外部引用 / 无 mods.toml 硬依赖）。两节点 `build`+`runClient` 加载 JEI（forge 15.20.0.134 / neo 19.39.0.369）+ 插件实例化零错、抵标题屏。进世界展示待人工目视；Gate MP 构建/加载层过。细节见 `docs/spec/machine-subsystem.md`，见平行表 CR-10。
- **2026-07-21**：**PP-3 接手 / PP-4 补完（Agent 6）**。接手「另一 agent 机器出问题·疑似任务冲突」排查：定位 **PP-4 Transmutator 缺 3 个客户端资产**（blockstate/block model/item model）——Agent 3（CR-8）标 ☑ 但仅 `runServer` 验、`runClient` 留 P3 → 缺资产未察觉、运行期紫黑缺失模型。补齐三资产（镜像 materializer，`cube_all` + `minecraft:block/smithing_table_top`）；两节点 `build`+`runClient` 核三机器模型烘焙零告警、干净退出（补 Agent 3 推迟的可视验证）。复核 **PP-3 Materializer** 竖切成立（PP-3 由 ☐ 改 ☑）。仅新增 3 个 json 资产、业务零改动。细节见 `docs/spec/machine-subsystem.md`，见平行表 CR-9。
- **2026-07-21**：**PP-2（Stage P2）Crystallizer 竖切（Agent 6）**。结晶器 方块/BE/菜单/屏幕 + `crystallization` 配方，镜像 Materializer 复用 PP-1 框架+兼容层（业务零 `//?`）。两节点 `build`+`runServer`+`runClient` 绿（注册/配方解析 forge 9·neo 1292 / 模型 / 屏幕零错、抵标题屏）。补 PP-1 `pilot_crystal`/`pilot_fuel` item model；旁修 PP-4 `TransmutatorRegistry` 菜单自引用编译 bug（1 行）。细节见 `docs/spec/machine-subsystem.md`，见平行表 CR-7。
- **2026-07-21**：**PP-1（Stage P1）试点机器框架 + Gate P1（Agent 2，两节点）**。交付 BE/菜单/配方 base（`content/blockentity/base/MachineBlockEntity`、`content/menu/base/MachineMenu`、`content/recipe/base/ProcessingRecipe` + 各 registrar）+ 试点晶体物品（`content/item/crystal/CrystalItems`）。新增兼容层 `ContainerCompat`/`RecipeCompat`/`RecipeSerializerCompat` 并增强 `BlockEntityCompat`（save/load 加 `HolderLookup.Provider`），吸收 1.21 Recipe/RecipeSerializer/ItemStack-NBT 大改（recipe 两版签名先 `javap` 核 MC jar 后写）；业务 `content/` 与 registrar 零 `//?`。5 registrar 接入 `ModRegistries.ALL`。两节点 `build` 绿 + `runServer`→`Done`（forge 2.340s / neo 0.970s）注册表冻结无错。见平行表 CR-6。限制：试点无机器方块（P2 提供）→ BE/菜单/tick 运行期未被调用（仅编译+注册证明）；无资产。
- **2026-07-21**：**PA-5（Agent 5）最小竖切 + Gate A4（M0 冻结，两节点）**。新增 `content/demo/DemoBlock`（vanilla `Block` 子类）+ `registry/DemoRegistrar`（demo 方块/方块物品/物品 + 专属创造页 `abyssalcraft:demo`，全 vanilla API 零 `//?`）+ 手写资产（blockstate/方块模型/两物品模型引用原版 obsidian/nether_star 贴图，免二进制 PNG）+ en_us 三键；Gate A4 集成把 `DemoRegistrar.{BLOCKS,ITEMS,TABS}` 接入 `ModRegistries.ALL`。构造 API 经两节点 jar javap 核同签名（Block/Item/BlockItem/CreativeModeTab）。两节点 `compileJava --rerun-tasks` 绿；两节点 `runClient` 加载零错→抵标题屏→干净退出。**限制**：创造页可视/可放置需人工目视。见平行表 CR-5。**M0（Stage A）收口**。
- **2026-07-21**：**Gate A2 + Gate A3**（集成验证，两节点）。主类 `init` 挂 `ACConfig.register()`（CR-3 计划的 Gate A2 集成）；**Gate A2** = 两节点 `build` 绿 + `runServer`→`Done` + `run/config/abyssalcraft-common.toml` 11 键默认值重生（disk 核）+ 创造栏注册无错；**Gate A3** = 两节点 `runData`→`datagen_smoke.json` 无报错。
- **2026-07-21**：**PA-4（Agent 4）** datagen 基建。新增 `platform/DataGenCompat`（GatherDataEvent 分叉仅 import、body 共享）+ `data/ACDataGenerators`（※ 聚合）+ `data/ACExampleProvider`（vanilla `DataProvider` 空壳）+ `build.gradle.kts` `runConfigs.create("data")` + 主类 datagen 挂载 + en_us 种子。两节点 `runData` 均产出 `src/main/generated/data/abyssalcraft/datagen_smoke.json`（disk 核）、两节点 `build` 绿。API 经 javap/jar 源码核（`RunConfigSettings.data()`、`GatherDataEvent`、`DataProvider.saveStable`）。见平行表 CR-4。
- **2026-07-21**：**PA-2（Agent 2）** 注册聚合骨架 + 创造栏 + 主类挂载。新增 `registry/ModRegistries`（`ALL` 聚合列表）与 `registry/ModCreativeTabs`（空 AC 创造页），主类 `init` 以 `ALL.forEach(r -> r.attach(bus))` 统一挂载；`IEventBus` 仅存于主类，`registry/` 零分叉 API（见平行表 CR-2）。两节点 `build` 绿、6 `.class` 齐、javap 核 `attach` 字节码、forge `runServer`→`Done (2.168s)` CREATIVE_MODE_TAB 冻结无错。旁注（非 PA-2 文件）：`platform/ConfigCompat.java:54` 现有 `ModLoadingContext.get()` [removal] 告警，疑 PA-3 并行引入，待其处理。
- **2026-07-21**：运行期基线验证（服务端双跑，判据 = run 日志 + 退出码）。`:1.20.1-forge:runServer`（`Done (11.082s)`）与 `:1.21.1-neoforge:runServer`（`Done (4.093s)`）均加载 mod、全注册表启动、`stop` 干净关服（BUILD SUCCESSFUL）。核实 **R4**：neoforge `loaderVersion="[4,)"` 被 FML loader 4.0.41 接受。首启 `server.properties` NoSuchFileException 为 vanilla 自动创建、非致命。`runClient` 客户端运行期尚未跑（opt-in）。
- **2026-07-20**：移植计划按用户三项决策修订（`docs/porting/`）：①子系统试点优先 → 新增 **MP 里程碑**（合成机器子系统：结晶器+物质化器+嬗变器，含 JEI）；②**全程双跑** + **版本兼容层优先**（差异全部收敛到 `platform/` compat 层，业务只调包装接口，`//?` 只在层内）；③**JEI 纳入**并前移到 MP。原 M10"neoforge 拉平"降级为"跨版本审计与回归"。
- **2026-07-20**：编制完整移植计划三件套于 `docs/porting/`（设计案 + 总任务表 M0–M11 + 多 agent 平行任务表 + 文件归属矩阵）。确立分层里程碑、混合世界生成方案、集中式 1.21 重命名 helper、最小竖切先行策略。
- **2026-07-20**：初始化多加载器脚手架（Forge 1.20.1 + NeoForge 1.21.1）；生成 Gradle 9.0.0 wrapper；`gradle build` 两节点构建通过并产出 jar。修复 Loom `useLegacyMixinAp`、wrapper `--no-validate-url` 两个坑。

## 10. 决策记录 (Decisions)

- **2026-07-20 · 全程双跑 + 版本兼容层优先**（用户决策）。不再"先 forge 后 neoforge"，每个 Gate 两节点同时 `build`+`run`；所有加载器/版本差异收敛到 `platform/` 一组 compat 类（`ACRef`/`ModRegistrar`/`NetworkChannel`/`CapabilityAccess`/`EventBuses`/`MenuCompat`/`TooltipCompat`/`AttributeCompat`/`BlockEntityCompat`/`SideExecutor`/`DataDirs`），常规代码只调包装接口，`//?` 只在层内（主类 @Mod 除外）。原 M10"neoforge 拉平"降级为"跨版本审计与回归"。
- **2026-07-20 · 子系统试点优先 + JEI 纳入**（用户决策）。M0 后先完整交付 **MP·自定义合成机器子系统**（结晶器+物质化器+嬗变器，含 JEI 配方/燃料分类、datagen、双加载器）作为全工具链模板与回归基线，再按 M1→M11 广度铺开。
- **2026-07-20 · 1.20.1 采用 Forge（而非 NeoForge）**。理由：整套 Skill 库的参考、示例、平台差异映射均围绕 `1.20.1-Forge + 1.21.1-NeoForge` 实测；Stonecutter 的 `//? if forge/neoforge` 与 `loom.platform` 正好一一对应此组合；1.20.1 生态与依赖以 Forge 为主，NeoForge 1.20.1 只是早期近似分叉，收益低、偏离 Skill 风险高。
- **2026-07-20 · 开发记忆放仓库根 `DEVELOPMENT.md`**（本文件）为唯一事实来源，另在 Copilot `/memories/repo/` 放指针，确保跨会话自动查阅与回写。
