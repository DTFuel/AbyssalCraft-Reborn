# <子系统名> 子系统规格 (Subsystem Spec)

> 复制本模板为 `docs/spec/<子系统>.md` 后删除本行提示。用不到的小节可删，但保留标题层级；能一手核实的结论优先写结论 + 证据。

- 里程碑 / Stage：<如 MP / Stage P>
- 关联平行任务：<如 PP-1..PP-5>
- 状态：<规划中 / 进行中 / 已冻结>
- 负责：<agent>
- 最后更新：<YYYY-MM-DD>

## 1. 概述 / 目标

<这个子系统是什么、为什么需要、玩家可见效果。>

## 2. 范围

- 含：<方块 / 物品 / 方块实体 / 实体 / 维度 / 配方 / GUI…>
- 不含：<明确排除项，避免与相邻子系统重叠或职责越界>

## 3. 设计 / 架构

- 包结构：<content/… · registry/… · client/… · integration/…>
- 关键类与职责：<类 → 职责>
- 数据流 / 控制流：<tick、菜单同步、配方匹配、网络往返、维度装配…>

## 4. 子系统内契约

- 注册名 / ID：<block / item / entity / dimension id>
- i18n key：<block.abyssalcraft.* / item.* / entity.*…>
- 网络 / 能力 key：<若有>
- 对外暴露 API：<供其他子系统只读复用的接口 / 基类>

## 5. 跨版本 / 加载器要点

- 触及的兼容层：<platform/… 里哪些 compat；是否需新增>
- javap / jar 核实结论：<1.20.1 ↔ 1.21.1 / Forge ↔ NeoForge 的签名差异与结论>
- `//?` 分叉点：<仅应落在 platform/ 或主类；业务代码应为零分叉>

## 6. 实现记忆 / 踩坑 (verified gotchas)

- <一手证据支撑的坑与结论；标注证据级别（字节码 / 编译报错 / 原文）>

## 7. 验证 / DoD

- 两节点 `build`：<结果>
- 两节点 `run*`：<结果，判据 = 日志 / 退出码 / disk 文件>
- 未机核项（如实标注）：<需人工目视的可视 / 交互部分>

## 修订日志

- <YYYY-MM-DD>：<变更>
